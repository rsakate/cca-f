// CCAR-F Mock Exam â Consolidated Question Bank
// Total: 273 questions across 5 domains and 6 scenarios
// IDs 1-80: Original curated questions with detailed explanations
// IDs 81-273: Extracted from mock tests 1-5 with researched answers
// Auto-generated by scripts/build_all_questions.py

QUESTIONS.push(
{
  id:1, module:1, scenario:"Customer Support Resolution Agent",
  text:"Your agent processes a return request. After calling <code>lookup_order</code>, your agentic loop receives a response with <code>stop_reason</code> set to <code>'end_turn'</code>, but the agent hasn't called <code>process_refund</code> yet. The model's response text says: 'I've found the order details. The order #4521 for a hiking backpack was placed on March 3rd.' What should your orchestration code do?",
  options:[
    "Continue the loop by sending another request to Claude — <code>end_turn</code> simply means 'ready for more input' and the model will proceed to the next step automatically",
    "Present the agent's response to the customer and wait for further instructions — <code>end_turn</code> means the model has decided to stop and communicate before taking further action",
    "Force a <code>process_refund</code> call by setting <code>tool_choice</code> to the specific tool, since the model clearly forgot to continue",
    "Log an error because <code>end_turn</code> should never occur mid-workflow when there are outstanding tools to call"
  ],
  correct:1,
  explanation:"When stop_reason is 'end_turn', the model has deliberately chosen to stop and present information to the user. In this case, the agent is surfacing order details before proceeding — perhaps to confirm the correct order before processing a refund. This is correct agent behavior: verify before acting. Option A misinterprets end_turn — it means the model is done, not 'ready for more.' Option C forces an action the model intentionally deferred. Option D treats normal model behavior as an error."
},
{
  id:2, module:1, scenario:"Customer Support Resolution Agent",
  text:"Your agentic loop receives a response with <code>stop_reason: 'tool_use'</code>. The response content contains a <code>tool_use</code> block requesting <code>lookup_order</code> with input <code>{\\\"order_id\\\": \\\"ORD-7823\\\"}</code>. What is the correct sequence of actions in your orchestration code?",
  options:[
    "Parse the tool name and input from the <code>stop_reason</code> field, execute <code>lookup_order</code>, and return the result directly to the user",
    "Execute <code>lookup_order</code> with the provided input, append the assistant's message to conversation history, append a <code>tool_result</code> message with the output, and send the updated history back to Claude",
    "Queue the tool call for batch execution, continue collecting any additional tool requests, then execute all queued tools simultaneously",
    "Send an acknowledgment message to Claude confirming the tool call was received, then execute the tool and start a new conversation with the results"
  ],
  correct:1,
  explanation:"The correct agentic loop pattern is: (1) extract tool_use blocks from the assistant's response content (not from stop_reason), (2) execute the tool externally, (3) append the full assistant message to conversation history, (4) append a tool_result message with the output, (5) send the complete updated history back to Claude. Option A doesn't continue the loop. Option C risks missing dependencies between sequential tool calls. Option D breaks conversation continuity by starting a new conversation."
},
{
  id:3, module:1, scenario:"Multi-Agent Research System",
  text:"Your agentic loop receives a response with <code>stop_reason: 'pause_turn'</code>. The agent has made 10 tool calls in this turn and appears to be mid-research. What does this indicate and what should your code do?",
  options:[
    "The model has paused to wait for user input before continuing — display a prompt asking the user to confirm continuation",
    "The API rate limit has been hit — implement exponential backoff and retry after a delay",
    "The server-side sampling loop reached its iteration limit; send the assistant's response back as a new message to continue processing where it left off",
    "The model encountered an internal error — retry the entire request from the beginning"
  ],
  correct:2,
  explanation:"'pause_turn' means the server-side agentic loop hit its per-turn iteration cap (default 10 tool calls). Your orchestration code should append the assistant's response to conversation history and send it back to continue where it left off. This is distinct from 'tool_use' (specific tool call request), 'end_turn' (model is done), and rate limiting (which returns HTTP 429). The model's state is preserved — it just needs another turn to keep going."
},
{
  id:4, module:1, scenario:"Multi-Agent Research System",
  text:"Your agentic loop implementation checks for termination by scanning the model's response text for phrases like 'I'm done' or 'task complete.' During testing, you find the agent sometimes says 'I'm done analyzing the first source' (meaning it will continue) and the loop terminates prematurely. What's the correct fix?",
  options:[
    "Add more termination phrases to the scanner to cover edge cases",
    "Use regular expressions to better parse the model's natural language signals",
    "Replace text scanning with the <code>stop_reason</code> field — terminate when <code>stop_reason</code> is <code>'end_turn'</code>, continue when it's <code>'tool_use'</code>",
    "Have the model output a special JSON token like <code>{\\\"done\\\": true}</code> to signal completion"
  ],
  correct:2,
  explanation:"Text scanning for termination is an anti-pattern — natural language is ambiguous ('I'm done analyzing' doesn't mean 'I'm done with everything'). The stop_reason field is the authoritative signal: 'tool_use' means the model wants to call a tool (continue looping), 'end_turn' means it's finished (terminate). Options A and B try to fix an inherently unreliable approach. Option D adds complexity when the API already provides the correct mechanism."
},
{
  id:5, module:1, scenario:"Multi-Agent Research System",
  text:"After running your system on the topic 'impact of AI on creative industries,' each subagent completes successfully: the web search agent finds relevant articles, the document analysis agent summarizes papers correctly, and the synthesis agent produces coherent output. However, the final report covers only visual arts — completely missing music, writing, and film. The coordinator's logs show it decomposed the topic into three subtasks: 'AI in digital art,' 'AI in graphic design,' and 'AI in photography.' What is the most likely root cause?",
  options:[
    "The synthesis agent lacks instructions for identifying coverage gaps in the findings it receives from other agents",
    "The coordinator agent's task decomposition is too narrow, resulting in subagent assignments that don't cover all relevant domains of the topic",
    "The web search agent's queries are not comprehensive enough and need to be expanded to cover more creative industry sectors",
    "The document analysis agent is filtering out sources related to non-visual creative industries due to overly restrictive relevance criteria"
  ],
  correct:1,
  explanation:"The coordinator's logs reveal the root cause directly: it decomposed 'creative industries' into only visual arts subtasks (digital art, graphic design, photography), completely omitting music, writing, and film. The subagents executed their assigned tasks correctly — the problem is what they were assigned. Options A, C, and D incorrectly blame downstream agents that are working correctly within their assigned scope."
},
{
  id:6, module:1, scenario:"Customer Support Resolution Agent",
  text:"You're implementing a hub-and-spoke architecture where the coordinator delegates billing issues to a billing subagent and returns issues to a returns subagent. During testing, you find the billing subagent sometimes processes a refund incorrectly because it doesn't know the customer's loyalty tier, which was established earlier in the coordinator's conversation. What's the root cause and fix?",
  options:[
    "The subagent's system prompt needs to request loyalty tier information before processing refunds",
    "The coordinator must explicitly pass relevant context (customer data, loyalty tier, order details) to the subagent — subagents don't inherit the parent's conversation history",
    "Switch to a single-agent architecture because multi-agent systems can't reliably share state",
    "Store the loyalty tier in a shared database that both the coordinator and subagents query"
  ],
  correct:1,
  explanation:"Subagents created via the Task tool start with a fresh, isolated context. The parent's conversation history is NOT automatically inherited. The coordinator must explicitly include relevant context (customer ID, loyalty tier, order details) in the Task tool's prompt when delegating. Option A would have the subagent ask the customer again, wasting time. Option C is an overreaction — multi-agent works fine with explicit context passing. Option D adds unnecessary infrastructure when the coordinator already has the information."
},
{
  id:7, module:1, scenario:"Multi-Agent Research System",
  text:"During testing, you observe that the synthesis agent frequently needs to verify specific claims while combining findings. Currently, when verification is needed, the synthesis agent returns control to the coordinator, which invokes the web search agent, then re-invokes synthesis with results. This adds 2-3 round trips per task and increases latency by 40%. Your evaluation shows that 85% of these verifications are simple fact-checks while 15% require deeper investigation. What's the most effective approach to reduce overhead while maintaining reliability?",
  options:[
    "Give the synthesis agent a scoped <code>verify_fact</code> tool for simple lookups, while complex verifications continue delegating to the web search agent through the coordinator",
    "Have the synthesis agent accumulate all verification needs and return them as a batch to the coordinator at the end of its pass, which then sends them all to the web search agent at once",
    "Give the synthesis agent access to all web search tools so it can handle any verification need directly without round-trips through the coordinator",
    "Have the web search agent proactively cache extra context around each source during initial research, anticipating what the synthesis agent might need to verify"
  ],
  correct:0,
  explanation:"Option A applies the principle of least privilege by giving the synthesis agent only what it needs for the 85% common case (simple fact verification) while preserving the existing coordination pattern for complex cases. Option B's batching approach creates blocking dependencies since synthesis steps may depend on earlier verified facts. Option C over-provisions the synthesis agent, violating separation of concerns. Option D relies on speculative caching that cannot reliably predict what the synthesis agent will need to verify."
},
{
  id:8, module:1, scenario:"Code Generation with Claude Code",
  text:"You want to restrict a subagent to use only specific tools: Grep, Glob, and Read. How do you configure this using AgentDefinition?",
  options:[
    "Use tool_choice to force only those tools",
    "Set allowedTools: [\\\"Grep\\\", \\\"Glob\\\", \\\"Read\\\"] in the AgentDefinition",
    "Add tool restrictions to CLAUDE.md for the subagent",
    "Create a skill with allowed-tools in the frontmatter"
  ],
  correct:1,
  explanation:"The allowedTools field in AgentDefinition is the correct mechanism to whitelist specific tools for a subagent. This is a hard constraint — the subagent literally cannot call tools outside this list. tool_choice (A) controls which tool to call on a given turn, not which tools are available overall. CLAUDE.md restrictions (C) are prompt-based guidance that can be ignored. Skills with frontmatter (D) is not the correct mechanism for restricting tool access in an AgentDefinition."
},
{
  id:9, module:1, scenario:"Customer Support Resolution Agent",
  text:"Production data shows that in 12% of cases, your agent skips <code>get_customer</code> entirely and calls <code>lookup_order</code> using only the customer's stated name, occasionally leading to misidentified accounts and incorrect refunds. What change would most effectively address this reliability issue?",
  options:[
    "Enhance the system prompt to state that customer verification via <code>get_customer</code> is mandatory before any order operations",
    "Add a programmatic prerequisite that blocks <code>lookup_order</code> and <code>process_refund</code> calls until <code>get_customer</code> has returned a verified customer ID",
    "Add few-shot examples showing the agent always calling <code>get_customer</code> first, even when customers volunteer order details",
    "Implement a routing classifier that analyzes each request and enables only the subset of tools appropriate for that request type"
  ],
  correct:1,
  explanation:"When a specific tool sequence is required for critical business logic (like verifying customer identity before processing refunds), programmatic enforcement provides deterministic guarantees that prompt-based approaches cannot. Options A and C rely on probabilistic LLM compliance, which is insufficient when errors have financial consequences. Option D addresses tool availability rather than tool ordering, which is not the actual problem."
},
{
  id:10, module:1, scenario:"Customer Support Resolution Agent",
  text:"Your agent's system prompt says: 'Always verify customer identity before processing any refund.' Despite this, production logs show that in 8% of cases the agent skips verification when customers provide detailed order information upfront. The team wants a 0% bypass rate for compliance. What enforcement mechanism achieves this?",
  options:[
    "Add the instruction in bold and repeat it three times in the system prompt for emphasis",
    "Add few-shot examples showing the agent verifying identity even when the customer provides order details",
    "Implement a <code>PreToolUse</code> hook that inspects the conversation history before <code>process_refund</code> executes and blocks it if <code>get_customer</code> hasn't been called",
    "Use <code>tool_choice: {type: 'tool', name: 'get_customer'}</code> for every turn to force identity verification"
  ],
  correct:2,
  explanation:"A PreToolUse hook provides deterministic enforcement with a 0% bypass rate — the refund literally cannot execute unless verification has occurred. This is the key distinction between programmatic enforcement (hooks, gates) and prompt-based guidance (options A and B), which are probabilistic and can be overridden by model reasoning. Option D would force get_customer on every turn including non-refund interactions, breaking the agent's ability to handle other requests."
},
{
  id:11, module:1, scenario:"Customer Support Resolution Agent",
  text:"Your agent uses a <code>PostToolUse</code> hook on the <code>lookup_order</code> tool. The hook receives raw order data containing timestamps in three formats (Unix epoch, ISO 8601, and 'MM/DD/YYYY' strings) from different backend systems. What should this hook do?",
  options:[
    "Log the format inconsistencies and pass the raw data through unchanged for the model to interpret",
    "Normalize all timestamps to a single ISO 8601 format before the model processes the tool result, ensuring consistent data regardless of backend source",
    "Reject the tool result and return an error asking the backend team to fix their timestamp formats",
    "Add metadata tags to each timestamp indicating its original format so the model can choose which to use"
  ],
  correct:1,
  explanation:"PostToolUse hooks are ideal for data normalization — transforming heterogeneous formats into a consistent shape before the model sees them. This ensures the agent always works with clean, uniform data regardless of which backend system responded. Option A forces the model to handle format differences, adding complexity and error risk. Option C fails the customer's request due to a backend issue. Option D adds complexity without solving the underlying inconsistency."
},
{
  id:12, module:1, scenario:"Multi-Agent Research System",
  text:"Your coordinator routes research tasks to subagents. For a task on 'renewable energy policy in the EU,' it needs to decide whether to invoke just the web search subagent, just the document analysis subagent, or both. How should you structure this routing decision?",
  options:[
    "Hard-code routing rules: policy topics always go to both subagents, technical topics go to web search only",
    "Let the coordinator use model-driven reasoning to decide dynamically based on the specific research question, available subagents, and the type of information needed",
    "Always invoke all subagents for every query to maximize coverage, filtering irrelevant results later",
    "Create a lookup table in <code>.claude/rules/</code> mapping topic keywords to specific subagent combinations"
  ],
  correct:1,
  explanation:"Model-driven routing lets the coordinator flexibly decide which subagents to invoke based on the specific question and context. For an EU policy question, the coordinator might reason that both web search (for recent developments) and document analysis (for official policy documents) are needed. Option A creates brittle rules that can't handle nuance. Option C wastes resources on irrelevant work. Option D can't anticipate all topic-subagent combinations."
},
{
  id:13, module:1, scenario:"Code Generation with Claude Code",
  text:"You're designing a multi-step code generation workflow. Step 1 generates a module skeleton, Step 2 adds implementations, and Step 3 creates tests. All steps depend on the previous output. Should you use prompt chaining or dynamic task decomposition?",
  options:[
    "Both equally; the choice doesn't matter for structured workflows",
    "Prompt chaining, since each step follows predictably from the previous one",
    "Prompt chaining only if you can predict all possible outcomes in Step 1",
    "Dynamic task decomposition, which adapts based on intermediate results"
  ],
  correct:1,
  explanation:"Prompt chaining is the right pattern when steps are sequential and predictable. Each step (skeleton, implementation, tests) follows a known order with a clear dependency chain, making prompt chaining ideal. Dynamic task decomposition (D) is better for ambiguous tasks where the next step depends on analysis of intermediate results, not for a fixed three-step pipeline. The choice does matter (A) since prompt chaining avoids the overhead of dynamic planning. Option C adds an unnecessary condition — prompt chaining works well for predictable sequential workflows regardless of outcome variability."
},
{
  id:14, module:1, scenario:"Developer Productivity with Claude",
  text:"An engineer proposes using <code>fork_session</code> to explore two different refactoring approaches for the authentication module, which they previously analyzed in an earlier session. Each fork would explore a different approach independently. Why is <code>fork_session</code> particularly well-suited here?",
  options:[
    "fork_session is faster than starting new sessions because it pre-loads the model weights",
    "fork_session creates independent branches from the existing context, preserving the prior analysis while preventing cross-contamination between the two exploration paths",
    "fork_session automatically merges the results of both explorations into a single recommendation",
    "fork_session allows both approaches to share tool results in real time while maintaining separate conversation threads"
  ],
  correct:1,
  explanation:"fork_session creates independent context branches from a shared starting point. Both forks inherit the existing analysis of the authentication module, so the engineer doesn't need to re-explain the context. The branches are fully isolated — exploring approach A won't pollute the context for approach B. Option A mischaracterizes the mechanism. Option C is wrong — merging is the engineer's job. Option D contradicts the isolation purpose."
},
{
  id:15, module:1, scenario:"Customer Support Resolution Agent",
  text:"A customer returns four hours after their initial session. Resuming with stale tool results causes the agent to reference outdated information even after fresh tool calls provide newer facts. What approach most reliably handles returning customers?",
  options:[
    "Start a new session, inject a structured summary of the previous interaction, then make fresh tool calls before engaging.",
    "Resume with full history and add a system prompt telling the agent to always prefer the most recent tool results.",
    "Resume with full history but filter out previous tool_result messages, keeping only the human and assistant turns.",
    "Resume with full history and automatically re-call all previously used tools at session start."
  ],
  correct:0,
  explanation:"Starting a new session with a structured summary avoids the stale data problem entirely. The summary preserves key context (customer ID, issue history, prior resolution attempts) without carrying forward outdated tool results that could confuse the model. Fresh tool calls then provide current data. Resuming with full history and a preference prompt (B) is unreliable since models may still reference earlier tool results. Filtering tool_result messages (C) creates conversation gaps that break the model's understanding. Re-calling all tools (D) is wasteful and still leaves stale results in context alongside fresh ones."
},
{
  id:16, module:1, scenario:"Claude Code for Continuous Integration",
  text:"A Claude Code session is running a complex refactor. Partway through, you need to pause and resume later. What should you use to continue the session?",
  options:[
    "Save the session output and use it as a new prompt",
    "Start a new session and manually repeat prior steps",
    "The --resume flag to continue the session with previous context intact",
    "Use fork_session to create a parallel continuation"
  ],
  correct:2,
  explanation:"The --resume flag is specifically designed to continue an existing Claude Code session with all previous context intact. This preserves the refactoring progress, tool results, and conversation history so work can continue seamlessly. Saving output as a new prompt (A) loses tool state and conversation context. Manually repeating steps (B) is inefficient and error-prone. fork_session (D) creates a branch for parallel exploration, not for simple pause-and-resume of a single workflow."
},
{
  id:61, module:1, scenario:"Multi-Agent Research System",
  text:"Your expense reimbursement agent uses a <code>process_reimbursement</code> tool. Policy requires reimbursements above $500 to be manager-approved before funds are disbursed, and the threshold enforcement must be tamper-proof. Which design ensures the $500 threshold cannot be bypassed?",
  options:[
    "Provide separate tools for auto-reimburse and request-manager-approval, with prompt instructions telling the agent which to call",
    "Use an <code>approved_by_manager</code> boolean parameter and tell the agent to set it only after approval",
    "Use a PreToolUse hook that inspects the amount and modifies the tool call to add a <code>requires_approval</code> flag",
    "Have <code>process_reimbursement</code> enforce the threshold internally — auto-disbursing amounts at or below $500 and creating a pending approval request for larger amounts"
  ],
  correct:3,
  explanation:"The most tamper-proof approach is enforcement within the tool itself. The tool checks the amount programmatically: ≤$500 auto-disburses, >$500 creates a pending approval. This cannot be bypassed by prompt manipulation or agent reasoning. Separate tools with prompt instructions (A) rely on probabilistic agent behavior. A boolean parameter (B) can be set incorrectly by the agent. A PreToolUse hook (C) adds enforcement but the tool-internal approach is the most self-contained and tamper-proof."
},
{
  id:62, module:1, scenario:"Developer Productivity with Claude",
  text:"A developer asks the agent to investigate why a specific API endpoint intermittently returns 500 errors. The codebase is large and the error must be traced through routing, middleware, business logic, and database layers. What task decomposition approach would be most effective?",
  options:[
    "Run parallel workers for all layers from the start and synthesize the results",
    "Dynamically generate investigation subtasks based on discoveries at each step and adapt the plan as new information emerges",
    "Create a comprehensive plan mapping all code paths before reading any files",
    "Use a fixed investigation sequence regardless of what intermediate findings show"
  ],
  correct:1,
  explanation:"Dynamic task decomposition is ideal for investigation-type tasks where each step's findings inform the next. Finding a null check failure in middleware might redirect investigation to the database layer — a rigid plan can't anticipate this. Option A wastes resources investigating irrelevant layers. Option C adds overhead without value since you can't plan what you don't know. Option D ignores findings that should change the approach."
},
{
  id:63, module:1, scenario:"Developer Productivity with Claude",
  text:"When your agent uses <code>Bash</code> to run tests during code generation, how should it handle test failures in the agentic loop?",
  options:[
    "Parse the test output, identify the failure reason, and attempt to fix the code automatically before re-running tests",
    "Show the failure output to the user and ask them to fix it",
    "Ignore failures and assume the code works",
    "Use <code>tool_choice: 'any'</code> to force a tool call that understands the failure"
  ],
  correct:0,
  explanation:"In an agentic loop, test failures are actionable feedback. The agent should parse Bash output to understand what failed, then use Read and Edit to fix the code, and re-run tests to verify — this is the core iterate-until-passing pattern. Option B breaks the autonomous workflow. Option C produces broken code. Option D misuses tool_choice for a problem that needs code analysis, not tool forcing."
},
{
  id:17, module:2, scenario:"Customer Support Resolution Agent",
  text:"You have a tool called get_customer that's critical for retrieving customer details. Your tool description currently says: 'Retrieves customer information.' A team member says this is sufficient. Why might this description be inadequate for reliable agent behavior?",
  options:[
    "The description is fine; agents don't need detailed tool descriptions to use tools correctly",
    "Tool descriptions should only be one sentence for brevity",
    "Detailed descriptions slow down the model's reasoning process",
    "The description is too vague. It doesn't specify what parameters are required, what output format to expect, what edge cases exist (e.g., customer not found), or when to use this tool vs. other alternatives"
  ],
  correct:3,
  explanation:"Tool descriptions are the primary mechanism LLMs use to decide when and how to call tools. A vague description like 'Retrieves customer information' gives the model no guidance on required parameters, expected output format, edge cases, or disambiguation from similar tools. Detailed descriptions with these elements dramatically improve tool selection accuracy and error handling."
},
{
  id:18, module:2, scenario:"Developer Productivity with Claude",
  text:"You're defining the JSON schema for a <code>create_ticket</code> tool's <code>priority</code> parameter. The values should be 'low', 'medium', 'high', or 'critical'. What's the best schema approach?",
  options:[
    "Define <code>priority</code> as a free-text <code>string</code> with instructions in the description to use one of the four values",
    "Define <code>priority</code> as a <code>string</code> with an <code>enum: ['low', 'medium', 'high', 'critical']</code> constraint",
    "Define <code>priority</code> as an <code>integer</code> from 1-4 and map to labels in code",
    "Define <code>priority</code> as a <code>boolean</code> (true = high priority, false = low)"
  ],
  correct:1,
  explanation:"An enum constraint guarantees the model outputs one of the valid values — the schema enforces it structurally, not just via instructions. Option A allows arbitrary strings ('urgent', 'ASAP', etc.) the model might generate. Option C loses semantic meaning (what does 3 mean?). Option D can't represent 4 priority levels."
},
{
  id:19, module:2, scenario:"Customer Support Resolution Agent",
  text:"Your tool <code>update_shipping_address</code> succeeds but has a side effect: it also triggers a re-routing of the package, adding 2 days to delivery. The tool currently returns <code>{success: true}</code>. The agent tells the customer 'Your address has been updated!' without mentioning the delivery delay. How should you fix the tool response?",
  options:[
    "Add the delay information to the tool description so the model always mentions it",
    "Return a richer response: <code>{success: true, side_effects: [{type: 'delivery_delay', additional_days: 2, new_estimated_delivery: '2024-03-15'}]}</code>",
    "Add a PostToolUse hook that appends a reminder about delivery delays",
    "Create a separate tool <code>check_delivery_impact</code> that the agent should call after address updates"
  ],
  correct:1,
  explanation:"The tool response should include all consequential information — the model can only communicate what it knows. Returning side effects in the response data ensures the model has the information to give the customer a complete picture. Option A is static and can't include dynamic details like the new date. Option C adds complexity. Option D requires the model to remember to call a second tool."
},
{
  id:20, module:2, scenario:"Customer Support Resolution Agent",
  text:"You're designing the process_refund tool. Your backend returns different failure modes: 'Insufficient funds in account', 'Account locked pending fraud review', and 'Refund window expired'. How should you structure the tool's error response?",
  options:[
    "Return success even on failure and embed error codes in the result for the agent to parse",
    "Use isError: true with errorCategory (transient, non_retryable, etc.), isRetryable (boolean), and a human-readable details message so the agent can reason about next steps",
    "Return all error messages as plain text strings without categorization",
    "Throw exceptions and let the SDK handle error translation automatically"
  ],
  correct:1,
  explanation:"Structured error responses with isError: true, errorCategory, isRetryable, and human-readable details give the agent full context to reason about recovery. For example, 'fraud review' is transient and retryable later, while 'refund window expired' is permanent. Returning success on failure misleads the agent, plain text lacks machine-parseable structure, and exceptions bypass the agent's reasoning."
},
{
  id:21, module:2, scenario:"Customer Support Resolution Agent",
  text:"Your MCP tool <code>search_orders</code> executes a database query that returns 0 rows for a valid customer who has no orders. The tool currently returns <code>isError: true</code> with 'No orders found.' Claude then apologizes for the error and suggests the customer try again later. What's wrong?",
  options:[
    "The error message should be more specific about why no orders were found",
    "The tool should return <code>isError: false</code> with <code>resultCount: 0</code> — an empty result from a successful query is NOT an error",
    "The tool should retry the query with broader search parameters before reporting no results",
    "The tool should return <code>isError: true</code> but with an <code>isRetryable: false</code> flag to prevent Claude from suggesting retries"
  ],
  correct:1,
  explanation:"A successful query that returns 0 rows is fundamentally different from a query that fails. Marking it as isError: true causes the model to treat it as a failure (apologizing, suggesting retries) instead of correctly informing the customer they have no orders. Return isError: false with the result count so the model can respond appropriately: 'I don't see any orders on your account.'"
},
{
  id:22, module:2, scenario:"Developer Productivity with Claude",
  text:"You're building an MCP server and a tool handler throws an unhandled Python exception. What happens at the protocol level?",
  options:[
    "The MCP framework catches it and returns a <code>CallToolResult</code> with <code>isError: true</code> containing the exception message",
    "The exception becomes a protocol-level error (not a tool error), which means Claude sees it as a system failure rather than a tool failure, preventing intelligent recovery",
    "The MCP server crashes and Claude Code automatically restarts it",
    "The exception is silently swallowed and Claude receives an empty successful response"
  ],
  correct:1,
  explanation:"Unhandled exceptions in MCP tool handlers become protocol-level errors, not tool-level errors. This means Claude sees them as infrastructure failures rather than actionable tool feedback. The correct pattern is to catch exceptions in your handler and return a CallToolResult with isError: true and a descriptive message, enabling Claude to reason about the failure and recover."
},
{
  id:23, module:2, scenario:"Customer Support Resolution Agent",
  text:"Your main support agent is configured with allowedTools: ['get_customer', 'lookup_order', 'process_refund', 'escalate_to_human']. You're concerned about scope creep where the agent might attempt operations outside its domain. What's the implication of this scoped tool access?",
  options:[
    "The agent will attempt all possible operations but these four will have priority",
    "The agent can only call these four tools; any attempt to call tools outside this list will fail at runtime",
    "The agent's system prompt is restricted, but it can request tools outside this list",
    "Tool scoping is only a recommendation; the agent will ignore it if it seems necessary"
  ],
  correct:1,
  explanation:"allowedTools is an enforcement mechanism, not a suggestion. When an agent is configured with a scoped tool list, any attempt to call a tool outside that list will fail at runtime. This provides a hard boundary preventing scope creep, unlike system prompt guidance which the model could potentially bypass."
},
{
  id:24, module:2, scenario:"Customer Support Resolution Agent",
  text:"You want the agent to dynamically decide which tool to call based on the situation. You're choosing between tool_choice: 'auto' and tool_choice: 'any'. What's the practical difference?",
  options:[
    "'auto' and 'any' are equivalent; both let the model choose any available tool",
    "'auto' uses model heuristics to decide whether and when to call tools; 'any' forces the model to always call a tool in every response, even if it might not need one",
    "'any' is more restrictive and prevents the model from calling certain tools",
    "'auto' prevents the model from using tools at all"
  ],
  correct:1,
  explanation:"tool_choice 'auto' lets the model decide whether to call a tool at all -- it may respond with text instead if appropriate. tool_choice 'any' forces the model to make a tool call in every response, even when a text response would suffice. This distinction matters when you need guaranteed structured output (use 'any') vs. flexible conversation (use 'auto')."
},
{
  id:25, module:2, scenario:"Structured Data Extraction",
  text:"You're using <code>tool_choice: 'auto'</code> in your agent. During testing, you notice that in 15% of cases Claude responds with a text explanation instead of calling the required <code>extract_data</code> tool, saying 'Based on the document, I can see that...' followed by prose. What's the fix?",
  options:[
    "Add stronger instructions in the system prompt: 'ALWAYS use the extract_data tool. NEVER respond with text.'",
    "Switch to <code>tool_choice: {type: 'tool', name: 'extract_data'}</code> to force the tool call, eliminating the possibility of text-only responses",
    "Add retry logic that detects text-only responses and re-sends the request",
    "Increase the temperature to encourage more diverse behavior including tool usage"
  ],
  correct:1,
  explanation:"With tool_choice 'auto', the model can choose not to call tools — which it does 15% of the time. Switching to forced tool selection guarantees the model calls extract_data on every request. Option A is probabilistic and won't achieve 100%. Option C wastes API calls. Option D increases randomness but doesn't fix the fundamental issue."
},
{
  id:26, module:2, scenario:"Customer Support Resolution Agent",
  text:"Your MCP server defines tools at the user level (~/.claude.json). Your team wants different projects to have different sets of tools. For example, one project uses process_refund, another uses process_replacement_order. How should you structure MCP server scoping?",
  options:[
    "Define all tools in ~/.claude.json and let each project's agent choose which ones to use via tool_choice",
    "Store tool definitions in a database and fetch them at runtime based on the project",
    "Require each project to copy the full ~/.claude.json and edit it locally",
    "Define project-level .mcp.json files so each project can have scoped tool access independent of others"
  ],
  correct:3,
  explanation:"Project-level .mcp.json files allow each project to define its own scoped set of MCP tools independently. This avoids polluting the global config with project-specific tools, prevents tool leakage between projects, and doesn't require manual copying or runtime fetching. Each project gets exactly the tools it needs."
},
{
  id:27, module:2, scenario:"Code Generation with Claude Code",
  text:"You want Claude Code to use the built-in <code>Glob</code>, <code>Grep</code>, <code>Read</code>, and <code>Edit</code> tools to explore and refactor a TypeScript codebase. A junior developer asks why not just load all files into the prompt upfront. What's the key advantage of the incremental exploration pattern?",
  options:[
    "The built-in tools are faster than reading files into the prompt",
    "Loading all files upfront consumes the context window with potentially irrelevant content, while the find -> read-only-what-matters -> change loop keeps context focused on relevant code",
    "The built-in tools provide syntax highlighting that the prompt doesn't",
    "Loading files into the prompt is not supported — you must use the built-in tools"
  ],
  correct:1,
  explanation:"The incremental exploration pattern (Glob to find files -> Read to inspect relevant ones -> Edit to change) keeps the context window focused on what matters. Loading everything upfront ('just in case') wastes context on irrelevant files, potentially pushing important content out of the model's attention window. This is especially critical for large codebases."
},
{
  id:64, module:2, scenario:"Multi-Agent Research System",
  text:"Your <code>remove_team_member</code> tool uses a <code>dry_run</code> parameter for previewing impacts before execution, but the agent sometimes bypasses the preview step. You need to ensure every removal is preceded by a preview that the user explicitly confirms. What is the most reliable approach?",
  options:[
    "Add detailed instructions and few-shot examples telling the agent to always call dry_run first",
    "Annotate the tool as requiring confirmation and rely on the orchestration layer to prompt the user",
    "Add server-side validation that permits execution only if an identical preview occurred recently",
    "Replace with a two-step flow: <code>preview_remove_member</code> returns impact details and a single-use confirmation token, and <code>execute_remove_member</code> requires that token"
  ],
  correct:3,
  explanation:"The two-step confirmation token pattern makes it architecturally impossible to execute without previewing first. The execute step requires a token that only the preview step produces, creating a cryptographic dependency chain. Prompt instructions (A) are probabilistic — the agent can still skip them. Orchestration-layer confirmation (B) doesn't guarantee the preview step ran. Server-side validation (C) is weaker than token-based linking since it relies on time-based matching rather than direct cryptographic coupling."
},
{
  id:65, module:2, scenario:"Multi-Agent Research System",
  text:"Your <code>update_game_score</code> tool accepts <code>game_date</code>, <code>home_team</code>, and <code>away_team</code>, but production logs show issues with nicknames, date formats, and selecting the wrong game when teams have rematches. What interface change would most effectively prevent these errors?",
  options:[
    "Add <code>season</code> and <code>confirm_before_update</code> parameters",
    "Replace the parameters with a <code>game_id</code> and a separate <code>search_games</code> lookup tool that returns matching IDs",
    "Add enum constraints for team names and a regex pattern for the date",
    "Add detailed examples showing the required date format and complete list of official team names"
  ],
  correct:1,
  explanation:"Replacing ambiguous parameters (team names, dates) with a unique game_id and a separate search_games lookup tool eliminates the class of errors entirely. The lookup returns candidate games the user can confirm, and the update uses an unambiguous ID. Adding constraints (C) still allows format edge cases. Examples (D) help but can't cover all nicknames. Additional parameters (A) add complexity without solving disambiguation."
},
{
  id:66, module:2, scenario:"Multi-Agent Research System",
  text:"Your <code>search_catalog</code> tool fails sometimes due to network timeouts that often succeed on retry and sometimes due to malformed user filters that never succeed on retry. Currently both errors are returned identically. How should the tool's error handling be modified?",
  options:[
    "Add few-shot examples to the system prompt explaining how to handle different error types",
    "Apply exponential backoff retries to all errors uniformly",
    "Return all errors with a <code>retryable</code> boolean and error type details",
    "Implement automatic retry with backoff for network timeouts inside the tool and return syntax errors immediately with parameter validation details"
  ],
  correct:3,
  explanation:"The most effective approach handles each error type at the appropriate level: transient network errors are retried internally (the agent never needs to know), while permanent errors like malformed filters are returned immediately with clear validation details so the agent can fix the parameters. Option B wastes time retrying permanent errors. Option C exposes transient retry logic to the agent unnecessarily. Option A relies on prompt-based guidance."
},
{
  id:67, module:2, scenario:"Developer Productivity with Claude",
  text:"Your agent needs to insert a helper function into the middle of a utility module, but the <code>Edit</code> tool cannot find unique text to match because the file contains repetitive patterns. What is the most reliable way to complete the insertion?",
  options:[
    "Use Edit with a very long <code>old_string</code> to try to force uniqueness",
    "Use <code>Read</code> to load the file, insert the function in the right place, then <code>Write</code> the updated file",
    "Append the function at the end of the file using Bash",
    "Use <code>replace_all</code> and embed the new function in the replacement text"
  ],
  correct:1,
  explanation:"When Edit fails due to non-unique text patterns, the reliable fallback is Read→modify→Write. Read loads the full file content, you insert the function at the correct position, and Write replaces the file. Option A may still fail if the surrounding context is also repetitive. Option C puts the function in the wrong location. Option D replaces all instances of a pattern, which could cause unintended changes throughout the file."
},
{
  id:74, module:2, scenario:"Multi-Agent Research System",
  text:"Your <code>log_workout</code> tool accepts <code>exercise_type</code>, <code>value</code>, and <code>measurement</code>, and the agent frequently passes invalid combinations such as reps for cardio and miles for strength exercises. What would most effectively reduce these errors?",
  options:[
    "Split the interface into <code>log_cardio_workout</code> and <code>log_strength_workout</code> with parameters appropriate to each category",
    "Implement server-side validation with descriptive error messages",
    "Add enum constraints on the measurement field",
    "Add explicit examples to the tool description showing valid combinations"
  ],
  correct:0,
  explanation:"Splitting into category-specific tools eliminates invalid combinations at the interface level. log_cardio_workout only accepts duration/distance parameters; log_strength_workout only accepts sets/reps. The agent can't mix them because the wrong parameter simply doesn't exist on the tool. Server-side validation (B) catches errors after the fact. Enum constraints (C) still allow mismatches between exercise type and measurement. Examples (D) are probabilistic guidance."
},
{
  id:75, module:2, scenario:"Multi-Agent Research System",
  text:"Your resource allocation tool returns only a simple acknowledgment after provisioning is requested. Users often confirm without understanding what they approved. What design change would most effectively address this?",
  options:[
    "Add a <code>user_acknowledged</code> boolean parameter that must be set true",
    "Add a <code>detail_level</code> parameter controlling how much context the agent presents",
    "Return structured data including cost estimate, target project, resource specifications, and impact summary",
    "Implement a hold period before execution completes"
  ],
  correct:2,
  explanation:"Returning structured details (cost, project, specs, impact) gives the agent the information needed to present a meaningful confirmation to the user. Users can then make informed decisions rather than blindly approving. A boolean parameter (A) doesn't add information. A detail_level parameter (B) puts the burden on the agent to choose. A hold period (D) delays but doesn't inform."
},
{
  id:76, module:2, scenario:"Multi-Agent Research System",
  text:"Your document analysis subagent has access to both a <code>summarize_content</code> and <code>analyze_document</code> tool. The model frequently calls <code>summarize_content</code> when <code>analyze_document</code> would be more appropriate. Why is this happening and how do you fix it?",
  options:[
    "The model is hallucinating; add error handling to reject summarize_content calls",
    "The tool descriptions are ambiguous — expand them to clarify when to use each tool and provide example queries for each",
    "The subagent's system prompt doesn't mention analyze_document; add it explicitly",
    "Restrict the subagent to only the analyze_document tool using allowedTools"
  ],
  correct:1,
  explanation:"When two tools have overlapping descriptions, the model can't reliably distinguish when to use each. The fix is expanding descriptions to clarify boundaries: 'summarize_content — for generating brief overviews of known documents. Do NOT use for detailed analysis' vs. 'analyze_document — for deep analysis including themes, structure, and key findings. Use when the user needs detailed understanding.' Adding example queries for each reinforces the distinction. Option D removes a useful tool. Option C is insufficient — system prompts don't fix ambiguous tool definitions."
},
{
  id:28, module:3, scenario:"Code Generation with Claude Code",
  text:"Your project has a <code>CLAUDE.md</code> at the root defining team standards, and a <code>src/auth/CLAUDE.md</code> with strict security rules for the authentication module. When Claude Code edits a file in <code>src/auth/</code>, which rules apply?",
  options:[
    "Only the <code>src/auth/CLAUDE.md</code> rules — more specific files completely override the root",
    "Only the root <code>CLAUDE.md</code> — subdirectory files are only read if explicitly imported",
    "Both apply — the root CLAUDE.md provides baseline rules and the subdirectory CLAUDE.md layers additional, more specific rules on top",
    "Claude Code randomly selects one file depending on which it reads first"
  ],
  correct:2,
  explanation:"CLAUDE.md files follow a layered hierarchy: User (~/.claude/CLAUDE.md) → Project (./CLAUDE.md) → Local (./subfolder/CLAUDE.md). More specific levels add to (not replace) broader ones. So editing in src/auth/ would apply both the root team standards AND the auth-specific security rules."
},
{
  id:29, module:3, scenario:"Customer Support Resolution Agent",
  text:"Your Claude Code project has a CLAUDE.md file with custom system instructions and rules for the support agent. You're adding a new project dependency (a special tool for handling warranty claims). Should you add the warranty tool definition to the root CLAUDE.md or use the @import syntax?",
  options:[
    "Always add everything to the root CLAUDE.md to keep configuration centralized",
    "Use @import to modularize the CLAUDE.md so different features (refunds, returns, warranty claims) are defined in separate files and imported as needed",
    "Create a separate CLAUDE.md in each subdirectory and ignore the root file",
    "Configuration should never be split across files; it violates best practices"
  ],
  correct:1,
  explanation:"The @import syntax allows you to modularize CLAUDE.md by splitting configuration into separate, focused files that are imported as needed. This keeps each file manageable and lets teams maintain feature-specific configurations (refunds, returns, warranty) independently without cluttering the root file."
},
{
  id:30, module:3, scenario:"Developer Productivity with Claude",
  text:"You're setting up Claude Code for a new team. The project has a committed <code>CLAUDE.md</code> with team standards. One developer has personal preferences in <code>~/.claude/CLAUDE.md</code>. They also have <code>CLAUDE.local.md</code> at the project root for local overrides. What's <code>CLAUDE.local.md</code> for?",
  options:[
    "It overrides all other configuration including user-level settings",
    "It's the main configuration file — <code>CLAUDE.md</code> is deprecated",
    "It provides personal, per-project configuration tweaks and is automatically gitignored, so each developer can customize behavior without affecting the team's shared <code>CLAUDE.md</code>",
    "It only applies during local development; <code>CLAUDE.md</code> applies in CI/CD"
  ],
  correct:2,
  explanation:"CLAUDE.local.md is for personal, per-project tweaks that shouldn't be shared with the team. It's automatically gitignored. A developer might use it to add their preferred debugging tools or personal code style overrides without modifying the team's committed CLAUDE.md."
},
{
  id:31, module:3, scenario:"Code Generation with Claude Code",
  text:"You want to create a custom <code>/review</code> slash command that runs your team's standard code review checklist. This command should be available to every developer when they clone or pull the repository. Where should you create this command file?",
  options:[
    "In the <code>CLAUDE.md</code> file at the project root",
    "In <code>~/.claude/commands/</code> in each developer's home directory",
    "In the <code>.claude/commands/</code> directory in the project repository",
    "In a <code>.claude/config.json</code> file with a commands array"
  ],
  correct:2,
  explanation:"Project-scoped custom slash commands should be stored in .claude/commands/ within the repository. These commands are version-controlled and automatically available to all developers when they clone or pull. Option B (~/.claude/commands/) is for personal commands not shared via version control. Option A (CLAUDE.md) is for project instructions, not command definitions. Option D describes a mechanism that doesn't exist."
},
{
  id:32, module:3, scenario:"Code Generation with Claude Code",
  text:"Your custom command <code>/generate-endpoint</code> needs the developer to specify the endpoint name. They type <code>/generate-endpoint users</code>. How does the command template receive the 'users' argument?",
  options:[
    "Via a <code>{{param}}</code> template syntax in the command file",
    "Via a JSON schema definition alongside the command",
    "Via the <code>$ARGUMENTS</code> placeholder in the command markdown file — 'users' replaces <code>$ARGUMENTS</code> in the prompt",
    "Arguments aren't supported — the command must ask for the endpoint name interactively"
  ],
  correct:2,
  explanation:"Custom commands use $ARGUMENTS as a placeholder in the markdown template. When the user types /generate-endpoint users, 'users' replaces $ARGUMENTS in the prompt. This is the built-in parameterization mechanism — no JSON schema or template syntax needed."
},
{
  id:33, module:3, scenario:"Developer Productivity with Claude",
  text:"Your team wants recurring chores (code review, test generation, changelog updates) available as one-word commands. For code review, you want it to run <code>Grep</code>, <code>Read</code>, and <code>Glob</code> but NOT <code>Bash</code>, <code>Edit</code>, or <code>Write</code>. How do you configure this?",
  options:[
    "Create a <code>.claude/commands/review.md</code> file with the review prompt and add <code>allowed-tools: [Grep, Read, Glob]</code> in the frontmatter",
    "Add the tool restrictions to CLAUDE.md with a conditional: 'When running /review, only use Grep, Read, and Glob'",
    "Create the command without tool restrictions and trust the review prompt to only use read operations",
    "Create a skill instead of a command, since only skills support tool restrictions"
  ],
  correct:0,
  explanation:"Custom commands support allowed-tools in their frontmatter, restricting which tools can be used during execution. This ensures the review command can only read code, never modify it. Option B is prompt-based, not enforced. Option C trusts probabilistic behavior. Option D is incorrect — both commands and skills support tool restrictions."
},
{
  id:34, module:3, scenario:"Code Generation with Claude Code",
  text:"Your codebase has distinct areas with different coding conventions: React components use functional style with hooks, API handlers use async/await with specific error handling, and database models follow a repository pattern. Test files are spread throughout the codebase (e.g., <code>Button.test.tsx</code> next to <code>Button.tsx</code>). You want all tests to follow the same conventions regardless of location. What's the most maintainable approach?",
  options:[
    "Create rule files in <code>.claude/rules/</code> with YAML frontmatter specifying glob patterns to conditionally apply conventions based on file paths",
    "Consolidate all conventions in the root <code>CLAUDE.md</code> file under headers for each area, relying on Claude to infer which section applies",
    "Create skills in <code>.claude/skills/</code> for each code type that include the relevant conventions in their SKILL.md files",
    "Place a separate <code>CLAUDE.md</code> in each subdirectory containing that area's specific conventions"
  ],
  correct:0,
  explanation:"Option A is correct because .claude/rules/ with glob patterns (e.g., **/*.test.tsx) allows conventions to be automatically applied based on file paths regardless of directory location — essential for test files spread throughout the codebase. Option B relies on inference rather than explicit matching, making it unreliable. Option C requires manual skill invocation. Option D can't easily handle files spread across many directories since CLAUDE.md files are directory-bound."
},
{
  id:35, module:3, scenario:"Code Generation with Claude Code",
  text:"You have a skill that checks code complexity metrics. Its <code>SKILL.md</code> uses <code>paths: ['**/*.service.ts']</code> in the frontmatter. A developer working on <code>Button.tsx</code> can't find the skill in the available commands. Why?",
  options:[
    "The skill has a syntax error in its SKILL.md file",
    "The paths field scopes when the skill is visible — it only appears when the developer is editing files matching <code>**/*.service.ts</code>, saving context tokens when working on unrelated files",
    "Skills are only available in plan mode",
    "The developer needs to install the skill separately using <code>/install-skill</code>"
  ],
  correct:1,
  explanation:"The paths field in SKILL.md scopes skill visibility. It only loads into context when the user is working on files matching the glob pattern (*.service.ts), saving token budget. Since the developer is editing Button.tsx, which doesn't match, the skill isn't shown. This is intentional — it keeps the available skills list relevant to current work."
},
{
  id:36, module:3, scenario:"Code Generation with Claude Code",
  text:"You've been assigned to restructure the team's monolithic application into microservices. This will involve changes across dozens of files and requires decisions about service boundaries and module dependencies. Which approach should you take?",
  options:[
    "Enter plan mode to explore the codebase, understand dependencies, and design an implementation approach before making changes",
    "Start with direct execution and make changes incrementally, letting the implementation reveal the natural service boundaries",
    "Use direct execution with comprehensive upfront instructions detailing exactly how each service should be structured",
    "Begin in direct execution mode and only switch to plan mode if you encounter unexpected complexity during implementation"
  ],
  correct:0,
  explanation:"Plan mode is designed for complex tasks involving large-scale changes, multiple valid approaches, and architectural decisions — exactly what monolith-to-microservices restructuring requires. It enables safe codebase exploration and design before committing to changes. Option B risks costly rework when dependencies are discovered late. Option C assumes you already know the right structure without exploring the code. Option D ignores that the complexity is already stated in the requirements."
},
{
  id:37, module:3, scenario:"Developer Productivity with Claude",
  text:"Your team uses the 'interview pattern' when working with Claude Code on unfamiliar codebases. A junior developer asks what this means. Which description is correct?",
  options:[
    "Claude Code asks the developer a series of questions about the codebase before making any changes, surfacing design considerations the developer may not have anticipated",
    "The developer interviews Claude Code about the codebase architecture before starting work",
    "Claude Code runs automated tests and 'interviews' the test output to understand the codebase",
    "The developer provides a structured questionnaire that Claude Code fills out about each file"
  ],
  correct:0,
  explanation:"The interview pattern has Claude ask questions to surface design considerations the developer may not have anticipated — cache invalidation strategies, failure modes, edge cases. This is especially valuable in unfamiliar domains where the developer might miss important considerations. It's a way to leverage Claude's broad knowledge before committing to an implementation approach."
},
{
  id:38, module:3, scenario:"Code Generation with Claude Code",
  text:"Your team's code generation process is producing duplicated variable names in some cases. What's the best approach to improve consistency?",
  options:[
    "Tell Claude Code to check for duplicates before completing",
    "Provide test cases showing expected variable naming and how duplicates should be handled",
    "Add naming convention rules to CLAUDE.md",
    "Use a post-processing linter to rename duplicates"
  ],
  correct:1,
  explanation:"Providing concrete test cases that demonstrate expected naming conventions and duplicate handling gives Claude Code unambiguous examples to follow. This is more effective than vague instructions (Option A), static rules without examples (Option C), or post-hoc fixes that don't address the root cause (Option D)."
},
{
  id:77, module:3, scenario:"Developer Productivity with Claude",
  text:"Your agent is tasked with refactoring a large function that spans 200 lines. What's the best approach?",
  options:[
    "Read the entire function, refactor in one go using Write",
    "Use multiple small <code>Edit</code> operations to refactor step-by-step, testing after each change",
    "Generate the refactored version, write it, then read it back to verify",
    "Use a <code>tool_choice</code> loop: first read and analyze, then propose changes to the user"
  ],
  correct:1,
  explanation:"Multiple small Edit operations with testing between each change is the most reliable approach for large refactors. Each Edit is a focused, verifiable change — if a test fails, you know exactly which edit caused it and can fix it immediately. Option A risks producing a broken file with many issues at once. Option C doesn't verify correctness between steps. Option D requires user interaction for each change, breaking the automated workflow."
},
{
  id:78, module:3, scenario:"Claude Code for Continuous Integration",
  text:"Your CI test generation system suggests test cases. How can you provide examples to improve consistency in test naming, structure, and coverage patterns?",
  options:[
    "Describe test naming conventions and best practices in the prompt",
    "Reference test generation guidelines in CLAUDE.md",
    "Include several existing test examples in the prompt showing naming, structure, and comprehensive coverage",
    "Ask Claude Code to follow the project's testing style"
  ],
  correct:2,
  explanation:"Including actual test examples in the prompt gives Claude Code concrete patterns to follow — exact naming conventions, assertion styles, edge case coverage, and file structure. This is few-shot prompting applied to test generation. Describing conventions (A) is vague. CLAUDE.md references (B) provide rules but not concrete patterns. Asking to follow style (D) gives no specifics about what that style looks like."
},
{
  id:39, module:3, scenario:"Claude Code for Continuous Integration",
  text:"Your CI pipeline needs Claude Code to output review results as structured JSON that a downstream script (<code>review_gate.py</code>) can parse to make pass/fail decisions. What flags achieve this?",
  options:[
    "<code>-p</code> with <code>--verbose</code> for detailed output that includes JSON",
    "<code>-p</code> with <code>--output-format json</code> to produce machine-parseable structured output",
    "<code>-p</code> with <code>--json</code> to enable JSON output mode",
    "<code>-p</code> with <code>--format structured</code> for structured output"
  ],
  correct:1,
  explanation:"-p enables non-interactive mode and --output-format json produces structured JSON output that CI/CD pipelines can parse programmatically. This combination is the standard pattern for integrating Claude Code into automated workflows. The other options reference non-existent flags."
},
{
  id:40, module:4, scenario:"Claude Code for Continuous Integration",
  text:"You want code review feedback to be specific and testable. Instead of 'improve error handling,' what should you specify?",
  options:[
    "\\\"Flag when exceptions are caught but not logged or re-raised\\\" with code examples",
    "\\\"Review error handling quality\\\"",
    "\\\"Ensure code is robust against failures\\\"",
    "\\\"Check for good error handling practices\\\""
  ],
  correct:0,
  explanation:"Explicit, testable criteria like 'Flag when exceptions are caught but not logged or re-raised' produce consistent, actionable feedback. Vague instructions like 'review quality' or 'ensure robustness' lead to inconsistent and unhelpful results because the model interprets them differently each time."
},
{
  id:41, module:4, scenario:"Code Generation with Claude Code",
  text:"You're building a code review system and getting too many false positives flagging suspicious patterns that are actually correct. What's the best approach?",
  options:[
    "Increase the confidence threshold to filter lower-confidence findings",
    "Use a veto process where engineers manually approve each finding",
    "Tell Claude Code to reduce false positives in the system prompt",
    "Define specific review criteria for each category and temporarily disable high false-positive categories"
  ],
  correct:3,
  explanation:"Defining specific criteria and temporarily disabling problematic categories addresses false positives at the root cause. Specific criteria reduce ambiguity that causes false flags, while disabling noisy categories preserves developer trust while you refine the prompts."
},
{
  id:42, module:4, scenario:"Structured Data Extraction",
  text:"After implementing tool use with strict schema definitions, syntax errors disappear, but some extractions still have valid JSON with empty strings or null values for required fields even though the source documents contain that information in varied formats. What is the most effective way to address these failures?",
  options:[
    "Add few-shot examples showing extractions from documents with varied structures and how to locate the required information",
    "Build a regex-based post-processing layer that scans source documents for likely patterns",
    "Implement retry logic that re-sends requests when validation detects empty required fields",
    "Modify the schema to make those fields optional and flag incomplete records for manual review"
  ],
  correct:0,
  explanation:"Few-shot examples showing varied input structures teach the model where to find information in different document layouts. The issue isn't schema compliance (tool_use handles that) but the model not recognizing where data appears in unfamiliar formats. Examples bridge that gap."
},
{
  id:43, module:4, scenario:"Structured Data Extraction",
  text:"Your extraction system parses e-commerce product descriptions to extract fields such as dimensions, weight, and materials. The model inconsistently extracts the materials field and sometimes omits it even when the information is present. What is the most effective way to improve consistency?",
  options:[
    "Set temperature to zero for deterministic output",
    "Make the materials field required instead of optional",
    "Add few-shot examples showing complete input-output pairs with standardized material descriptions",
    "Switch to a more capable model tier"
  ],
  correct:2,
  explanation:"Few-shot examples with standardized material descriptions show the model exactly how to extract and format the materials field from varied product descriptions. This addresses the root cause: the model not recognizing diverse material descriptions as extraction targets."
},
{
  id:44, module:4, scenario:"Structured Data Extraction",
  text:"The extraction pipeline receives multiple document types, each with its own extraction tool and schema. With tool_choice set to 'auto', the model sometimes returns conversational text instead of calling a tool. You need guaranteed structured output without knowing the document type in advance. What is the most effective approach?",
  options:[
    "Add a preliminary classification call, then make a second call with tool_choice forced to the identified extraction tool",
    "Set tool_choice to 'any' with all extraction tools defined",
    "Consolidate all document types into a single unified schema extraction tool and force that tool",
    "Keep tool_choice as 'auto' and add system prompt instructions requiring tool use"
  ],
  correct:1,
  explanation:"Setting tool_choice to 'any' forces the model to always call one of the defined tools, guaranteeing structured output. The model selects the most appropriate extraction tool based on the document content. This avoids the extra latency and cost of a preliminary classification call while ensuring a tool is always called."
},
{
  id:45, module:4, scenario:"Structured Data Extraction",
  text:"Your extraction needs to handle receipts where the currency isn't always explicit. Some show '$' (could be USD, CAD, AUD), some show 'EUR', some have no currency symbol at all. Your schema currently has <code>currency</code> as a required enum of ['USD', 'EUR', 'GBP']. What's the best schema improvement?",
  options:[
    "Add all possible currencies to the enum",
    "Change <code>currency</code> to a free-text string so the model can write whatever it sees",
    "Add <code>'unclear'</code> to the enum and make a <code>currency_source</code> field noting where the currency was inferred from (e.g., 'symbol', 'document header', 'not specified')",
    "Remove the currency field entirely and let downstream systems figure it out"
  ],
  correct:2,
  explanation:"Adding 'unclear' to the enum lets the model express uncertainty rather than guessing. The currency_source field provides provenance so downstream systems know whether the currency was explicitly stated or inferred. This follows the 'other/unclear + detail' pattern for handling ambiguity."
},
{
  id:46, module:4, scenario:"Structured Data Extraction",
  text:"Your extraction system retries when validation fails, appending the validation error to the prompt each time. For which failure pattern would additional retries be least effective?",
  options:[
    "The model outputs 'et al.' for co-authors when the full list exists only in an external document not present in the input",
    "The model outputs a nested object where the schema requires a flat array",
    "Model outputs locale-formatted strings where the schema requires integers",
    "The model outputs ISO 8601 datetimes where the schema requires only dates"
  ],
  correct:0,
  explanation:"Retries are ineffective when the required information simply doesn't exist in the provided input. If co-author names are only in an external document not included in the context, no amount of retrying will produce them. The other options are format mismatches that the model can correct with error feedback."
},
{
  id:47, module:4, scenario:"Structured Data Extraction",
  text:"Monitoring shows many extractions fail Pydantic validation with specific errors such as 'expected float for quantity but got a range value like 2 to 3.' Retrying without modification produces identical failures. What is the most effective recovery?",
  options:[
    "Send a follow-up request including the validation error and ask the model to correct its output",
    "Use a larger model tier to reprocess failed documents",
    "Set temperature to zero to ensure consistent formatting",
    "Pre-process source documents to standardize problematic formats before extraction"
  ],
  correct:0,
  explanation:"Sending a follow-up request with the specific validation error gives the model targeted feedback about what went wrong and how to fix it. Retrying without modification repeats the same error, while pre-processing or model changes don't address the model's interpretation of ambiguous values."
},
{
  id:48, module:4, scenario:"Claude Code for Continuous Integration",
  text:"Your CI pipeline generates thousands of test cases across many PRs. The standard API would be very expensive. Which Claude API feature should you use?",
  options:[
    "Message Batches API for 50% cost savings on non-interactive requests",
    "Cache API to reuse test generation logic",
    "Streaming API to reduce token usage",
    "Clustering API to group similar test requests"
  ],
  correct:0,
  explanation:"The Message Batches API provides 50% cost savings on non-interactive, latency-tolerant requests. CI test generation is a perfect fit since results don't need to be real-time. Caching, streaming, and clustering don't address the core cost issue for high-volume batch workloads."
},
{
  id:49, module:4, scenario:"Structured Data Extraction",
  text:"Your batch processing job for 10,000 documents completes, but 200 documents failed extraction. Each failed result is identified by its <code>custom_id</code>. What's the correct recovery approach?",
  options:[
    "Resubmit the entire batch of 10,000 documents to ensure consistency",
    "Identify the 200 failed documents by their <code>custom_id</code> values, analyze the failure patterns, fix the issues, and resubmit only the failed subset as a new batch",
    "Skip the failed documents and report 98% success rate",
    "Switch the failed documents to synchronous processing for faster turnaround"
  ],
  correct:1,
  explanation:"custom_id fields exist for correlating batch request/response pairs so you can identify failures and resubmit only what failed. Analyze failure patterns first (context limits? unsupported format?) and fix before resubmitting. Resubmitting the entire batch wastes API calls on already-successful extractions."
},
{
  id:50, module:4, scenario:"Claude Code for Continuous Integration",
  text:"Your CI system includes Claude Code's findings in pull request comments. Different team members see duplicate or contradictory comments in the same PR. Why are independent review instances better than self-review?",
  options:[
    "Independent instances require less configuration than a single instance",
    "Independent instances eliminate bias completely",
    "Independent instances run in parallel, completing reviews faster",
    "Independent instances catch issues the other misses; fresh perspective reduces blind spots from retained reasoning context"
  ],
  correct:3,
  explanation:"Independent review instances each evaluate code with a fresh perspective, catching issues that a single reviewer might miss due to its own reasoning blind spots. A model retains its reasoning context from the initial analysis, making it biased toward its own conclusions. A fresh independent instance evaluates findings without that bias."
},
{
  id:51, module:4, scenario:"Claude Code for Continuous Integration",
  text:"Your CI pipeline runs automated code review on every PR. For a codebase with hundreds of files changed, how should you structure Claude Code to avoid overwhelming developers with feedback?",
  options:[
    "Run per-file analysis first, then cross-file integration checks in separate passes",
    "Run multiple independent reviewers and aggregate findings",
    "Analyze all files together in one comprehensive review for consistency",
    "Review only files that changed significantly to reduce noise"
  ],
  correct:0,
  explanation:"Per-file analysis first, then cross-file integration checks, structures the review into manageable passes. This prevents context overload from analyzing hundreds of files at once and produces more focused, accurate feedback at each level."
},
{
  id:68, module:4, scenario:"Structured Data Extraction",
  text:"Your extraction pipeline processes contracts with amendments. The model inconsistently extracts either the original term or the amended term, with no indication of which applies. What is the most effective approach?",
  options:[
    "Implement post-extraction validation to detect amendments and flag those cases for review",
    "Redesign the schema so amended fields can capture multiple values, each with source location and effective date",
    "Preprocess documents to remove superseded sections before extraction",
    "Add prompt instructions to always extract the most recent amendment value and ignore superseded terms"
  ],
  correct:1,
  explanation:"Redesigning the schema to capture multiple values with source location and effective date handles amendments structurally. Each extracted value carries provenance (where in the document, when effective), letting downstream systems determine which applies. Option A catches problems but doesn't fix them. Option C risks losing important historical context. Option D forces a single-value choice that may not be appropriate for all use cases."
},
{
  id:69, module:4, scenario:"Structured Data Extraction",
  text:"Your extraction system needs to extract nested data (e.g., invoice items with line-item details). How should the schema handle this?",
  options:[
    "Extract only top-level fields; nested data is too complex",
    "Ask the user to specify nesting structure for each document",
    "Flatten nested data into separate fields",
    "Use nested objects in JSON schema: <code>items</code> (array of <code>{product_id, quantity, price}</code>) with validation at each level"
  ],
  correct:3,
  explanation:"JSON schema natively supports nested objects and arrays. Defining items as an array of objects with typed, validated fields at each level preserves the natural structure of the data. Flattening (C) loses relationships between line-item fields. Avoiding nesting (A) misses critical data. User-specified structure (B) doesn't scale."
},
{
  id:70, module:4, scenario:"Claude Code for Continuous Integration",
  text:"Your team wants to use Claude Code for code review but worries about false positives creating noise. Which strategy is most effective for building trust?",
  options:[
    "Run preview mode first, then apply findings manually",
    "Start with a small set of high-confidence checks, expand gradually as accuracy builds trust",
    "Implement all possible checks immediately to maximize coverage",
    "Use manual approval for all findings to ensure accuracy"
  ],
  correct:1,
  explanation:"Starting with a small set of high-confidence checks builds developer trust incrementally. Early wins with accurate, actionable findings establish credibility. As the team sees value, you expand to additional check categories. Implementing everything at once (C) overwhelms with false positives and erodes trust. Preview mode (A) adds friction without building confidence in automated findings. Manual approval (D) doesn't scale."
},
{
  id:52, module:5, scenario:"Customer Support Resolution Agent",
  text:"Your agent handles a conversation that spans 25 turns. The customer's first message established they're a Premium Gold member with a $10,000 annual spending history. By turn 20, the agent applies a Standard tier discount instead of the Gold tier discount, costing the customer $50. The <code>/compact</code> summary had condensed 'Premium Gold member' to 'valued customer.' What's the root cause and fix?",
  options:[
    "The model's context window is too small for 25-turn conversations",
    "Progressive summarization risk: /compact condensed critical transactional data (exact tier, spending level) into vague generalizations. The fix is extracting these facts into a persistent [CASE FACTS] block or scratchpad that is re-injected every turn, outside the summarizable conversation",
    "The agent should confirm the customer's tier on every turn",
    "The /compact algorithm has a bug that loses membership information"
  ],
  correct:1,
  explanation:"This is a classic progressive summarization risk: lossy compression turns precise facts into vague labels. 'Premium Gold member, $10K annual spend' becomes 'valued customer,' losing the information needed for correct discount application. The fix is separating critical facts from the conversation stream into a persistent block (CASE FACTS or scratchpad) that survives summarization."
},
{
  id:53, module:5, scenario:"Customer Support Resolution Agent",
  text:"Your agent encounters a long support document (50 pages) that it needs to analyze. The document is loaded into context, but the model consistently misses information from pages 20–35, finding details from the beginning and end but omitting the middle section. What phenomenon is this?",
  options:[
    "The document exceeded the token limit and pages 20–35 were truncated",
    "The 'lost in the middle' effect: models reliably process information at the beginning and end of long inputs but may omit findings from middle sections",
    "The model is rate-limited and skipping portions to save compute",
    "The document formatting is broken in the middle pages"
  ],
  correct:1,
  explanation:"The 'lost in the middle' effect is a known limitation: models reliably process the beginning and end of long inputs but may underweight or miss information from middle sections. Mitigations include: placing key findings summaries at the beginning, using explicit section headers, or splitting long documents into separate processing passes."
},
{
  id:54, module:5, scenario:"Customer Support Resolution Agent",
  text:"Your <code>lookup_order</code> tool returns a 200-field JSON object for every order query, but the agent only uses 5 fields (status, items, total, shipping, tracking) for 95% of interactions. After 8 tool calls, the accumulated tool results consume 60% of the context window, pushing early conversation turns out of the model's effective attention. What pattern fixes this?",
  options:[
    "Increase the context window to accommodate larger tool results",
    "Route every tool result through an <code>optimize(tool_name, raw)</code> function that strips irrelevant fields before the model sees them, reducing each result from 200 fields to the ~5 relevant ones",
    "Ask the model to ignore fields it doesn't need",
    "Modify the backend API to return fewer fields"
  ],
  correct:1,
  explanation:"Tool output optimization trims verbose results to only relevant fields before they enter the context window. A 200-field JSON becomes 5 fields, dramatically reducing token consumption and keeping earlier conversation turns within the model's attention. Option A is expensive and doesn't scale. Option C doesn't reduce token count. Option D requires backend changes outside your control and might break other consumers."
},
{
  id:55, module:5, scenario:"Code Generation with Claude Code",
  text:"Your Claude Code integration sends a long system prompt (2,500 tokens of coding guidelines) plus tool definitions (1,800 tokens) on every API request. The system prompt and tools rarely change, but each request is billed for the full input. After 50 requests in a session, you've paid to process those 4,300 static tokens 50 times. What optimization reduces this cost?",
  options:[
    "Shorten the system prompt by removing less-important guidelines",
    "Place all static content (system prompt, tool definitions) at the beginning of the messages array and mark the last static block with <code>cache_control: { type: 'ephemeral' }</code>. Subsequent requests within the TTL (~5 minutes) pay only a cache-read fee instead of full input processing for those tokens",
    "Send the system prompt only on the first request and omit it from subsequent ones",
    "Move the system prompt into a tool description so it's only processed when the tool is called"
  ],
  correct:1,
  explanation:"Prompt caching with cache_control marks a prefix breakpoint. Static content (system prompt, tool definitions) placed before the breakpoint is cached and reused within the TTL (~5 minutes). Subsequent requests pay a reduced cache-read fee (~0.1x) instead of full input cost for the cached prefix. The key requirement: static content must come first, dynamic content (conversation turns) must come after. Option C breaks coherence — the model needs the system prompt every turn. Option D misunderstands tool definitions."
},
{
  id:56, module:5, scenario:"Multi-Agent Research System",
  text:"Your multi-agent healthcare claims pipeline processes claims through three stages: Intake, Validation, and Adjudication. When the Validation subagent encounters a timeout calling an external eligibility API, it raises a Python exception. The coordinator catches it as a generic <code>Exception</code> and logs 'Validation failed.' Investigation reveals 30% of 'failures' were actually transient timeouts that would succeed on retry. What's the architectural fix?",
  options:[
    "Add automatic retry logic with exponential backoff for all exceptions",
    "Wrap every subagent result in a <code>StageResult(stage, ok, data, error)</code> envelope — subagents never raise exceptions. The coordinator receives structured context (stage, failure type, partial results) and decides whether to log, retry, or escalate",
    "Have subagents handle all retries internally before returning results",
    "Add a global exception handler that retries any failed stage up to 3 times"
  ],
  correct:1,
  explanation:"StageResult envelopes ensure failures are always data, never exceptions. The coordinator receives structured context: which stage failed, why (timeout vs. invalid data), and any partial results. This enables intelligent decisions: retry timeouts, escalate validation errors, continue with partial data. Option A retries indiscriminately. Option C hides retry decisions from the coordinator. Option D doesn't distinguish transient from permanent failures."
},
{
  id:57, module:5, scenario:"Structured Data Extraction",
  text:"After deployment, you find that some extractions contain semantic errors that pass schema validation, and reviewers can check only a subset of outputs. Which approach most effectively allocates reviewer attention?",
  options:[
    "Randomly sample 20 percent of extractions for review",
    "Review all extractions from documents with formatting anomalies",
    "Have the model output field-level confidence scores and calibrate review thresholds using a labeled validation set",
    "Prioritize review of all extractions where required fields are empty or marked not found"
  ],
  correct:2,
  explanation:"Field-level confidence scores let the system prioritize reviewer attention on the extractions most likely to contain errors. Calibrating thresholds against a labeled validation set ensures the confidence scores correlate with actual accuracy, making review allocation data-driven rather than arbitrary or surface-level."
},
{
  id:58, module:5, scenario:"Customer Support Resolution Agent",
  text:"A customer says 'cancel my order' but has two open orders: ORD-5521 (headphones, $89) and ORD-5534 (laptop stand, $149). Your agent picks the cheaper order and cancels it. The customer is furious — they wanted to cancel the laptop stand. What design change prevents this?",
  options:[
    "Always cancel the most recent order when the customer doesn't specify",
    "Always cancel the most expensive order since customers are more likely to want to cancel high-value items",
    "Instruct the agent to ask for clarification when a request is ambiguous: 'I see two open orders — the headphones ($89) and the laptop stand ($149). Which one would you like to cancel?'",
    "Cancel both orders to be safe and let the customer re-order the one they wanted to keep"
  ],
  correct:2,
  explanation:"When a request is under-specified ('cancel my order' with multiple open orders), the agent should ask which order they mean rather than applying a heuristic. Any heuristic (cheapest, newest, most expensive) will be wrong in some cases. Asking takes one extra turn but prevents costly mistakes. Option D is extreme and creates a terrible customer experience."
},
{
  id:59, module:5, scenario:"Multi-Agent Research System",
  text:"Your synthesis agent combines findings from three research subagents. The web search agent reports 'revenue grew 15% in 2023,' while the document analysis agent reports 'revenue grew 12% in 2023' from a different source. The current synthesis just picks one number. What's the correct approach?",
  options:[
    "Always use the number from the most recent source",
    "Average the two numbers and report 13.5% growth",
    "Annotate the conflict with source attribution: 'Revenue grew 15% according to [Source A, Q3 press release] vs. 12% per [Source B, annual report]. The discrepancy may reflect different measurement periods or definitions.'",
    "Omit the revenue growth figure entirely since it's contested"
  ],
  correct:2,
  explanation:"When credible sources conflict, the synthesis should annotate the conflict with source attribution rather than arbitrarily selecting one value. This preserves provenance and lets the reader understand the discrepancy. Option A applies an arbitrary heuristic. Option B creates a number that no source actually reported. Option D loses potentially important information."
},
{
  id:60, module:5, scenario:"Multi-Agent Research System",
  text:"Your synthesis agent produces a report on renewable energy but some sections are marked 'insufficient data — the wind energy subagent timed out before completing research on offshore installations.' A reviewer complains that these gaps aren't clearly indicated in the report summary. What output structure helps?",
  options:[
    "Remove sections with incomplete data from the report entirely",
    "Add coverage annotations to the synthesis output indicating which findings are well-supported versus which topic areas have gaps due to unavailable sources or subagent failures",
    "Retry the failed subagent until it succeeds and only then generate the report",
    "Lower quality standards so partial data is acceptable"
  ],
  correct:1,
  explanation:"Coverage annotations explicitly mark which sections are well-supported and which have gaps. Rather than hiding incomplete coverage (A) or blocking on retries (C), the report transparently communicates what it knows well and where gaps exist. This lets the reader make informed decisions about which findings to trust."
},
{
  id:71, module:5, scenario:"Structured Data Extraction",
  text:"Your extraction system has operated with full human review for months. High-confidence extractions appear highly accurate overall, and you want to automate those cases. Before deploying, what validation step is most critical?",
  options:[
    "Analyze accuracy by document type and field to confirm high-confidence performance is consistent across segments",
    "Compare accuracy at different confidence thresholds to find an optimal cutoff",
    "Verify that aggregate accuracy meets downstream requirements",
    "Run a pilot that routes some high-confidence extractions directly downstream and monitor reports"
  ],
  correct:0,
  explanation:"Aggregate accuracy can mask segment-level failures. A system that's 98% accurate overall might be only 60% accurate on a specific document type or field. Before automating, you must verify that high-confidence performance is consistent across all segments (document types, fields, edge cases). Threshold comparison (B) and aggregate verification (C) don't catch segment disparities. A pilot (D) is a good follow-up step but should come after segment analysis."
},
{
  id:72, module:5, scenario:"Structured Data Extraction",
  text:"Your extraction model is hallucinating dates — creating dates that don't appear in the source document. How should you address this?",
  options:[
    "Use validation to reject dates that don't match document patterns",
    "Add a <code>required_evidence</code> field to the schema; dates without source evidence are flagged as hallucinations",
    "Accept the hallucinations; downstream systems can validate",
    "Add a note in the system prompt: 'Do not hallucinate dates'"
  ],
  correct:1,
  explanation:"A required_evidence field forces the model to cite where in the source document each date appears. Dates without evidence are flagged as potential hallucinations, enabling automated detection. Pattern-based validation (A) can miss novel hallucinations that look plausible. Downstream validation (C) passes the problem along. Prompt instructions (D) are probabilistic and cannot guarantee compliance."
},
{
  id:73, module:5, scenario:"Developer Productivity with Claude",
  text:"A codebase exploration tool stores session IDs so engineers can continue investigations. An engineer wants to resume a prior session, but some previously read files changed overnight. What approach best balances efficiency and accuracy?",
  options:[
    "Resume and immediately re-read all previously analyzed files",
    "Start a completely new session from scratch",
    "Resume without mentioning the changed files",
    "Resume and inform the agent which specific files changed for targeted reanalysis"
  ],
  correct:3,
  explanation:"Informing the agent which specific files changed enables targeted reanalysis — the agent re-reads only what's different rather than everything. This balances efficiency (no full re-read) with accuracy (stale analysis is refreshed). Re-reading everything (A) is wasteful when most files haven't changed. A new session (B) loses all prior context. Ignoring changes (C) risks acting on stale analysis."
},
{
  id:79, module:5, scenario:"Structured Data Extraction",
  text:"Your extraction system must maintain accuracy over time as documents evolve. What monitoring approach helps detect degradation?",
  options:[
    "Assume accuracy stays constant",
    "Re-test the full sample suite quarterly against the current model",
    "Track extraction confidence trends; if average confidence drops, investigate",
    "Sample extractions monthly and manually compare against prior results to detect drift"
  ],
  correct:2,
  explanation:"Tracking confidence trends provides continuous, automated monitoring. A sudden or gradual confidence drop signals that document formats or content patterns have shifted, triggering investigation before accuracy degrades significantly. Option A ignores drift entirely. Option B checks only quarterly — too slow to catch emerging issues. Option D is labor-intensive and infrequent compared to automated trend monitoring."
},
{
  id:80, module:5, scenario:"Developer Productivity with Claude",
  text:"Your agent generates boilerplate code files that should follow specific naming conventions and be placed in specific directories. How should you ensure correctness?",
  options:[
    "Use <code>Bash</code> to run linters or validation scripts that check naming and placement",
    "All of the above — use Glob to verify files exist with correct names, Read to inspect content, and Bash to run linters",
    "Generate files without validation; rely on users to verify",
    "Use <code>Glob</code> to check if files with correct naming exist, then verify via <code>Read</code>"
  ],
  correct:1,
  explanation:"A comprehensive validation approach combines multiple built-in tools: Glob to verify files were created with correct naming patterns, Read to inspect that file content matches templates, and Bash to run linters or validation scripts for language-specific checks. Each tool validates a different aspect — name/location (Glob), content (Read), and code quality (Bash). Using only one tool or skipping validation entirely misses failure modes."
},
{
  id:81, module:5, scenario:"Developer Productivity with Claude",
  text:"An engineer previously analyzed a legacy authentication module and identified two distinct refactoring approaches. They now want to explore both in depth before deciding. What is the most effective way to structure this exploration?",
  options:[
    "Start two fresh sessions and manually summarize yesterday‘s findings into each.",
    "Resume the old session and explore both approaches sequentially in one conversation.",
    "Resume the old session for the first approach, then start a new one for the second.",
    "Use fork_session to create two branches from the prior analysis and explore one approach in each fork."
  ],
  correct:3,
  explanation:"fork_session creates branching explorations from existing context, allowing both approaches to be explored independently while preserving the prior analysis. This avoids manual summarization and context loss."
},
{
  id:82, module:1, scenario:"Multi-Agent Research System",
  text:"When the coordinator agent calls a subagent using the Task tool, what is NOT automatically inherited by the subagent?",
  options:[
    "User authentication credentials",
    "System prompt configuration",
    "Parent session context and conversation history",
    "Tool definitions available to the agent"
  ],
  correct:2,
  explanation:"Subagents created via the Task tool get their own isolated context. Parent session context and conversation history are NOT inherited — the subagent starts fresh with only what is explicitly passed in the Task call."
},
{
  id:83, module:1, scenario:"Multi-Agent Research System",
  text:"Your coordinator detects that one research subagent is making repeated incorrect tool calls. What is the most reliable way to enforce compliance at the architecture level?",
  options:[
    "Modify the subagent‘s system prompt to include stricter rules",
    "Add a validation step in the coordinator that rejects bad requests",
    "Reduce the number of tools available to that subagent via allowedTools",
    "Use a PostToolUse hook to validate tool calls before they execute"
  ],
  correct:2,
  explanation:"allowedTools is an architecture-level enforcement mechanism that restricts which tools a subagent can invoke. This is more reliable than prompt instructions or coordinator-level validation because it prevents bad calls entirely rather than catching them after the fact."
},
{
  id:84, module:1, scenario:"Multi-Agent Research System",
  text:"Your web search subagent needs to refine its queries based on initial results before synthesizing findings. How should you structure this in the agentic loop?",
  options:[
    "Let the subagent iterate internally - it sees tool results, evaluates them, and calls the search tool again if needed",
    "Create a prompt chain that executes all searches sequentially without feedback",
    "Have the coordinator re-invoke the subagent multiple times with updated prompts",
    "Queue all queries in advance and execute them in parallel"
  ],
  correct:0,
  explanation:"In the agentic loop, the subagent naturally iterates: it calls a tool, sees the result (stop_reason=tool_use), evaluates it, and decides whether to call the tool again or produce a final answer (stop_reason=end_turn). This is the core agentic loop pattern."
},
{
  id:85, module:1, scenario:"Multi-Agent Research System",
  text:"Your synthesis subagent needs to work from research conducted by both the web search and document analysis subagents. What is the recommended pattern for providing this information?",
  options:[
    "Pass the coordinator‘s summary of both subagents‘ findings in the Task tool call to the synthesis subagent",
    "Have the synthesis subagent invoke both the web search and document analysis tools directly",
    "Chain subagent calls: first get results, then invoke synthesis with those results passed as context",
    "Have each subagent store results in a shared database that the synthesis agent queries"
  ],
  correct:0,
  explanation:"In hub-and-spoke orchestration, the coordinator collects results from subagents and passes a consolidated summary to the synthesis subagent via the Task tool call. This keeps context focused and avoids giving the synthesis agent unnecessary tool access."
},
{
  id:86, module:1, scenario:"Multi-Agent Research System",
  text:"You want the coordinator to detect when a subagent‘s response is incomplete and automatically trigger a refinement loop. Where should this logic be implemented?",
  options:[
    "In a separate validation microservice called after all subagents complete",
    "In the coordinator‘s agentic loop - after receiving the subagent‘s result via the Task tool, the coordinator evaluates completeness and re-invokes if needed",
    "In the subagent‘s system prompt as conditional rules",
    "In a PostToolUse hook attached to the Task tool"
  ],
  correct:1,
  explanation:"The coordinator's agentic loop is the natural place for this logic: after receiving a subagent's result via the Task tool, the coordinator evaluates completeness and re-invokes the subagent if needed. This is the standard refinement loop pattern in multi-agent orchestration."
},
{
  id:87, module:1, scenario:"Multi-Agent Research System",
  text:"Your coordinator needs to route research tasks to subagents, but some tasks might be better served by multiple subagents. How should you structure this decision-making?",
  options:[
    "Queue all possible subagent invocations and execute in parallel regardless of relevance",
    "Create a decision tree in .claude/rules/ that lists all topic-to-subagent mappings",
    "Hard-code rules in the coordinator‘s system prompt: ‘Topic X always goes to subagent A‘",
    "Let the coordinator use model-driven reasoning to decide: based on the research question and available subagents, decide dynamically how to partition work"
  ],
  correct:3,
  explanation:"Dynamic decomposition lets the coordinator use model-driven reasoning to decide how to partition work across subagents based on the research question. This is more flexible than hard-coded rules and adapts to varying task requirements."
},
{
  id:88, module:1, scenario:"Multi-Agent Research System",
  text:"Your coordinator implements a refinement loop: after evaluating findings, if gaps exist, re-invoke the subagent with specific gap prompts. How many re-invocation rounds should you allow before considering the research ‘good enough‘?",
  options:[
    "Use adaptive stopping: continue if new information is discovered, stop when returns diminish",
    "Unlimited; iterate until the subagent reports completion",
    "Set a hard limit (e.g., max 3 rounds) to control costs and latency",
    "Re-invoke once per unique gap identified, then synthesize the result"
  ],
  correct:2,
  explanation:"Setting a hard limit (e.g., max 3 rounds) is the recommended approach to control costs and latency in refinement loops. Unlimited iteration risks runaway costs, while adaptive stopping is harder to implement reliably."
},
{
  id:89, module:5, scenario:"Developer Productivity with Claude",
  text:"An engineer who just joined the team asks the agent to help them understand the authentication and authorization architecture across a large multi service codebase. What exploration strategy most effectively builds understanding under context limits?",
  options:[
    "Use Grep to find authentication entry points, read those files, and follow imports and function calls incrementally.",
    "Read every file containing auth related keywords in content or filename.",
    "Launch parallel subagents to explore different services simultaneously and then synthesize their findings.",
    "Read CLAUDE.md and README files first, then ask the engineer to identify the most important files."
  ],
  correct:0,
  explanation:"Using Grep to find authentication entry points and then following imports incrementally is the most effective strategy under context limits. It builds understanding progressively without loading unnecessary files, unlike reading all files or requiring engineer guidance."
},
{
  id:90, module:2, scenario:"Developer Productivity with Claude",
  text:"Engineers often want the agent to cross reference code changes with Jira tickets during reviews. The team wants the agent to access standard Jira data directly rather than via manual copy paste. What is the most effective approach?",
  options:[
    "Build a custom MCP server around Jira‘s API for this workflow.",
    "Integrate an existing Jira MCP server that exposes tickets, comments, and metadata as tools.",
    "Export Jira tickets to markdown files in the repository.",
    "Use Bash and curl directly against Jira‘s REST API."
  ],
  correct:1,
  explanation:"Integrating an existing Jira MCP server is the most effective approach — it provides structured tool access to Jira data without building custom infrastructure. MCP servers are the standard mechanism for connecting Claude to external services."
},
{
  id:91, module:1, scenario:"Developer Productivity with Claude",
  text:"Your code review assistant must evaluate pull requests for style, security, and documentation using the same three step workflow for every PR. Which task decomposition pattern is most appropriate?",
  options:[
    "An orchestrator workers pattern that dynamically chooses which checks are needed.",
    "A single comprehensive prompt that handles all three aspects simultaneously.",
    "A routing pattern that first classifies the PR by type.",
    "Prompt chaining, with each aspect analyzed separately and combined in a final synthesis step."
  ],
  correct:3,
  explanation:"Prompt chaining is appropriate when the workflow is fixed and sequential (style, security, documentation). Each step analyzes one aspect independently, and a final synthesis step combines findings. Dynamic orchestration is unnecessary for a predetermined workflow."
},
{
  id:92, module:5, scenario:"Developer Productivity with Claude",
  text:"After a long exploration of a rendering subsystem, the agent starts responding with generic rendering patterns instead of the specific classes it had discovered. An engineer now asks how physics integrates with rendering for debug overlays. What is the most effective approach?",
  options:[
    "Clear the context completely and restart the exploration from scratch.",
    "Spawn a subagent to explore physics independently and manually synthesize later.",
    "Summarize the key rendering findings, then spawn a subagent for physics exploration with that summary in its initial context.",
    "Continue in the current context with more targeted prompts naming the specific classes."
  ],
  correct:2,
  explanation:"Summarizing key rendering findings and spawning a subagent with that summary preserves essential context while getting fresh context capacity for physics exploration. This addresses the context degradation problem without losing prior discoveries."
},
{
  id:93, module:2, scenario:"Developer Productivity with Claude",
  text:"An engineer asks the agent to find all callers of a function before removing it. The function is exposed through wrapper modules that rename it for domain specific use. What exploration strategy will most reliably identify all callers?",
  options:[
    "Search project documentation to understand intended usage patterns.",
    "Find all files that import from the library or wrapper modules, then read them to check usage.",
    "Use Grep to search for the function‘s original name across the codebase.",
    "Read the library and wrapper modules to identify all exposed names for the function, then Grep for each name across the codebase."
  ],
  correct:3,
  explanation:"Since the function is exposed through wrapper modules that rename it, you must first read the library and wrappers to find all exposed names, then Grep for each name. Searching only the original name would miss renamed references."
},
{
  id:94, module:5, scenario:"Developer Productivity with Claude",
  text:"An engineer wants to understand a caching layer before adding a new invalidation trigger. The caching logic spans many files. What is the most effective next step while managing context constraints?",
  options:[
    "Use Grep to search for invalidate and expire patterns and read only those line ranges.",
    "Sequentially load all files with the Read tool.",
    "Analyze imports and class hierarchies to identify the base cache class, read it first, then trace specific invalidation implementations.",
    "Use Glob to find cache related filenames and read the largest files first."
  ],
  correct:2,
  explanation:"Analyzing imports and class hierarchies to identify the base cache class first, then tracing specific invalidation implementations, builds understanding systematically while managing context constraints. This is more targeted than grep-only or loading all files."
},
{
  id:95, module:2, scenario:"Developer Productivity with Claude",
  text:"Your agent is exploring a large codebase and finds 150 matching files with a Glob pattern. To understand file relationships, what should it do next?",
  options:[
    "Use Grep to search for import statements, function calls, and dependencies across the matched files",
    "Use Bash to generate a dependency graph automatically",
    "Use Read to open all 150 files and analyze them",
    "Ask the user which files are important"
  ],
  correct:0,
  explanation:"With 150 files, reading all is impractical. Using Grep to search for import statements and dependencies across the matched files efficiently reveals relationships without overwhelming context."
},
{
  id:96, module:2, scenario:"Developer Productivity with Claude",
  text:"Your agent encounters a large configuration file that the Bash ‘head‘ tool would normally read in pieces. Should the agent use Read tool instead?",
  options:[
    "Use Read only if the file is less than 100 lines",
    "No; large files should always be processed with Bash piping",
    "No; always use Bash for efficiency",
    "Yes; the Read tool is better for reading file contents, especially large files, because it provides line numbering and handles formatting"
  ],
  correct:3,
  explanation:"The Read tool is the preferred method for reading file contents in Claude Code. It provides line numbering and better formatting than Bash head/cat, and is explicitly recommended over Bash for file reading."
},
{
  id:97, module:2, scenario:"Developer Productivity with Claude",
  text:"When your agent reads a source file that hasn‘t been modified in 2 years, what strategy helps understand its purpose without reading all of it?",
  options:[
    "Use Bash to count lines and estimate complexity",
    "Use Grep to find test files or documentation that reference this module",
    "Skip it; old code is usually not important",
    "Read the entire file to understand its full context"
  ],
  correct:1,
  explanation:"Using Grep to find test files or documentation that reference the module provides understanding of its purpose and usage without reading the entire file. Tests and docs often describe intended behavior concisely."
},
{
  id:98, module:2, scenario:"Developer Productivity with Claude",
  text:"Your agent needs to understand how a function is used throughout the codebase. What Grep pattern should it use?",
  options:[
    "Search for imports that might use this functio",
    "Try all three: search for the name, search for calls, search for imports",
    "Search for function calls: ‘grep functionName(‘ or ‘grep functionName\\\\(‘",
    "Search for the function name: ‘grep functionName‘"
  ],
  correct:1,
  explanation:"To comprehensively understand function usage, you should search for the function name, function calls (with parentheses), and imports. All three patterns together give a complete picture of how the function is used throughout the codebase."
},
{
  id:99, module:2, scenario:"Developer Productivity with Claude",
  text:"Your agent is generating boilerplate code (a database migration script) and writes it to a file using the Write tool. After writing, what should the agent do?",
  options:[
    "Immediately return the file path to the user",
    "Run Bash to execute the generated code and verify it works",
    "Ask the user to review the generated code before using it",
    "Read the file back to verify the generated content matches expectations"
  ],
  correct:3,
  explanation:"After writing generated code with the Write tool, reading the file back verifies the content was written correctly and matches expectations. This is a standard verification step, especially for boilerplate that needs to be structurally correct."
},
{
  id:100, module:2, scenario:"Developer Productivity with Claude",
  text:"Your agent encounters an Edit tool failure: ‘old_string is not unique in the file‘. What‘s the best recovery strategy?",
  options:[
    "Use Read to examine the file, then use Write with a complete file rewrite",
    "Use Bash sed to make the change instead",
    "Ask the user to manually edit the file",
    "Try Edit again with a larger substring that includes more context"
  ],
  correct:3,
  explanation:"When Edit fails due to a non-unique old_string, the recommended recovery is to retry with a larger substring that includes more surrounding context to make it unique. This is stated directly in the Edit tool's documentation."
},
{
  id:101, module:2, scenario:"Developer Productivity with Claude",
  text:"Your agent needs to understand the dependency structure of a large codebase. What‘s the most efficient approach?",
  options:[
    "Read every file in the codebase",
    "Use Glob to find file patterns (imports, module declarations) and Grep to map relationships",
    "Use Bash to run build tools or language-specific analysis (gradle, npm, cargo)",
    "Visualize the entire codebase first with external tools"
  ],
  correct:1,
  explanation:"Using Glob to find file patterns and Grep to map import relationships is the most efficient approach for understanding dependency structure. It leverages the built-in tools designed for pattern-based file discovery and content search."
},
{
  id:102, module:3, scenario:"Code Generation with Claude Code",
  text:"Your team has a monorepo with different services. You want to create a custom command that generates boilerplate for a new microservice. Where should this command be stored to ensure it‘s version-controlled and shared across the team?",
  options:[
    "A dedicated /commands directory at the repository root",
    ".claude/commands/ in the project root",
    "~/.claude/commands/ in the user‘s home directory",
    ".claude/microservice_generator.md in the project"
  ],
  correct:1,
  explanation:".claude/commands/ in the project root is the correct location for team-shared, version-controlled custom commands. Personal commands go in ~/.claude/commands/, but project commands belong in the project's .claude/commands/ directory."
},
{
  id:103, module:1, scenario:"Customer Support Resolution Agent",
  text:"Group of answer choices",
  options:[
    "Inspect the assistant‘s message for tool_use blocks, extract the tool name/input, execute the tool externally, and append results back to conversation history",
    "Extract the tool name and input from the stop_reason field and execute immediately",
    "Treat the stop_reason as a signal to end the conversation and return results to the user",
    "Automatically send another request to Claude without executing any tools yet"
  ],
  correct:0,
  explanation:"When stop_reason is 'tool_use', you inspect the assistant's message for tool_use content blocks, extract the tool name and input, execute the tool externally, and append the results back to the conversation history. The stop_reason itself doesn't contain tool details."
},
{
  id:104, module:1, scenario:"Multi-Agent Research System",
  text:"What is the most efficient pattern for addressing gaps without restarting the entire research?",
  options:[
    "Summarize findings to date and start a fresh session with the new research direction",
    "Invoke the search and analysis subagents again with targeted prompts to fill specific gaps",
    "Queue gap queries and execute all in a single new coordinator invocation",
    "Use fork_session to branch from the current research state and explore alternatives in parallel"
  ],
  correct:1,
  explanation:"Invoking subagents again with targeted prompts to fill specific gaps is the most efficient pattern. It reuses existing orchestration without restarting research, and focuses subagent effort on precisely the missing information."
},
{
  id:105, module:1, scenario:"Customer Support Resolution Agent",
  text:"Your agent processes a customer return request and after calling lookup_order, the response arrives with stop_reason set to ‘tool_use‘. What should you do next in the agentic loop?",
  options:[
    "Extract the tool name and input from the stop_reason field and execute immediately",
    "Inspect the assistant‘s message for tool_use blocks, extract the tool name/input, execute the tool externally, and append results back to conversation history",
    "Automatically send another request to Claude without executing any tools yet",
    "Treat the stop_reason as a signal to end the conversation and return results to the user"
  ],
  correct:1,
  explanation:"When stop_reason is 'tool_use', the correct agentic loop step is to inspect the assistant's message for tool_use blocks, extract the tool name/input, execute the tool, and append results back. The stop_reason signals that tools need execution, but the details are in the message content."
},
{
  id:106, module:3, scenario:"Code Generation with Claude Code",
  text:".mcp.json contains database credentials. How should you handle credential storage securely?",
  options:[
    "Use environment variable expansion in .mcp.json (e.g., $DB_PASSWORD) and store variables locally",
    "Store credentials in .claude/secrets.json with additional encryption",
    "Embed credentials directly in .mcp.json and restrict file permission",
    "Use a separate .env.local file that .mcp.json references"
  ],
  correct:0,
  explanation:"Environment variable expansion in .mcp.json (e.g., $DB_PASSWORD) is the recommended approach. Credentials are stored as environment variables locally, keeping secrets out of version-controlled files while allowing the config to reference them."
},
{
  id:107, module:2, scenario:"Code Generation with Claude Code",
  text:"You’re designing a custom tool for your domain and want Claude Code to use it effectively. What is the most important factor in the tool definition?",
  options:[
    "A complex implementation that shows the tool’s full capabilities",
    "A list of example use cases in the tool name",
    "A clear, descriptive tool description that helps the model understand when to use it",
    "Detailed parameter documentation with many optional fields"
  ],
  correct:2,
  explanation:"A clear, descriptive tool description is the single most important factor for helping the model understand when and how to use a tool. Good descriptions guide tool selection more effectively than complex examples or detailed parameter docs."
},
{
  id:108, module:3, scenario:"Claude Code for Continuous Integration",
  text:"Your CI process uses Claude Code with the -p flag in a GitHub Actions workflow.",
  options:[
    "Parallel execution of multiple Claude Code instances simultaneously",
    "Non-interactive mode, allowing Claude Code to run without requiring approvals or human interaction",
    "Preview mode, showing potential changes without applying them",
    "Performance profiling to measure execution time"
  ],
  correct:1,
  explanation:"The -p flag enables non-interactive (print) mode, allowing Claude Code to run without requiring user approvals or human interaction. This is essential for CI/CD pipelines where no human is present to approve actions."
},
{
  id:109, module:1, scenario:"Customer Support Resolution Agent",
  text:"You are building a customer support resolution agent using the Claude Agent SDK. The agent handles high-ambiguity requests like returns, billing disputes, and account issues. It has access to your backend systems through custom Model Context Protocol (MCP) tools (get_customer, lookup_order, process_refund, escalate_to_human). Your target is 80%+ first-contact resolution while knowing when to escalate. \nA customer says they are frustrated, have explained the issue twice, and want to talk to a real person now. The agent has not yet called any tools to investigate the account. What should the agent do?",
  options:[
    "Immediately call escalate_to_human with the conversation history.",
    "Acknowledge the frustration and ask one targeted question before escalating.",
    "Briefly explain what the agent can help with and offer to resolve the issue quickly, escalating only if the customer repeats the request.",
    "First call get_customer and lookup_order to gather account context, then escalate."
  ],
  correct:0,
  explanation:"When a customer explicitly and repeatedly asks to talk to a real person, the agent should immediately call escalate_to_human with the conversation history. Attempting further investigation or asking more questions ignores the customer's clear request and worsens frustration."
},
{
  id:110, module:5, scenario:"Structured Data Extraction",
  text:"Your system has operated with full human review for months. High confidence extractions appear highly accurate overall, and you want to automate those cases. Before deploying, what validation step is most critical?",
  options:[
    "Analyze accuracy by document type and field to confirm high confidence performance is consistent across segments",
    "Compare accuracy at different confidence thresholds to find an optimal cut off.",
    "Verify that aggregate accuracy meets downstream requirements.",
    "Run a pilot that routes some high confidence extractions directly downstream and monitor reports."
  ],
  correct:0,
  explanation:"Before automating high-confidence extractions, you must analyze accuracy by document type and field to confirm performance is consistent across segments. Aggregate accuracy can mask poor performance on specific document types or fields."
},
{
  id:111, module:1, scenario:"Claude Code for Continuous Integration",
  text:"Your CI coordinator agent needs to spawn test-generation subagents scoped to write tests but not modify source code. How do you restrict the subagent?",
  options:[
    "Add a system prompt instruction not to modify source code",
    "Create a separate test-generation tool rather than a subagent",
    "Trust the agent to understand boundaries via instructions",
    "Set allowedTools in the Task definition to include only Read, Write (for test files), and Grep"
  ],
  correct:3,
  explanation:"allowedTools in the Task definition is the architectural mechanism to restrict subagent capabilities. Setting it to include only Read, Write (for test files), and Grep ensures the subagent can read source and write tests but cannot modify source code."
},
{
  id:112, module:5, scenario:"Claude Code for Continuous Integration",
  text:"Your CI pipeline includes Claude Code review comments on PRs. A developer has a PR with a known limitation flagged by the reviewer. On resubmission, the same issue is flagged again. What context should you provide to the CI reviewer?",
  options:[
    "Include prior review findings and developer comments explaining the limitation is intentional",
    "Use a database to track which issues have been discussed",
    "Skip review of known limitations to avoid duplicate comments",
    "Ask developers to mark issues resolved before re-review"
  ],
  correct:0,
  explanation:"Including prior review findings and developer comments explaining the limitation is intentional provides the CI reviewer with proper context. This prevents redundant flagging of known issues and respects developer decisions."
},
{
  id:113, module:5, scenario:"Claude Code for Continuous Integration",
  text:"Your test generation CI job produces thousands of tests. Some are redundant with existing tests. What context would prevent this waste?",
  options:[
    "Use a database to deduplicate generated tests",
    "Include existing test files in context so Claude Code avoids testing already-covered scenarios",
    "Run test generation in separate jobs to compartmentalize work",
    "Filter generated tests through a uniqueness checker"
  ],
  correct:1,
  explanation:"Including existing test files in context allows Claude Code to see what scenarios are already covered and generate only tests for uncovered paths. This is context management — providing the right information prevents wasteful duplication."
},
{
  id:114, module:5, scenario:"Claude Code for Continuous Integration",
  text:"Your CI code review has grown very large after analyzing many files and building context. The review is becoming unreliable with hallucinations. What should you do?",
  options:[
    "Increase the context window size to accommodate more information",
    "Clear the session cache and continue from the same point",
    "Start a fresh review with a summary of prior findings rather than continuing the stale session",
    "Resume the existing session with --resume to preserve all analysis"
  ],
  correct:2,
  explanation:"When context becomes too large and causes hallucinations, starting a fresh review with a summary of prior findings is the correct approach. This resets context while preserving key insights, addressing the context degradation problem."
},
{
  id:115, module:4, scenario:"Claude Code for Continuous Integration",
  text:"Your CI code review identifies security vulnerabilities. The criteria need to be very specific to be actionable. Instead of “check for security issues,“ what should you specify?",
  options:[
    "“Use security expertise to find vulnerabilities“",
    "“Rate code security and suggest improvements“",
    "“Flag when unvalidated user input is passed to SQL queries or shell commands“ with concrete code examples",
    "“Identify code that might have security problems“"
  ],
  correct:2,
  explanation:"Explicit, concrete criteria with examples (like 'flag when unvalidated user input is passed to SQL queries or shell commands') is far more effective than vague instructions. This is the explicit criteria prompt engineering pattern."
},
{
  id:116, module:2, scenario:"Claude Code for Continuous Integration",
  text:"You‘re designing a test case generation tool for your CI pipeline. The tool generates test cases for a given function. How should you describe this tool to help Claude Code select it appropriately?",
  options:[
    "A detailed implementation description of how the tool works internally",
    "A clear description explaining it generates test cases, what it accepts as input, and what test cases it returns",
    "A vague description: “helps with testing“",
    "A description listing all possible test types it can generate"
  ],
  correct:1,
  explanation:"A clear description explaining what the tool does, what inputs it accepts, and what outputs it returns helps the model select the tool appropriately. This follows the tool description best practice of being clear about purpose, inputs, and outputs."
},
{
  id:117, module:4, scenario:"Claude Code for Continuous Integration",
  text:"Your CI pipeline includes a code quality gate that fails if security review flags are above a threshold. The system is currently very conservative, finding many low-severity issues. What‘s the best approach?",
  options:[
    "Define and enable only high-severity security checks; disable low-severity checks while refining criteria",
    "Disable the security gate entirely until criteria improve",
    "Lower the severity threshold to pass with more issues",
    "Require manual approval for all security findings"
  ],
  correct:0,
  explanation:"Defining and enabling only high-severity security checks while disabling low-severity ones is the best iterative approach. It keeps the gate useful while reducing noise, and allows criteria refinement for lower-severity checks over time."
},
{
  id:118, module:1, scenario:"Claude Code for Continuous Integration",
  text:"Your CI pipeline runs Claude Code to review PRs. Some PRs span multiple domains (frontend, backend, database). Should you use specialized reviewers for each domain or one comprehensive reviewer?",
  options:[
    "Sequential reviewers where frontend is reviewed first, then backend",
    "One comprehensive reviewer analyzing all domains to ensure consistency",
    "Specialized reviewers without coordination to avoid bottlenecks",
    "Specialized reviewers per domain, each with domain-relevant tools and criteria, coordinated by a hub agent"
  ],
  correct:3,
  explanation:"Hub-and-spoke with specialized reviewers per domain (frontend, backend, database), each with domain-relevant tools and criteria, coordinated by a hub agent, is the ideal pattern. It provides domain expertise while maintaining coordination."
},
{
  id:119, module:4, scenario:"Claude Code for Continuous Integration",
  text:"Your CI test generation is producing tests for paths that are already well-covered. Including existing test file content in context, how should you structure the prompt to avoid duplication?",
  options:[
    "Show existing tests, explain coverage gaps, ask to generate tests for unexplored scenarios",
    "Show test count and ask to generate more",
    "Show existing tests and ask to generate different tests",
    "Include existing tests but expect the model to figure out gaps"
  ],
  correct:0,
  explanation:"Showing existing tests, explaining coverage gaps, and asking to generate tests for unexplored scenarios is the most effective prompt structure. It provides explicit criteria and context that guides generation toward valuable new tests."
},
{
  id:120, module:1, scenario:"Claude Code for Continuous Integration",
  text:"Your CI pipeline needs to analyze code from the perspective of two concerns: does this code work correctly, and does it follow security practices? What architecture would best handle this?",
  options:[
    "Two agents running in the same session to share findings",
    "Sequential analysis where functional correctness is checked first, then security",
    "One comprehensive agent analyzing both concerns to ensure consistency",
    "Two independent agents focused on functional correctness and security respectively, coordinated by a hub"
  ],
  correct:3,
  explanation:"Two independent agents focused on functional correctness and security respectively, coordinated by a hub, is the best architecture. Each agent has specialized focus and criteria, and the hub coordinates findings — this is the hub-and-spoke pattern."
},
{
  id:121, module:4, scenario:"Claude Code for Continuous Integration",
  text:"Your CI code review identifies potential bugs through pattern matching. Most findings are actionable, but some are false positives. What‘s the best approach to improve precision?",
  options:[
    "Require developer approval for all findings before posting",
    "Define specific, concrete patterns to check; disable patterns with persistent false positives while refining criteria",
    "Lower the sensitivity threshold to reduce findings",
    "Use a voting system where multiple review instances must agree"
  ],
  correct:1,
  explanation:"Defining specific, concrete patterns and disabling patterns with persistent false positives while refining criteria is the iterative improvement approach. It maintains precision by keeping what works and refining what doesn't."
},
{
  id:122, module:2, scenario:"Structured Data Extraction",
  text:"Your pipeline uses an extract_metadata tool and also citation and DOI verification tools. When users ask for extraction and enrichment together, the model sometimes calls the enrichment tool first even though it needs metadata produced by extract_metadata. What is the most effective way to ensure structured metadata extraction happens first?",
  options:[
    "Keep tool_choice as auto and reorder the tools array so extract_metadata appears first.",
    "Set tool_choice to any and add prompt instructions prioritizing extract_metadata.",
    "Force extract_metadata first, then process the enrichment request in subsequent turns after metadata is available.",
    "Force extract_metadata for every API call in the pipeline."
  ],
  correct:2,
  explanation:"Forcing extract_metadata first using tool_choice, then processing enrichment in subsequent turns ensures the dependency order is respected. This guarantees metadata is available before enrichment tools attempt to use it."
},
{
  id:123, module:1, scenario:"Claude Code for Continuous Integration",
  text:"Your CI pipeline coordinates multiple specialized Claude Code agents: one reviews tests, one reviews performance, one reviews security. What architectural pattern should you use?",
  options:[
    "Pipeline: sequential agents where each agent‘s output feeds the next",
    "Consensus: all agents run independently and voting determines findings",
    "Hub-and-spoke: coordinator orchestrates specialized subagents with isolated context",
    "Single agent with broad capabilities and role-based instructions"
  ],
  correct:2,
  explanation:"Hub-and-spoke is the correct pattern for coordinating multiple specialized agents (tests, performance, security). The coordinator orchestrates subagents with isolated context, aggregates findings, and ensures comprehensive review."
},
{
  id:124, module:4, scenario:"Structured Data Extraction",
  text:"Your extraction model is hallucinating dates (creating dates that don‘t appear in the source document). How should you address this?",
  options:[
    "Use validation to reject dates that don‘t match document patterns",
    "Add a required_evidence field to the schema; dates without source evidence are marked as hallucinations",
    "Accept the hallucinations; downstream systems can validate",
    "Add a note in the system prompt: ‘Do not hallucinate dates‘"
  ],
  correct:1,
  explanation:"Adding a required_evidence field to the schema forces the model to provide source evidence for each extracted date. Dates without evidence are flagged as potential hallucinations, making the system self-auditing."
},
{
  id:125, module:4, scenario:"Structured Data Extraction",
  text:"When validating extraction against a JSON schema, a document successfully validates but the extracted data is semantically wrong (e.g., a price is extracted but it‘s actually a phone number). How should you detect this?",
  options:[
    "JSON schema is sufficient; semantic validation is downstream‘s problem",
    "Ask the user to review borderline extractions",
    "Add semantic validation rules: validate that prices are within reasonable ranges, emails contain @, phone numbers match regional patterns",
    "Use a separate model to verify semantic correctness"
  ],
  correct:2,
  explanation:"Adding semantic validation rules (price ranges, email patterns, phone number formats) catches errors that JSON schema validation cannot detect. This provides domain-specific validation beyond structural correctness."
},
{
  id:126, module:5, scenario:"Structured Data Extraction",
  text:"Your system extracts information from multiple documents for the same entity (e.g., different invoices for the same customer). How should you handle discrepancies?",
  options:[
    "Return an error if documents disagree",
    "Extract from all documents and include a record_source field; downstream systems reconcile",
    "Use the first document‘s values",
    "Average values from multiple documents"
  ],
  correct:1,
  explanation:"Extracting from all documents and including a record_source field lets downstream systems reconcile discrepancies with full provenance. This preserves all data rather than silently dropping conflicting information."
},
{
  id:127, module:5, scenario:"Structured Data Extraction",
  text:"When extraction confidence is low for a critical field, what should the system do?",
  options:[
    "Extract the value anyway; confidence is just a number",
    "Return an error and skip the document",
    "Re-prompt the model to try again",
    "Extract with low confidence AND include an extraction_quality flag; downstream systems decide if the extraction is usable"
  ],
  correct:3,
  explanation:"Extracting with low confidence AND including an extraction_quality flag allows downstream systems to decide how to handle uncertain extractions. This provides transparency about extraction reliability without losing potentially valid data."
},
{
  id:128, module:5, scenario:"Structured Data Extraction",
  text:"Your extraction system processes documents in a queue, and some fail. How should you structure error handling?",
  options:[
    "For each document, track extraction success/failure and include failure reasons in output",
    "Retry failed documents immediately",
    "Skip failed documents and continue",
    "Return error and halt processing"
  ],
  correct:0,
  explanation:"Tracking success/failure per document with failure reasons in output follows the StageResult/error propagation pattern. It provides structured error information without halting the entire pipeline."
},
{
  id:129, module:4, scenario:"Structured Data Extraction",
  text:"Before processing a large batch of documents, how should you validate your extraction system?",
  options:[
    "Run a single test extraction and assume scaling will work",
    "Test the extraction on a sample of documents, review results manually, and refine the schema/prompts before bulk processing",
    "Start processing; find issues as they arise",
    "Skip testing; ML models handle variation automatically"
  ],
  correct:1,
  explanation:"Testing on a sample, reviewing results manually, and refining schema/prompts before bulk processing is the standard validation approach. It catches issues early and allows iterative improvement before committing to scale."
},
{
  id:130, module:4, scenario:"Structured Data Extraction",
  text:"When designing extraction prompts, what pattern is most effective for reducing false positives and hallucinations?",
  options:[
    "Write a detailed prompt explaining what to extract",
    "Provide explicit extraction criteria plus few-shot examples showing correct vs. incorrect extractions",
    "Ask the model to explain its reasoning before extracting",
    "Use only the schema; let the model figure out what to extract"
  ],
  correct:1,
  explanation:"Explicit extraction criteria combined with few-shot examples showing correct vs. incorrect extractions is the most effective pattern. Few-shot examples ground the model's understanding and reduce hallucinations by demonstrating expected behavior."
},
{
  id:131, module:5, scenario:"Structured Data Extraction",
  text:"When extraction errors occur (model timeout, empty document, malformed response), what should you log?",
  options:[
    "Log nothing; just skip the document",
    "Log the error type, document identifier, extraction attempt count, and timestamp for debugging",
    "Log the error and immediately retry",
    "Log full document content for debugging"
  ],
  correct:1,
  explanation:"When extraction errors occur, you should log structured diagnostic information including error type, document identifier, attempt count, and timestamp. This enables systematic debugging and pattern detection rather than silently skipping documents or logging excessive raw content."
},
{
  id:132, module:4, scenario:"Structured Data Extraction",
  text:"When a field can have multiple valid formats (e.g., phone numbers as 1234567890, 123-456-7890, or +1 123-456-7890), how should your schema handle this?",
  options:[
    "Ask users to specify which format they want",
    "Pick one format and normalize all inputs to that",
    "Accept multiple formats and include a normalized version in output along with the original",
    "Use a regex pattern that accepts all formats"
  ],
  correct:2,
  explanation:"The best schema approach for multiple valid formats is to accept the original format and include a normalized version in the output. This preserves source fidelity while enabling consistent downstream processing, aligning with the exam guide's emphasis on format normalization rules alongside strict output schemas."
},
{
  id:133, module:4, scenario:"Structured Data Extraction",
  text:"Your extraction system processes documents with optional fields (some have them, some don‘t). How should the schema define these?",
  options:[
    "Include optional fields with a default value",
    "Extract whatever is present; don‘t worry about missing fields",
    "Mark optional fields in the schema; they can be null if genuinely missing from the document",
    "Include all fields as required; return error if any are missing"
  ],
  correct:2,
  explanation:"Optional fields should be explicitly marked in the schema as nullable, allowing null when the information is genuinely missing from the document. This prevents the model from fabricating values to satisfy required fields, as specified in Task Statement 4.3 on schema design considerations."
},
{
  id:134, module:1, scenario:"Developer Productivity with Claude",
  text:"An engineer used Claude Code yesterday to investigate authentication flows in a monolith and wants to continue that specific named investigation today. How should she resume?",
  options:[
    "Use continue to pick up the most recent conversation.",
    "Use the session UUID manually from the transcript.",
    "Start fresh and reread the same files.",
    "Use resume with the specific session name."
  ],
  correct:3,
  explanation:"The exam guide (Task Statement 1.7) explicitly covers named session resumption using --resume with session names to continue specific prior conversations. The 'resume' command with a specific session name is the correct way to pick up a named investigation, not 'continue' which picks up the most recent session."
},
{
  id:135, module:2, scenario:"Multi-Agent Research System",
  text:"Your product search tool frequently retries after zero results because the agent treats no matches found as a failure. The external API returns HTTP 200 with an empty results array. How should the tool‘s result be structured?",
  options:[
    "Add a suggestions field with alternative search strategies when results are empty.",
    "Return isError true with a message that no products matched.",
    "Return a natural language string and let the agent interpret the outcome contextually.",
    "Return a structured result with a success boolean and results array, reserving error signaling for actual execution failures."
  ],
  correct:3,
  explanation:"An empty results array from a successful API call (HTTP 200) is not an error - it is a valid empty result. The tool should return a structured result with success=true and an empty results array, reserving isError for actual execution failures like timeouts or network errors, per Task Statement 2.2 on distinguishing access failures from valid empty results."
},
{
  id:136, module:2, scenario:"Multi-Agent Research System",
  text:"Your post_content tool requires user confirmation before publishing, but the current confirmation step is so minimal that users approve almost instantly and miss errors such as wrong accounts or wrong schedule times. How should the confirmation workflow be redesigned?",
  options:[
    "Include the complete post text, target account, scheduled time, and platform in the confirmation request.",
    "Auto approve routine posts and require explicit confirmation only for unusual patterns.",
    "Require users to type a confirmation phrase instead of clicking a button.",
    "Add a mandatory waiting period before confirmation becomes available."
  ],
  correct:0,
  explanation:"To prevent users from approving errors, the confirmation request should include complete details: full post text, target account, scheduled time, and platform. This gives users enough context to catch mistakes before approval, aligning with human-in-the-loop best practices."
},
{
  id:137, module:2, scenario:"Multi-Agent Research System",
  text:"Your control_device tool manages smart home devices through external APIs. When a device does not respond within the timeout period, the tool returns an error. The agent currently tells users only that the device is not responding, without helpful next steps. Which error response structure would best enable useful follow up?",
  options:[
    "Return is_error true with a structured technical error containing only debugging fields such as device ID and raw API status.",
    "Return is_error true with a brief device offline message and make the agent call another tool for troubleshooting suggestions.",
    "Return is_error false with an optimistic message indicating the command may still be pending.",
    "Return is_error true with a message explaining the likely cause and suggesting troubleshooting steps the agent can offer."
  ],
  correct:3,
  explanation:"The error response should include isError true with a message explaining the likely cause and suggesting troubleshooting steps the agent can offer to the user. This enables the agent to provide actionable follow-up rather than just raw technical details or requiring additional tool calls, per Task Statement 2.2 on structured error responses with human-readable descriptions."
},
{
  id:138, module:2, scenario:"Multi-Agent Research System",
  text:"When your web search tool returns an empty result set, how should it indicate this to be distinguishable from an access error?",
  options:[
    "Return null or an empty array with isError: false",
    "Return a tool result with isError: true and errorCategory set to ‘no_results_found‘",
    "Return an error message indicating ‘no results found‘",
    "Throw an exception that the subagent must catch"
  ],
  correct:0,
  explanation:"An empty result set from a successful search is not an error - it should return null or an empty array with isError: false. This distinguishes it from an actual access error (which would use isError: true), per Task Statement 2.2 and 5.3 on distinguishing access failures from valid empty results."
},
{
  id:139, module:1, scenario:"Multi-Agent Research System",
  text:"Your coordinator wants to partition research work across subagents to parallelize efforts. Which partitioning strategy is most appropriate?",
  options:[
    "Partition by topic: each subagent researches a different subtopic of the main research question",
    "Partition by processing phase: one subagent retrieves all sources, another analyzes all sources, a third synthesizes all findings",
    "Partition by source type: one subagent searches web results, another analyzes PDF documents, a third consults APIs",
    "Partition by output format: one subagent produces summaries, another produces detailed reports, a third produces data tables"
  ],
  correct:0,
  explanation:"Partitioning by topic (each subagent researches a different subtopic) is the most appropriate parallelization strategy. The exam guide (Task Statement 1.2) specifically mentions partitioning research scope across subagents by assigning distinct subtopics or source types to minimize duplication."
},
{
  id:140, module:1, scenario:"Multi-Agent Research System",
  text:"Your document analysis subagent works with potentially sensitive documents. What is the best practice for scoping access to minimize security risk?",
  options:[
    "Use MCP server scoping in ~/.claude.json to restrict the subagent‘s MCP server connections to a document-specific server",
    "Define allowedTools to include only a vetted ‘analyze_document‘ tool with explicit read permissions and access logging",
    "Create a PostToolUse hook to redact sensitive fields before the subagent sees them",
    "Grant the subagent access to all document reading tools; let it decide what‘s appropriate"
  ],
  correct:1,
  explanation:"The best practice for scoping access is to define allowedTools that include only a vetted tool with explicit read permissions and access logging. This follows the principle of least privilege from Task Statement 2.3, giving agents only the tools needed for their role rather than unrestricted access."
},
{
  id:141, module:2, scenario:"Multi-Agent Research System",
  text:"When defining tools for your subagents, you‘re deciding between making one ‘search_content‘ tool that handles web, documents, and APIs vs three specialized tools. What‘s the right choice?",
  options:[
    "Three specialized tools with clear, distinct descriptions reduce misrouting and allow role-specific optimization",
    "One generalized tool reduces cognitive load and is easier to maintain",
    "Create both; let subagents choose which to use",
    "Use one tool with conditional logic inside; agents will route based on parameters"
  ],
  correct:0,
  explanation:"Three specialized tools with clear, distinct descriptions reduce misrouting and allow role-specific optimization. The exam guide (Task Statement 2.1) emphasizes splitting generic tools into purpose-specific tools with defined input/output contracts to prevent ambiguous or overlapping descriptions that cause misrouting."
},
{
  id:142, module:2, scenario:"Multi-Agent Research System",
  text:"Your research subagents each use different MCP servers (one for web APIs, one for document management). How should you configure these servers to ensure isolation and security?",
  options:[
    "Define all servers in a single .mcp.json file in the project root; subagents filter at runtime",
    "Define project-wide servers in .claude/mcp.json; each subagent‘s AgentDefinition specifies which servers it can access",
    "Define servers in ~/.claude.json for user-level access; subagents inherit the full set",
    "Create separate environment configurations for each subagent"
  ],
  correct:1,
  explanation:"The correct approach is to define project-wide servers in .claude/mcp.json and use each subagent's AgentDefinition to specify which servers it can access. This aligns with Task Statement 1.3 on AgentDefinition configuration including tool restrictions for each subagent type."
},
{
  id:143, module:2, scenario:"Developer Productivity with Claude",
  text:"You have configured an agent with multiple MCP servers for git, Jira, and documentation. When a user asks for a task that spans those systems, how does the agent access tools across the servers?",
  options:[
    "It selects the most relevant server and loads only that server‘s tools.",
    "The agent queries each server sequentially to determine which handles each tool.",
    "You must specify which server to use for each turn.",
    "Tools from all configured MCP servers are discovered at connection time and are available simultaneously."
  ],
  correct:3,
  explanation:"Tools from all configured MCP servers are discovered at connection time and available simultaneously to the agent. This is explicitly stated in Task Statement 2.4: tools from all configured MCP servers are discovered at connection time and available simultaneously."
},
{
  id:144, module:2, scenario:"Developer Productivity with Claude",
  text:"After adding an MCP server with specialized code refactoring tools, the agent still uses basic text manipulation for refactoring tasks. The tools have minimal descriptions. What is the most effective way to improve adoption of the specialized tools?",
  options:[
    "Remove the Write tool so the agent must use MCP refactoring tools.",
    "Enhance MCP tool descriptions to explain when each tool is preferable and clarify expected inputs and outputs.",
    "Implement a classifier that automatically routes refactoring requests to the MCP server.",
    "Accept the behavior because simpler tools are more predictable."
  ],
  correct:1,
  explanation:"Enhancing MCP tool descriptions to explain when each tool is preferable and clarify expected inputs and outputs is the most effective approach. The exam guide (Task Statement 2.1 and 2.4) emphasizes that tool descriptions are the primary mechanism LLMs use for tool selection, and minimal descriptions lead to unreliable selection."
},
{
  id:145, module:2, scenario:"Developer Productivity with Claude",
  text:"You are building a security scanning workflow. When engineers need to locate all occurrences of a dangerous function such as eval across a large codebase, which tool should the agent use for content search?",
  options:[
    "Read the project‘s main entry file and follow imports.",
    "Use Grep to search for the pattern across file contents.",
    "Use Bash with ls and grep on filenames.",
    "Use Glob to find filenames matching the pattern and then read them."
  ],
  correct:1,
  explanation:"Grep is the correct tool for searching file contents for patterns like function names or dangerous functions. Task Statement 2.5 explicitly states Grep is for content search (searching file contents for patterns like function names, error messages, or import statements)."
},
{
  id:146, module:2, scenario:"Developer Productivity with Claude",
  text:"After integrating a local MCP server providing code analysis tools such as analyze_dependencies, the agent still uses Grep for dependency questions. The MCP tool description is very minimal. What is the most effective approach to improve MCP tool selection?",
  options:[
    "Remove Grep from the available tools when the MCP server is connected.",
    "Split analyze_dependencies into several granular tools.",
    "Add routing instructions to the system prompt telling the agent to use MCP tools for dependency questions.",
    "Expand the MCP tool descriptions to detail capabilities and outputs such as direct imports, transitive dependencies, and cycles."
  ],
  correct:3,
  explanation:"Expanding the MCP tool descriptions to detail capabilities and outputs is the most effective approach. The exam guide (Task Statement 2.4) states that enhancing MCP tool descriptions to explain capabilities and outputs in detail prevents the agent from preferring built-in tools over more capable MCP tools."
},
{
  id:147, module:2, scenario:"Developer Productivity with Claude",
  text:"Your codebase exploration agent is using the built-in tools effectively but users want to query specific knowledge (e.g., ‘How does the payment system work?‘). Should you create a custom MCP tool for domain knowledge?",
  options:[
    "No; ask users to provide domain knowledge in their initial prompt",
    "Yes; create an MCP tool that connects to a documentation system or knowledge base",
    "Yes; create an MCP tool that returns domain knowledge documents",
    "No; the agent should explore the codebase using built-in tools"
  ],
  correct:2,
  explanation:"Creating an MCP tool that returns domain knowledge documents is the best approach. This connects the agent to a structured knowledge base, enabling it to answer domain-specific questions that cannot be inferred from code alone, aligning with MCP integration patterns from Task Statement 2.4."
},
{
  id:148, module:2, scenario:"Developer Productivity with Claude",
  text:"Your agent is tasked with refactoring a large function. What‘s the best approach?",
  options:[
    "Read the entire function, refactor in one go using Write",
    "Use multiple small Edit operations to refactor step-by-step, testing after each change",
    "Generate the refactored version, write it, then read it back to verify",
    "Use a tool_choice loop: first read and analyze, then propose changes to the user"
  ],
  correct:1,
  explanation:"Using multiple small Edit operations to refactor step-by-step, testing after each change, is the best approach. This follows the agentic loop pattern of iterative changes with verification, using Edit for targeted modifications rather than full file rewrites, per Task Statement 2.5."
},
{
  id:149, module:1, scenario:"Developer Productivity with Claude",
  text:"Your agent is building a system that generates tests for a function. How should you structure this in the agentic loop?",
  options:[
    "Generate all tests at once and write them",
    "Use a test generation MCP server to generate tests automatically",
    "Generate tests iteratively: create test 1, verify it runs, create test 2, verify, etc.",
    "Have the user specify test cases first, then generate tests based on their specifications"
  ],
  correct:2,
  explanation:"Generating tests iteratively - create test 1, verify it runs, create test 2, verify - follows the agentic loop pattern. This ensures each test is validated before proceeding, aligning with the exam guide's emphasis on test-driven iteration (Task Statement 3.5)."
},
{
  id:150, module:1, scenario:"Developer Productivity with Claude",
  text:"When your agent uses Bash to run tests, how should it handle test failures?",
  options:[
    "Parse the test output, identify the failure reason, and attempt to fix the code automatically",
    "Show the failure output to the user and ask them to fix it",
    "Ignore failures and assume the code works",
    "Use a tool_choice: ‘any‘ to force a tool call that understands the failure"
  ],
  correct:0,
  explanation:"The agent should parse the test output, identify the failure reason, and attempt to fix the code automatically. This is core agentic loop behavior - using tool results to reason about the next action and iterate toward a solution, per Task Statement 1.1."
},
{
  id:151, module:2, scenario:"Developer Productivity with Claude",
  text:"Your agent is exploring a codebase written in an unfamiliar language. How should it discover coding patterns and conventions?",
  options:[
    "Ask the user to explain conventions",
    "Use Glob and Grep to find example code (test files, well-documented functions) and analyze patterns through code examples",
    "Search for comments mentioning ‘pattern‘ or ‘convention‘",
    "Use an MCP server for language-specific knowledge"
  ],
  correct:1,
  explanation:"Using Glob and Grep to find example code (test files, well-documented functions) and analyze patterns is the correct approach. Task Statement 2.5 describes building codebase understanding incrementally by starting with Grep to find entry points and using Read to follow imports and trace flows."
},
{
  id:152, module:2, scenario:"Developer Productivity with Claude",
  text:"Your productivity agent needs to automate repetitive code changes across multiple files (e.g., updating an API endpoint across 20 service files). What‘s the most reliable approach?",
  options:[
    "Use Bash to find and replace across all files at once",
    "Generate a script that makes the changes, then have the user run it",
    "Loop: for each file, Read it, Edit the content, verify with Read",
    "Use an MCP tool to apply batch changes"
  ],
  correct:2,
  explanation:"The most reliable approach is to loop through each file: Read it, Edit the content, and verify with Read. This follows the incremental Edit-based modification pattern with verification from Task Statement 2.5, ensuring each change is correct before moving to the next file."
},
{
  id:153, module:3, scenario:"Claude Code for Continuous Integration",
  text:"Your CI code review runs Claude Code in different configurations: strict for critical services, permissive for experimental code. Where should these different configurations live?",
  options:[
    "In .claude/rules/ with glob patterns for critical/ vs experimental/ directory paths",
    "In .claude/commands/ with conditional logic",
    "In separate CLAUDE.md files at the service level",
    "In a master config file with if-then rules"
  ],
  correct:0,
  explanation:"Using .claude/rules/ with glob patterns for critical/ vs experimental/ directory paths allows path-specific rule loading. Task Statement 3.3 specifically covers .claude/rules/ files with YAML frontmatter paths fields containing glob patterns for conditional rule activation."
},
{
  id:154, module:3, scenario:"Claude Code for Continuous Integration",
  text:"Your CI review findings must be machine-parseable for downstream integration with your issue tracker. What combination of Claude Code output flags ensures compatibility?",
  options:[
    "--output-format structured with automatic schema detection",
    "--json-only flag with automatic formatting",
    "--output-format json combined with --json-schema specifying the required structure",
    "--output-format machine-readable with schema validation"
  ],
  correct:2,
  explanation:"The correct combination is --output-format json combined with --json-schema specifying the required structure. Task Statement 3.6 explicitly states: using --output-format json with --json-schema CLI flags for enforcing structured output in CI contexts."
},
{
  id:155, module:2, scenario:"Developer Productivity with Claude",
  text:"Your agent encounters a request to modify a test file. How should it verify the changes don‘t break the test suite?",
  options:[
    "Have the user run tests to verify",
    "Ask the user if they want to run tests",
    "Assume the changes are correct",
    "Run the test suite with Bash after making changes"
  ],
  correct:3,
  explanation:"The agent should run the test suite with Bash after making changes to verify they don't break anything. This is standard agentic practice - using Bash to execute tests and validate changes, following the iterative verification pattern."
},
{
  id:156, module:2, scenario:"Developer Productivity with Claude",
  text:"Your agent is tasked with documenting an undocumented function. What information should it extract and document?",
  options:[
    "Just the function signature",
    "Full code analysis including implementation details and edge cases",
    "Ask the user what should be documented",
    "Function signature, parameters, return value, and examples derived from test code"
  ],
  correct:3,
  explanation:"The agent should extract and document the function signature, parameters, return value, and examples derived from test code. This provides comprehensive, useful documentation grounded in actual usage rather than just the signature or over-detailed implementation internals."
},
{
  id:157, module:3, scenario:"Developer Productivity with Claude",
  text:"Your agent needs to refactor code but the codebase lacks tests. What should it do?",
  options:[
    "Ask the user to write tests first",
    "Proceed with refactoring and hope it works",
    "Generate tests first, then refactor with test coverage",
    "Use type checking or linting as a proxy for correctness"
  ],
  correct:2,
  explanation:"The agent should generate tests first, then refactor with test coverage. This follows the test-driven iteration pattern from Task Statement 3.5: writing test suites covering expected behavior before implementation, then iterating by sharing test failures."
},
{
  id:158, module:2, scenario:"Developer Productivity with Claude",
  text:"When your agent generates configuration files (YAML, JSON, TOML), what‘s the best way to verify correctness?",
  options:[
    "Parse and validate the file format using Bash tools (yaml parser, json validator, toml parser)",
    "Have the user review the configuration before using it",
    "Trust the generation and assume it‘s correct",
    "Generate a test that uses the configuration to verify it works"
  ],
  correct:0,
  explanation:"Parsing and validating the file format using Bash tools (yaml parser, json validator, toml parser) provides programmatic verification of correctness. This uses the Bash tool for automated validation rather than relying on trust or manual review."
},
{
  id:159, module:1, scenario:"Developer Productivity with Claude",
  text:"Your agent is building a script to migrate data between two systems. How should it structure this to be safe?",
  options:[
    "Ask the user to create test data and run tests",
    "Generate the full migration script and let the user run it",
    "Generate the script, create a test migration against sample data with Bash, show results, then provide the production script",
    "Use an MCP server to handle migrations safely"
  ],
  correct:2,
  explanation:"The safest approach is to generate the script, create a test migration against sample data with Bash, show results, then provide the production script. This follows the iterative verification pattern with safe testing before production use."
},
{
  id:160, module:4, scenario:"Developer Productivity with Claude",
  text:"When your agent generates API endpoints, what should it validate?",
  options:[
    "Run a quick syntax check with Bash",
    "Generate documentation",
    "Just the endpoint URL format",
    "Parameter validation, request schema, response schema, and error handling"
  ],
  correct:3,
  explanation:"When generating API endpoints, the agent should validate parameter validation, request schema, response schema, and error handling. This comprehensive validation ensures the API is robust, not just syntactically correct."
},
{
  id:161, module:3, scenario:"Developer Productivity with Claude",
  text:"Your agent is tasked with upgrading a library dependency (e.g., upgrading from Flask 1.x to Flask 2.x). What‘s the best approach?",
  options:[
    "Implement a gradual migration with multiple small changes, each tested",
    "Ask the user to manage the upgrade",
    "Read the upgrade guide, identify breaking changes, generate fixes, run tests after each change",
    "Update the dependency version and hope nothing breaks"
  ],
  correct:2,
  explanation:"The best approach is to read the upgrade guide, identify breaking changes, generate fixes, and run tests after each change. This combines research with iterative, test-verified changes, aligning with Task Statement 3.5 on iterative refinement."
},
{
  id:162, module:1, scenario:"Developer Productivity with Claude",
  text:"Your agent is tasked with generating a comprehensive code documentation. How should it organize this task?",
  options:[
    "Generate API documentation only",
    "Write one long document describing everything",
    "Ask the user what documentation they need",
    "Break it down: generate module-level docs, function-level docs, examples, all cross-linked"
  ],
  correct:3,
  explanation:"Breaking documentation into module-level docs, function-level docs, and examples that are all cross-linked follows the task decomposition principle from Task Statement 1.6 - decomposing complex tasks into structured subtasks."
},
{
  id:163, module:5, scenario:"Developer Productivity with Claude",
  text:"Your agent encounters code with security issues (e.g., SQL injection vulnerabilities). Should it fix them proactively?",
  options:[
    "Use an MCP security tool to handle fixes",
    "Yes; fix security issues immediately with explanation",
    "Report the issue and ask the user for permission before fixing",
    "No; that‘s the developer‘s responsibility"
  ],
  correct:2,
  explanation:"The agent should report the security issue and ask the user for permission before fixing. This follows human-in-the-loop principles - security fixes may have unintended consequences and the developer should approve changes, especially for sensitive security-related code."
},
{
  id:164, module:3, scenario:"Claude Code for Continuous Integration",
  text:"Your CI system runs test generation on PRs. When a developer fixes a failing test, on re-run the system suggests the same test again. What context is missing?",
  options:[
    "Track which tests were already suggested using a database",
    "Include the test file with previous test implementations so Claude Code sees what‘s already written",
    "Ask Claude Code to remember previous suggestions in the prompt",
    "Skip test generation for PRs that have recent test changes"
  ],
  correct:1,
  explanation:"The missing context is the existing test file with previous test implementations. Task Statement 3.6 explicitly states: providing existing test files in context so test generation avoids suggesting duplicate scenarios already covered by the test suite."
},
{
  id:165, module:4, scenario:"Claude Code for Continuous Integration",
  text:"Your CI pipeline has limited compute budget. You‘re running 100s of review requests monthly. The Message Batches API offers 50% savings. What‘s a key consideration?",
  options:[
    "Batch API doesn't support multi-turn tool calling, so reviews must be stateless",
    "Batch API limits the number of requests per batch",
    "Batch API can only be used during off-peak hours",
    "Batch API requires pre-approval from all developers"
  ],
  correct:0,
  explanation:"A key consideration is that the Batch API does not support multi-turn tool calling within a single request, so reviews must be stateless. Task Statement 4.5 explicitly states: the batch API does not support multi-turn tool calling within a single request (cannot execute tools mid-request and return results)."
},
{
  id:166, module:1, scenario:"Claude Code for Continuous Integration",
  text:"Your code review agent is configured with allowedTools: [“Read“, “Grep“, “Glob“]. What type of review is this agent optimized for?",
  options:[
    "Performance optimization and algorithmic improvement",
    "Documentation generation and testing",
    "Automated code fixing and refactoring",
    "Static analysis and pattern finding without modifying code"
  ],
  correct:3,
  explanation:"An agent configured with only Read, Grep, and Glob (all read-only tools) is optimized for static analysis and pattern finding without modifying code. It cannot write, edit, or execute code, making it a read-only review agent."
},
{
  id:167, module:2, scenario:"Claude Code for Continuous Integration",
  text:"Your CI MCP tool for code complexity analysis returns results with isRetryable: false and errorCategory: “code_not_analyzable“. How should the system respond?",
  options:[
    "Log the error but continue with other files",
    "Treat it identically to any other error",
    "Retry the analysis since all errors are temporary",
    "Skip analysis for this file since it‘s a permanent issue, not a transient error"
  ],
  correct:3,
  explanation:"When isRetryable is false and errorCategory indicates a permanent issue like 'code_not_analyzable', the system should skip analysis for this file since it is a permanent issue, not a transient error. Task Statement 2.2 covers the distinction between retryable and non-retryable errors."
},
{
  id:168, module:4, scenario:"Claude Code for Continuous Integration",
  text:"Your CI test generation gives developers a single comprehensive test suite on each PR. Developers are overwhelmed by the volume. What approach would deliver more value?",
  options:[
    "Generate all tests upfront for maximum coverage",
    "Generate tests incrementally for highest-impact scenarios first; let developers see early value",
    "Ask developers which test types they want before generating",
    "Generate tests only for changed functions to reduce noise"
  ],
  correct:1,
  explanation:"Generating tests incrementally for highest-impact scenarios first lets developers see early value without being overwhelmed. This follows the iterative delivery principle rather than dumping everything at once."
},
{
  id:169, module:3, scenario:"Developer Productivity with Claude",
  text:"When your agent writes generated code, it should include comments. What kind of comments are most useful?",
  options:[
    "Use docstring format comments for all functions",
    "Minimal comments; let the code speak for itself",
    "Copy the style of comments from the codebase and include reasoning for non-obvious decisions",
    "Include extensive comments explaining every line"
  ],
  correct:2,
  explanation:"The agent should copy the style of comments from the codebase and include reasoning for non-obvious decisions. This respects existing conventions and adds value where it matters most, rather than over-commenting or under-commenting."
},
{
  id:170, module:4, scenario:"Claude Code for Continuous Integration",
  text:"Your CI review system flags the same code smell in every PR. Developers have confirmed it‘s a false positive for your codebase. What‘s the best action?",
  options:[
    "Disable this specific check while you refine the criteria to match your patterns",
    "Lower the confidence threshold for this check",
    "Ask Claude Code to ignore this pattern in future reviews",
    "Require developer approval to override the"
  ],
  correct:0,
  explanation:"The best action is to disable this specific check while refining the criteria to match your patterns. Task Statement 4.1 discusses temporarily disabling high false-positive categories to restore developer trust while improving prompts for those categories."
},
{
  id:171, module:4, scenario:"Claude Code for Continuous Integration",
  text:"Your CI code review needs to understand the codebase conventions before it can review effectively. What context would help it understand your project‘s patterns?",
  options:[
    "Include existing code examples showing your team‘s conventions and patterns",
    "Use a linter configuration to establish baselines",
    "Ask Claude Code to infer conventions from the PR changes",
    "Provide documentation of your coding standards"
  ],
  correct:0,
  explanation:"Including existing code examples showing your team's conventions and patterns is the most effective context. Task Statement 4.2 emphasizes few-shot examples as the most effective technique for achieving consistently formatted, actionable output, including providing examples that distinguish acceptable patterns from genuine issues."
},
{
  id:172, module:1, scenario:"Claude Code for Continuous Integration",
  text:"Your CI system assigns separate agents to review security, performance, and style. Each agent should only focus on its domain. How should you restrict tool access?",
  options:[
    "Trust each agent to focus on its domain via instructions",
    "Use allowedTools in each agent‘s Task configuration to give only domain-relevant tools",
    "Create separate tool sets for each domain that agents must use",
    "Give all agents the same tools but different system prompts"
  ],
  correct:1,
  explanation:"Using allowedTools in each agent's Task configuration to give only domain-relevant tools follows the principle of scoped tool access from Task Statement 2.3, restricting each subagent's tool set to those relevant to its role to prevent cross-specialization misuse."
},
{
  id:173, module:3, scenario:"Claude Code for Continuous Integration",
  text:"Your CI test generator creates tests but produces many duplicate tests across multiple runs. What‘s the most effective way to prevent this?",
  options:[
    "Run test generation in a separate, isolated session",
    "Ask the generator to avoid duplicates in the prompt",
    "Use a database to deduplicate tests after generation",
    "Include the existing test file in context so the generator knows what tests exist"
  ],
  correct:3,
  explanation:"Including the existing test file in context so the generator knows what tests exist prevents duplicate generation. Task Statement 3.6 explicitly covers providing existing test files in context so test generation avoids suggesting duplicate scenarios."
},
{
  id:174, module:4, scenario:"Claude Code for Continuous Integration",
  text:"Your CI review findings include both high-priority (must-fix) and low-priority (nice-to-have) issues. How should you structure feedback to developers?",
  options:[
    "Separate high and low priority findings; let developers tackle high-priority first for early value",
    "Mix all findings together; developers can prioritize themselves",
    "Provide numeric severity scores for developers to interpret",
    "Only include high-priority findings to reduce noise"
  ],
  correct:0,
  explanation:"Separating high and low priority findings lets developers tackle high-priority issues first for early value. This follows the incremental delivery principle and reduces cognitive overload by providing clear prioritization."
},
{
  id:175, module:2, scenario:"Claude Code for Continuous Integration",
  text:"Your CI code review agent needs access to an MCP tool that analyzes code complexity. Where should this tool be configured so the CI process can access it?",
  options:[
    "In the project .mcp.json file so it‘s version-controlled and available to CI",
    "In a .mcp-ci.json file specifically for CI",
    "In ~/.claude.json so it‘s personal and protected",
    "In CLAUDE.md as a tool reference"
  ],
  correct:0,
  explanation:"The MCP tool should be configured in the project .mcp.json file so it is version-controlled and available to CI. Task Statement 2.4 specifies project-level (.mcp.json) for shared team tooling versus user-level (~/.claude.json) for personal/experimental servers."
},
{
  id:176, module:1, scenario:"Claude Code for Continuous Integration",
  text:"Your CI coordinate agent spawns three review subagents (security, performance, style). What‘s the primary benefit of this hub-and-spoke architecture?",
  options:[
    "Reduces the need for manual code review",
    "Each subagent focuses on its domain with isolated context; the coordinator aggregates specialized results",
    "Subagents run in parallel, enabling faster overall execution",
    "Shared context between subagents ensures consistency"
  ],
  correct:1,
  explanation:"The primary benefit of hub-and-spoke architecture is that each subagent focuses on its domain with isolated context while the coordinator aggregates specialized results. Task Statement 1.2 describes this pattern: subagents operate with isolated context and the coordinator manages communication, error handling, and information routing."
},
{
  id:177, module:5, scenario:"Claude Code for Continuous Integration",
  text:"Your CI code review agent analyzes hundreds of files per PR. The review is unreliable with hallucinations. The analysis has built substantial context. What‘s the best recovery?",
  options:[
    "Resume the stale session to preserve all analysis context",
    "Break the PR into smaller sub-PRs and review separately",
    "Start a fresh review with a summary of high-priority findings from the prior analysis",
    "Increase the context window to accommodate more information"
  ],
  correct:2,
  explanation:"Starting a fresh review with a summary of high-priority findings from the prior analysis is the best recovery. Task Statement 1.7 states that starting a new session with a structured summary is more reliable than resuming with stale tool results when context is degraded."
},
{
  id:178, module:5, scenario:"Multi-Agent Research System",
  text:"Your coordinator session runs out of context midway through synthesis. What is the best recovery strategy?",
  options:[
    "Export findings to a database, start a fresh session, and continue synthesis with the exported data as context",
    "Prune the coordinator's conversation history to remove intermediate results, then continue",
    "Resume the session with session --resume and continue from where it left off",
    "Increase the model's context window size"
  ],
  correct:0,
  explanation:"Exporting findings to a structured format, starting a fresh session, and continuing synthesis with the exported data as context is the correct recovery strategy. Task Statement 5.4 covers structured state persistence for crash recovery where each agent exports state to a known location."
},
{
  id:179, module:5, scenario:"Multi-Agent Research System",
  text:"Your research report needs to distinguish between high-confidence findings and tentative claims. How should you structure the output schema to capture this distinction reliably?",
  options:[
    "Add a required confidence_notes field where the subagent explains reasoning",
    "Schema with explicit confidence scores (0-100) and required_evidence_count (min 2 sources for high)",
    "Include a confidence_level field (enum: high, medium, low)",
    "Trust the subagent to indicate confidence implicitly through language"
  ],
  correct:1,
  explanation:"A schema with explicit confidence scores (0-100) and required_evidence_count (min 2 sources for high) provides the most reliable distinction. This combines quantitative scoring with evidence requirements, making confidence calibration verifiable rather than relying on implicit language or simple enums without evidence backing."
},
{
  id:180, module:2, scenario:"Developer Productivity with Claude",
  text:"Your agent is exploring a codebase and needs to understand the database schema. How should it find this information?",
  options:[
    "Search for database migration files (e.g., *.sql, migration files) using Glob",
    "Use all three approaches to cross-reference",
    "Ask the user for the schema",
    "Read the ORM model files (e.g., models.py, entities.ts) that define the schema"
  ],
  correct:1,
  explanation:"Using all three approaches to cross-reference (migration files via Glob, ORM models, and asking the user) provides the most comprehensive understanding. Task Statement 2.5 emphasizes building codebase understanding incrementally using multiple search strategies."
},
{
  id:181, module:2, scenario:"Developer Productivity with Claude",
  text:"Your agent is generating code that should follow the codebase’s style guide. How should it access the style guide?",
  options:[
    "Use Grep to find comments mentioning style conventions",
    "Create an MCP tool that returns the style guide",
    "Have the user provide the style guide in their prompt",
    "Search for a style guide file in the codebase using Glob"
  ],
  correct:1,
  explanation:"Creating an MCP tool that returns the style guide provides a reliable, structured, and always-available mechanism for the agent to access the style guide. This is preferable to heuristic searches (Grep/Glob) or relying on user input."
},
{
  id:182, module:2, scenario:"Developer Productivity with Claude",
  text:"Your agent is tasked with understanding a codebase‘s entry point (main function, initialization code). Where should it start?",
  options:[
    "Ask the user where the entry point is",
    "Use Bash to trace execution flow from the project‘s build/run configuration",
    "Use Glob to find files named main, init, or start; check which is actually called",
    "Start with the main.py or index.js file (common entry points)"
  ],
  correct:2,
  explanation:"Using Glob to find files named main, init, or start and then checking which is actually called is the most systematic approach. It searches broadly for common entry-point naming conventions and then verifies, rather than assuming (option 3) or requiring user input."
},
{
  id:183, module:3, scenario:"Claude Code for Continuous Integration",
  text:"Your CI system is configured to run in non-interactive mode with the -p flag. Before that, you configured it with plan mode. Should the CI version keep plan mode?",
  options:[
    "It depends on the PR size",
    "Yes, but only for critical services",
    "No, plan mode creates unnecessary latency in CI. Use direct execution for speed",
    "Yes, plan mode helps catch architectural issues before making changes"
  ],
  correct:2,
  explanation:"In CI with the -p flag, plan mode creates unnecessary latency since there is no human to review and approve the plan. CI should use direct execution for speed and efficiency."
},
{
  id:184, module:1, scenario:"Multi-Agent Research System",
  text:"When a research subagent calls the web search tool and receives an MCP result with isError: false but malformed content, what should happen?",
  options:[
    "Return an error to the user indicating the search API is broken",
    "The subagent should retry the search automatically",
    "The coordinator should detect this in a PostToolUse hook and request the subagent re-query with different parameters",
    "The subagent sees the result, detects the malformation, and decides whether to retry or pivot to a different search strategy"
  ],
  correct:3,
  explanation:"Since isError is false, the tool call technically succeeded. The subagent (which is the model in the agentic loop) sees the result, can detect the malformation in the content, and autonomously decides whether to retry or pivot to a different strategy."
},
{
  id:185, module:3, scenario:"Customer Support Resolution Agent",
  text:"You‘re updating the agent to handle a new refund policy with complex conditions (different refund windows for different product categories, special handling for digital goods). This involves multiple files and rule changes. Should you use Claude Code‘s plan mode or direct execution?",
  options:[
    "Use plan mode to review the multi-file changes, then execute the plan after approval, reducing risk of unintended side effects",
    "Always use direct execution for speed, even for complex changes",
    "Plan mode is only for documentation, not actual code changes",
    "Never use plan mode; it‘s too slow"
  ],
  correct:0,
  explanation:"Plan mode is ideal for complex, multi-file changes because it lets you review the proposed changes before execution, reducing the risk of unintended side effects in a complex refund policy update."
},
{
  id:186, module:3, scenario:"Customer Support Resolution Agent",
  text:"Your CI/CD pipeline uses Claude Code to auto-generate test cases for new customer support features. You want structured output (JSON format) and non-interactive execution. What flags and configurations should you use?",
  options:[
    "Use --async to run in the background",
    "You cannot use Claude Code in CI pipelines; it‘s only for interactive use",
    "Just run claude-code with the prompt and let it output text",
    "Use -p for non-interactive mode, --output-format json, and --json-schema to define the expected JSON structure"
  ],
  correct:3,
  explanation:"For CI/CD pipelines, -p enables non-interactive mode, --output-format json ensures structured output, and --json-schema defines the expected JSON structure for validation. These are the correct flags for CI integration."
},
{
  id:187, module:4, scenario:"Customer Support Resolution Agent",
  text:"Your agent‘s system prompt says: ‘Process refunds quickly and efficiently.‘ But you find the agent sometimes refunds orders incorrectly. Why is this prompt guidance insufficient for reliability?",
  options:[
    "The prompt is fine; the agent just needs more training data",
    "The agent understands the rule but is choosing to ignore it",
    "The prompt is vague and lacks explicit criteria. ‘Correctly‘ means different things. You need specific rules like: ‘Only refund if: (1) order is within 30 days, (2) customer status is active, (3) item is not digital media‘",
    "Prompts cannot enforce rules; only code can"
  ],
  correct:2,
  explanation:"The prompt 'process refunds quickly and efficiently' is vague and lacks explicit criteria. Reliable agent behavior requires specific, enumerated rules (e.g., order within 30 days, active customer, non-digital item) rather than ambiguous instructions."
},
{
  id:188, module:4, scenario:"Customer Support Resolution Agent",
  text:"Your system prompt describes the agent‘s goals but doesn‘t provide examples of how to handle ambiguous cases (e.g., ‘What if a customer says they received the wrong item AND the return window expired?‘). What‘s the benefit of adding few-shot examples to the prompt?",
  options:[
    "Examples should be in the conversation history, not in the system prompt",
    "Few-shot examples show the agent how to handle ambiguous cases consistently, reducing interpretation variance and improving first-contact resolution",
    "Only test data needs examples; system prompts don‘t",
    "Few-shot examples make the prompt longer but don‘t improve agent behav"
  ],
  correct:1,
  explanation:"Few-shot examples demonstrate how to handle ambiguous cases consistently, reducing interpretation variance. They show the agent concrete decision patterns for edge cases, improving first-contact resolution."
},
{
  id:189, module:4, scenario:"Customer Support Resolution Agent",
  text:"You want the agent to return escalation decisions in a specific JSON format with fields: issue_type, severity, reason_for_escalation, recommended_handler. You‘re concerned the agent might not follow this format consistently. How should you enforce structured output?",
  options:[
    "Include the format in the system prompt and trust the agent to follow it",
    "Use tool_use with a JSON schema. Define an escalate_decision tool that takes the structured fields and returns the JSON-formatted response, forcing schema compliance",
    "Parse whatever the agent returns and convert it to JSON manually",
    "Structured output cannot be enforced; accept whatever format the agent produces"
  ],
  correct:1,
  explanation:"Using tool_use with a JSON schema forces the model to comply with a defined structure. Defining an escalate_decision tool with the required fields guarantees schema compliance rather than relying on the model to follow instructions."
},
{
  id:190, module:4, scenario:"Customer Support Resolution Agent",
  text:"Your agent sometimes makes mistakes in refund decisions (e.g., offering a refund when the policy says the customer is ineligible). You implement a validation mechanism where, after the agent decides on a resolution, you check it against the policy rules. If the decision is invalid, you return an error and ask the agent to reconsider. What‘s this pattern called?",
  options:[
    "Retry-with-error-feedback, where the agent receives validation error details and has the opportunity to revise its decision",
    "Session pruning",
    "Tool result caching",
    "Model fine-tuning"
  ],
  correct:0,
  explanation:"This is the retry-with-error-feedback pattern: after the agent makes a decision, validation checks it against policy rules, and if invalid, returns error details so the agent can revise its decision."
},
{
  id:191, module:4, scenario:"Customer Support Resolution Agent",
  text:"Your support team receives hundreds of customer requests daily. You want to use the Anthropic Batch API to process support tickets overnight, trading latency for cost savings. What‘s a key constraint you must understand about the Batch API in the context of agentic workflows?",
  options:[
    "The Batch API supports full multi-turn agentic loops with tool calling",
    "The Batch API can only process text, not JSON",
    "The Batch API is faster than real-time API calls",
    "The Batch API does not support multi-turn tool calling within a single request; each batch request is single-turn. You can‘t use the agentic loop inside a batch job"
  ],
  correct:3,
  explanation:"The Batch API does not support multi-turn tool calling within a single request. Each batch request is single-turn, so you cannot run the agentic loop (which requires multiple turns of tool calls) inside a batch job."
},
{
  id:192, module:4, scenario:"Customer Support Resolution Agent",
  text:"Your manager asks: ‘Why can‘t the agent just review its own decisions to catch mistakes?‘ You explain that self-review has limitations. What‘s the primary reason self-review is less effective than independent review?",
  options:[
    "Self-review is less cost-effective",
    "Self-review takes longer than independent review",
    "Self-review cannot access the tool results",
    "The model tends to agree with its own previous reasoning even if flawed. An independent review instance (separate request to Claude) is better at catching errors the original request missed"
  ],
  correct:3,
  explanation:"Self-review is less effective because the model tends to agree with its own previous reasoning, even if flawed. An independent review instance (separate API request) is better at catching errors because it evaluates the output without the bias of having produced it."
},
{
  id:193, module:4, scenario:"Customer Support Resolution Agent",
  text:"Your agent successfully resolves most customer issues, but sometimes it doesn‘t realize when it should escalate. For example, it attempted a refund for a case that required fraud verification. How would you improve escalation detection using prompt engineering and validation?",
  options:[
    "Accept that the agent will occasionally miss escalation opportunities",
    "Define explicit escalation criteria in the prompt: ‘Escalate if: (1) Customer reports fraud or unauthorized transaction, (2) Multiple failed refund attempts, (3) Account shows suspicious activity.‘ Combine with a validation rule: Before process_refund, check if the case has fraud signals and block refund with escalation instruction",
    "Tell the agent ‘Always escalate for fraud cases‘ and hope it remembers",
    "Require manual approval for all refunds"
  ],
  correct:1,
  explanation:"The best approach combines explicit escalation criteria in the prompt (specific conditions like fraud reports, multiple failed attempts, suspicious activity) with programmatic validation rules that block refunds when fraud signals are detected."
},
{
  id:194, module:5, scenario:"Customer Support Resolution Agent",
  text:"Your agent‘s conversation has grown to 8000 tokens. The customer has multiple unresolved issues, and the context window is nearly full. What‘s the best strategy to avoid hitting context limits while keeping the issues in scope?",
  options:[
    "Summarize the issues (names, status, pending actions) in a concise summary (200 tokens), clear the history, and continue with the summary as context. Store the full history separately for audit",
    "Force the agent to resolve all issues in the remaining context, even if it means skipping important steps",
    "End the conversation immediately and tell the customer to contact support again",
    "Ignore context warnings and keep the full history"
  ],
  correct:0,
  explanation:"Summarizing issues into a concise summary, clearing history, and continuing with the summary preserves essential context while freeing up the context window. Storing full history separately ensures audit compliance."
},
{
  id:195, module:5, scenario:"Customer Support Resolution Agent",
  text:"Your agent calls lookup_order and receives a response with 50 fields (order ID, customer ID, items, payment method, shipping address, billing address, dates, statuses, tracking info, notes, metadata, etc.). You want to include this in the conversation history to keep context efficient. What should you do with the tool result?",
  options:[
    "Include the entire 50-field response as-is in the conversation history",
    "Compress the result to a base64 string and include in history",
    "Don‘t include tool results in the conversation history at all",
    "Prune the result to relevant fields only (e.g., order ID, items, status, tracking) and include the pruned version in history. This keeps context efficient while preserving necessary information"
  ],
  correct:3,
  explanation:"Pruning the tool result to only relevant fields keeps the context efficient while preserving necessary information. Including all 50 fields wastes context window space on irrelevant data."
},
{
  id:196, module:5, scenario:"Customer Support Resolution Agent",
  text:"Your support agent handles multiple concurrent customer issues in a single conversation. One issue is resolved (return approved), but the other (billing dispute) needs more investigation. How should you track state across multiple issues to avoid losing context?",
  options:[
    "Implement structured state tracking: maintain a record of each issue‘s status, decisions made, and next steps. Include this state in the context explicitly as the conversation progresses (e.g., ‘OPEN ISSUES: Return (approved, return label sent), Billing (investigating, will follow up tomorrow‘)",
    "Create a new session for each issue",
    "Have the agent tell the customer which issues are resolved",
    "Just keep chatting; the agent will remember all issu"
  ],
  correct:0,
  explanation:"Structured state tracking explicitly maintains each issue's status, decisions, and next steps in the conversation context. This prevents context loss and ensures the agent can track multiple concurrent issues reliably."
},
{
  id:197, module:2, scenario:"Customer Support Resolution Agent",
  text:"Your agent encounters a situation where get_customer returns an error: ‘Customer not found.‘ But the customer ID provided was what the user gave you. Is this a transient error or a non-retryable error? What should the agent do?",
  options:[
    "It‘s transient; retry immediately",
    "It‘s a server error; scale up capacity",
    "It‘s non-retryable. The customer ID doesn‘t exist in your system. Return {“isError“: true, “errorCategory“: “not_found“, “isRetryable“: false} and instruct the agent to ask the customer for verification (perhaps they misremembered their ID, or it‘s a new customer not yet in the system)",
    "Ignore the error and return a dummy customer object"
  ],
  correct:2,
  explanation:"A 'Customer not found' error for a user-provided ID is non-retryable since the ID does not exist in the system. The structured error response should indicate not_found with isRetryable: false, and the agent should ask the customer to verify their ID."
},
{
  id:198, module:5, scenario:"Customer Support Resolution Agent",
  text:"You decide to start a fresh session for a resumed conversation the next day instead of using –resume. You summarize the previous session and pass it as context. What‘s the benefit of this approach over resuming the old session?",
  options:[
    "Fresh sessions are cheaper",
    "There‘s no real difference between resume and fresh sessions",
    "A fresh session with a summary starts with 20-30% context usage instead of 70%+ from the old session. This preserves context window for new work and can improve reasoning clarity by focusing on essential facts vs. full history",
    "Fresh sessions are always faster"
  ],
  correct:2,
  explanation:"A fresh session with a summary starts with much less context usage (20-30%) compared to resuming an old session (70%+). This preserves context window for new work and improves reasoning clarity by focusing on essential facts."
},
{
  id:199, module:3, scenario:"Customer Support Resolution Agent",
  text:"You‘re implementing a custom slash command called /resolve that your support team uses to quickly summarize a customer issue and suggest a resolution. Where should you define this custom command?",
  options:[
    "Custom commands cannot be defined; slash commands are built-in only",
    "In a .claude/commands/ file named resolve.md with the command definition and prompt",
    "In the .mcp.json configuration file",
    "In the system prompt of your agent"
  ],
  correct:1,
  explanation:"Custom slash commands in Claude Code are defined as markdown files in the .claude/commands/ directory. A file named resolve.md would create the /resolve command with the specified prompt and behavior."
},
{
  id:200, module:2, scenario:"Multi-Agent Research System",
  text:"Your synthesis tool needs to produce output in a specific JSON format. Should you design it as a generic ‘format_output‘ tool or a specialized ‘generate_research_report‘ tool?",
  options:[
    "Generic ‘format_output‘ - it can be reused across different scenarios",
    "Specialized ‘generate_research_report‘ - it can be optimized for your specific research domain and output requirements",
    "Create both; let the subagent choose",
    "Implement it as an MCP resource rather than a tool"
  ],
  correct:1,
  explanation:"A specialized 'generate_research_report' tool can be optimized for the specific research domain and output requirements, providing better tool descriptions, validation, and domain-specific formatting than a generic tool."
},
{
  id:201, module:1, scenario:"Multi-Agent Research System",
  text:"Your coordinator needs to validate that subagent results contain all required fields before synthesis. Where should this validation occur?",
  options:[
    "In the coordinator‘s agentic loop as reasoning about result quality",
    "In a separate validation tool that the subagent calls before returning",
    "In a PostToolUse hook applied to the Task tool result, validating structure and required fields",
    "In the subagent‘s system prompt as self-checks before returning results"
  ],
  correct:2,
  explanation:"A PostToolUse hook applied to the Task tool result can systematically validate the structure and required fields of subagent results before they reach the coordinator's reasoning loop. This provides programmatic validation at the right point in the workflow."
},
{
  id:202, module:4, scenario:"Multi-Agent Research System",
  text:"Your report generation subagent must produce output in a specific JSON format with citation fields. How should you guarantee that the output meets this requirement?",
  options:[
    "Define a JSON schema in a tool and set tool_choice to that tool, combined with a system prompt explaining the format",
    "Have the coordinator parse and reformat the output before presenting it to users",
    "Include detailed formatting rules in the subagent‘s system prompt",
    "Use tool_choice: ‘any‘ to force the subagent to call a formatting tool for output"
  ],
  correct:0,
  explanation:"Defining a JSON schema in a tool and setting tool_choice to that specific tool forces the model to call that tool with the exact schema, guaranteeing structured output with citation fields. Combining with a system prompt explanation ensures the model understands the format requirements."
},
{
  id:203, module:4, scenario:"Multi-Agent Research System",
  text:"When the synthesis subagent encounters conflicting findings from different research sources, what pattern helps it resolve the conflict reliably?",
  options:[
    "Discard one source and use only the most recent information",
    "Include both claims in the report and let the user decide",
    "Re-invoke the web search subagent to find a third source that breaks the tie",
    "Prompt the subagent to explicitly evaluate conflicting claims by source credibility, date, and methodology, then document the conflict resolution decision"
  ],
  correct:3,
  explanation:"Prompting the subagent to explicitly evaluate conflicting claims by source credibility, date, and methodology, then documenting the resolution decision, provides a systematic and transparent conflict resolution approach rather than arbitrary choices."
},
{
  id:204, module:4, scenario:"Multi-Agent Research System",
  text:"Your coordinator uses few-shot examples to teach subagents the expected output format. What should these examples illustrate?",
  options:[
    "Example queries only; let the subagent figure out the output format",
    "Output format in abstract (schema definition) without concrete examples",
    "Good examples of synthesis output only; avoid bad examples to prevent confusion",
    "Both good and bad examples: show desired output format, common mistakes, edge cases, and how to handle them"
  ],
  correct:3,
  explanation:"Effective few-shot examples should include both good and bad examples, showing desired output format, common mistakes, edge cases, and how to handle them. This gives the model a complete picture of expected behavior."
},
{
  id:205, module:5, scenario:"Multi-Agent Research System",
  text:"You need the synthesis subagent to cite sources accurately. What combination of techniques is most effective?",
  options:[
    "Pass citation metadata (URL, title, access date) from research subagents to synthesis; use a JSON schema tool to force structured citations",
    "Have the subagent re-validate each citation by checking the original URL",
    "Train the subagent on citation format rules in its system prompt and hope it follows them",
    "Create a manual citation review step after synthesis completes"
  ],
  correct:0,
  explanation:"Passing citation metadata (URL, title, access date) from research subagents to synthesis and using a JSON schema tool to force structured citations ensures citations are accurate, complete, and consistently formatted through provenance tracking."
},
{
  id:206, module:4, scenario:"Multi-Agent Research System",
  text:"Your synthesis subagent‘s output sometimes includes claims without adequate supporting evidence. How should you address this in the schema?",
  options:[
    "Add a required_evidence field that lists sources for each claim; validate programmatically that evidence ex",
    "Implement a separate evidence validation tool that runs after synthesis",
    "Add clear instructions in the system prompt: ‘Always cite your sources‘",
    "Include an optional evidence_notes field; subagents can skip it if they prefer"
  ],
  correct:0,
  explanation:"Adding a required_evidence field that lists sources for each claim and validating programmatically that evidence exists enforces citation requirements through schema design rather than relying on prompt instructions alone."
},
{
  id:207, module:4, scenario:"Multi-Agent Research System",
  text:"Your research system processes high-volume queries using the Batch API. When should you use Batch vs real-time invocations?",
  options:[
    "Batch API is incompatible with agentic loops; never use it in multi-agent systems",
    "Use real-time for all operations; Batch adds unnecessary complexity",
    "Use Batch for all coordinator invocations to reduce costs",
    "Use Batch for initial research phase (subagent invocations with longer latency tolerance); use real-time for interactive refinement and synthesis"
  ],
  correct:3,
  explanation:"Use Batch API for the initial research phase where subagent invocations can tolerate longer latency (cost savings), and use real-time API for interactive refinement and synthesis that requires immediate responses."
},
{
  id:208, module:5, scenario:"Multi-Agent Research System",
  text:"Your web search tool returns potentially voluminous results. To manage coordinator context window usage, what should you do?",
  options:[
    "Strip the tool results to only include URLs and relevance scores; omit content snippets",
    "Store full results in a database and reference them by ID in the coordinator‘s context",
    "Prune results aggressively to the top 3-5 most relevant items before appending to the coordinator‘s conversation",
    "Let the tool return all results; the model will manage context automatically"
  ],
  correct:2,
  explanation:"Pruning results aggressively to the top 3-5 most relevant items before appending to the coordinator's conversation manages context window usage effectively while preserving the most useful information for decision-making."
},
{
  id:209, module:5, scenario:"Multi-Agent Research System",
  text:"Your synthesis subagent needs to produce a findings report citing all sources. What is the best practice for ensuring citations are accurate and traceable?",
  options:[
    "Require the synthesis subagent to include full source text for every claim, then deduplicate later",
    "Trust the synthesis subagent‘s memory of sources and have the coordinator spot-check a few citations",
    "Pass citation metadata from research subagents to the synthesis subagent; include source location, access date, and retrieval method",
    "Have the synthesis subagent re-validate each source by checking the original URL in its own session"
  ],
  correct:2,
  explanation:"Passing citation metadata (source location, access date, retrieval method) from research subagents to the synthesis subagent ensures provenance tracking and accurate, traceable citations in the final report."
},
{
  id:210, module:5, scenario:"Multi-Agent Research System",
  text:"How should you structure state tracking to ensure consistency when a coordinator session spans multiple research phases (initial search, gap analysis, synthesis)?",
  options:[
    "Store state in each subagent‘s local context; they‘ll communicate relevant updates to the coordinator",
    "Use an external state management service to track phase transitions and results",
    "Rely on implicit state: the full conversation history implicitly tracks all decisions",
    "Maintain structured state in the coordinator‘s conversation history: summarize findings after each phase, track identified gaps, list sources consulted"
  ],
  correct:3,
  explanation:"Maintaining structured state in the coordinator's conversation history by summarizing findings after each phase, tracking identified gaps, and listing sources consulted provides explicit and reliable state tracking across multiple research phases."
},
{
  id:211, module:3, scenario:"Multi-Agent Research System",
  text:"Your .claude/CLAUDE.md file contains system prompt rules for all coordinator and subagent definitions. How should you organize this to ensure subagents receive only role-relevant rules?",
  options:[
    "Embed rules directly in each AgentDefinition system prompt to ensure isolation",
    "Define rules in .claude/rules/ with YAML paths that conditionally load based on agent role",
    "Include all rules in a global section; subagents ignore irrelevant ones automatically",
    "Use @import to modularize rules; create separate CLAUDE.md files or rule segments for coordinator vs research vs synthesis subagents"
  ],
  correct:3,
  explanation:"Using @import to modularize rules allows creating separate CLAUDE.md files or rule segments for different agent roles (coordinator vs research vs synthesis), ensuring each subagent receives only role-relevant rules through the CLAUDE.md hierarchy."
},
{
  id:212, module:2, scenario:"Developer Productivity with Claude",
  text:"Your agent is helping a developer add a new feature to a legacy system. How should it prioritize understanding the codebase before suggesting changes?",
  options:[
    "Spend time exploring: read key files, understand architecture, trace dependencies before suggesting changes",
    "Ask the developer to explain the codebase first",
    "Start suggesting changes immediately based on the feature request",
    "Generate a codebase analysis report for the developer to review"
  ],
  correct:0,
  explanation:"For legacy systems, the agent should spend time exploring by reading key files, understanding architecture, and tracing dependencies before suggesting changes. This ensures suggestions are compatible with the existing codebase."
},
{
  id:213, module:4, scenario:"Structured Data Extraction",
  text:"When extraction output is consumed by both humans (for review) and systems (for automation), how should you structure data for each audience?",
  options:[
    "Provide raw extraction data; each audience adapts",
    "Include extracted_for_human (readable, with context) and extracted_for_system (structured, normalized)",
    "Focus on system compatibility; humans can interpret",
    "Generate separate outputs for humans and systems"
  ],
  correct:1,
  explanation:"Including both extracted_for_human (readable, with context) and extracted_for_system (structured, normalized) in the output serves both audiences effectively. This dual-format approach ensures humans can review while systems can automate."
},
{
  id:214, module:5, scenario:"Structured Data Extraction",
  text:"Your extraction system sometimes produces slightly different results for the same document when run multiple times. What should you do?",
  options:[
    "Run once and treat that as ground truth",
    "Average results from multiple runs",
    "Accept variation as normal",
    "Determinism is important; investigate and fix inconsistency sources"
  ],
  correct:3,
  explanation:"Determinism is important for extraction systems, especially in production. Inconsistent results should be investigated and fixed rather than accepted as normal, as they indicate issues with prompting, temperature, or context handling."
},
{
  id:215, module:2, scenario:"Developer Productivity with Claude",
  text:"When your agent implements error handling in generated code, what patterns should it follow?",
  options:[
    "Include try-catch/try-except blocks matching the codebase‘s error handling style",
    "Implement comprehensive error handling with custom error types and logging",
    "Ignore errors; assume everything works",
    "Ask the user how to handle errors"
  ],
  correct:0,
  explanation:"The agent should match the codebase's existing error handling style by including try-catch/try-except blocks consistent with how the rest of the codebase handles errors, ensuring consistency rather than imposing a different pattern."
},
{
  id:216, module:2, scenario:"Developer Productivity with Claude",
  text:"Your agent needs to understand how the codebase handles logging. What should it search for?",
  options:[
    "Ask the user about logging setup",
    "Read all files looking for log statements",
    "Search for the word ‘log‘",
    "Use Grep to find logger instantiation patterns and examine how logging is configured"
  ],
  correct:3,
  explanation:"Using Grep to find logger instantiation patterns is the most efficient and targeted approach to understanding how logging is configured in a codebase, rather than reading all files or searching for a generic term like 'log'."
},
{
  id:217, module:2, scenario:"Developer Productivity with Claude",
  text:"Your agent is generating a new module that follows the codebase‘s architecture. How should it validate the architecture?",
  options:[
    "Ask the user about the architecture",
    "Use MCP tools to validate against an architectural standard",
    "Read key architectural documents, examine module structure, verify the generated module matches the pattern",
    "Trust the agent to understand architecture from prompts"
  ],
  correct:2,
  explanation:"Reading key architectural documents, examining existing module structure, and verifying the generated module matches the established pattern is the most thorough approach to validating architecture compliance."
},
{
  id:218, module:2, scenario:"Developer Productivity with Claude",
  text:"Your agent is tasked with adding a new feature to an existing API. What should it analyze first?",
  options:[
    "Analyze existing endpoints to understand URL patterns, parameter conventions, response formats, and error responses",
    "Start implementing the feature immediately",
    "Ask the user to describe the API structure",
    "Use an API documentation tool"
  ],
  correct:0,
  explanation:"When adding a feature to an existing API, the agent should first analyze existing endpoints to understand URL patterns, parameter conventions, response formats, and error handling to ensure the new feature is consistent."
},
{
  id:219, module:2, scenario:"Developer Productivity with Claude",
  text:"When your agent modifies configuration files, how should it handle comments and formatting?",
  options:[
    "Preserve existing comments and formatting as much as possible",
    "Ask the user about formatting preferences",
    "Reformat the file for consistency and remove unnecessary comments",
    "Don‘t modify configuration files; generate new ones instead"
  ],
  correct:0,
  explanation:"When modifying configuration files, preserving existing comments and formatting is critical because comments often contain important context about why settings exist, and unexpected formatting changes can cause confusion in code reviews."
},
{
  id:220, module:2, scenario:"Developer Productivity with Claude",
  text:"Your agent is exploring a codebase and wants to find code that needs refactoring (complexity, duplication, etc.). How should it approach this?",
  options:[
    "Generate a refactoring proposal based on code analysis",
    "Ask the user what needs refactoring",
    "Read every file looking for problems",
    "Use linters or complexity analyzers with Bash to identify issues"
  ],
  correct:3,
  explanation:"Using linters or complexity analyzers via Bash is the most efficient and systematic approach to identifying code that needs refactoring. These tools can objectively measure complexity, duplication, and other quality metrics."
},
{
  id:221, module:2, scenario:"Developer Productivity with Claude",
  text:"Your agent is tasked with fixing a failing test. What‘s the first step?",
  options:[
    "Modify the function the test is testing",
    "Run the test with Bash to see the failure message, analyze it, understand what the function should do",
    "Skip the failing test",
    "Ask the user why the test is failing"
  ],
  correct:1,
  explanation:"The first step when fixing a failing test is to run it with Bash to see the actual failure message, analyze what went wrong, and understand what the function should do. This diagnostic approach prevents premature or incorrect fixes."
},
{
  id:222, module:2, scenario:"Developer Productivity with Claude",
  text:"When your agent has modified multiple files as part of a feature implementation, how should it verify the feature is complete and correct?",
  options:[
    "Run test suites with Bash, check build status with Bash, verify documentation is updated",
    "Assume the implementation is correct",
    "Ask the user to test the feature",
    "Generate a test that exercises the new feature"
  ],
  correct:0,
  explanation:"After modifying multiple files, running test suites, checking build status, and verifying documentation are all necessary verification steps. Using Bash to run these checks provides comprehensive validation of the feature implementation."
},
{
  id:223, module:2, scenario:"Developer Productivity with Claude",
  text:"Your agent is building a system that generates boilerplate code for different frameworks (React, Vue, Angular). How should it handle framework-specific differences?",
  options:[
    "For each framework, Glob+Read examples, understand framework-specific patterns, generate framework-specific code",
    "Generate generic code that works with all frameworks",
    "Create separate prompts for each framework",
    "Use MCP servers for each framework"
  ],
  correct:0,
  explanation:"For framework-specific boilerplate, using Glob+Read to examine existing examples in each framework, understanding framework-specific patterns, and then generating framework-specific code ensures the output follows each framework's conventions."
},
{
  id:224, module:2, scenario:"Developer Productivity with Claude",
  text:"Your agent needs to understand a codebase‘s deployment process. Where should it look?",
  options:[
    "Use an MCP deployment tool",
    "Ask the user about deployment",
    "Use Glob+Grep to find deployment scripts, CI/CD configs (GitHub Actions, Jenkins, Makefile), and Dockerfiles",
    "Read the project documentation"
  ],
  correct:2,
  explanation:"Using Glob+Grep to find deployment scripts, CI/CD configs (GitHub Actions, Jenkins, Makefile), and Dockerfiles is the most systematic approach to understanding a codebase's deployment process from the source of truth."
},
{
  id:225, module:2, scenario:"Developer Productivity with Claude",
  text:"When your agent generates code, how should it decide whether the code should be in a new file vs. added to an existing file?",
  options:[
    "Ask the user where code should go",
    "Always modify existing files to avoid file proliferation",
    "Always create new files to keep changes isolated",
    "Analyze the codebase structure and follow existing conventions for where code lives"
  ],
  correct:3,
  explanation:"The agent should analyze the codebase structure and follow existing conventions for where code lives. This ensures new code fits naturally into the project organization rather than arbitrarily creating new files or modifying existing ones."
},
{
  id:226, module:5, scenario:"Developer Productivity with Claude",
  text:"When your agent generates documentation from code, how should it handle inline comments that contradict the actual behavior?",
  options:[
    "Ask the user which is correct",
    "Use only the comments; they explain intent",
    "Use only the code; comments are unreliable",
    "Extract both code behavior and comments; flag discrepancies for human review"
  ],
  correct:3,
  explanation:"Extracting both code behavior and comments and flagging discrepancies for human review is the safest approach. Neither code nor comments should be blindly trusted when they contradict each other; the discrepancy needs human judgment."
},
{
  id:227, module:5, scenario:"Developer Productivity with Claude",
  text:"When your agent is generating code but encounters a design pattern used inconsistently in the codebase, should it normalize to one pattern?",
  options:[
    "Ask the user which pattern to follow",
    "Analyze which pattern is dominant, generate code using the most common pattern, and document the choice",
    "Generate code that matches the inconsistent pattern exactly",
    "Use a new pattern that‘s clearly better"
  ],
  correct:1,
  explanation:"Analyzing which pattern is dominant, generating code using the most common pattern, and documenting the choice is the best approach. It follows existing conventions while being transparent about the decision."
},
{
  id:228, module:4, scenario:"Structured Data Extraction",
  text:"When you need to extract similar information from different document types (invoices, receipts, bills), what‘s the best schema strategy?",
  options:[
    "Create separate schemas for each document type",
    "Create a unified schema that works for all types",
    "Use a base schema with type-specific extensions (inheritance)",
    "Ask the user which document type to extract from"
  ],
  correct:2,
  explanation:"A base schema with type-specific extensions (inheritance) provides the best balance: shared fields are defined once in the base schema, while document-type-specific fields are added through extensions, avoiding duplication and maintaining flexibility."
},
{
  id:229, module:2, scenario:"Developer Productivity with Claude",
  text:"Your agent is analyzing a large monorepo with multiple languages and frameworks. What‘s the strategy for understanding the structure quickly?",
  options:[
    "Ask the user to explain the monorepo structure",
    "Visualize the repo with external tools",
    "Use Glob to find root configuration files and Grep to understand the language breakdown per directory",
    "Read every file to understand the full structure"
  ],
  correct:2,
  explanation:"Using Glob to find root configuration files and Grep to understand the language breakdown per directory is the most efficient strategy for quickly understanding a large monorepo's structure without reading every file."
},
{
  id:230, module:5, scenario:"Structured Data Extraction",
  text:"When extracted data will be used in critical systems (e.g., legal contracts), what additional validation is essential?",
  options:[
    "Multi-level validation: schema validation, semantic validation, human review sampling, confidence thresholds for automation",
    "Use external services to validate everything",
    "Standard JSON schema validation is sufficient",
    "Focus only on accuracy; other validation is unnecessary"
  ],
  correct:0,
  explanation:"For critical systems like legal contracts, multi-level validation is essential: schema validation, semantic validation, human review sampling, and confidence thresholds for automation provide defense-in-depth against extraction errors."
},
{
  id:231, module:5, scenario:"Structured Data Extraction",
  text:"When extraction quality varies by field (some fields are highly accurate, others less so), how should your system respond?",
  options:[
    "Set field-specific thresholds: require 95% confidence for critical fields, 70% for optional ones",
    "Average confidence across all fields",
    "Use the same confidence threshold for all fields",
    "Extract everything; let downstream handle quality"
  ],
  correct:0,
  explanation:"Field-specific confidence thresholds (95% for critical, 70% for optional) allow the system to enforce stricter accuracy where it matters most, rather than applying a one-size-fits-all threshold or ignoring quality altogether."
},
{
  id:232, module:5, scenario:"Structured Data Extraction",
  text:"When extracting PII (personally identifiable information), what additional safeguards are essential?",
  options:[
    "Extraction should include: PII_detected flag, minimal_retention_period, access_control_required in metadata; sensitive fields should be flagged for encryption",
    "Standard extraction with no special handling",
    "Extract PII but don‘t log it for privacy",
    "Avoid extracting PII altogether"
  ],
  correct:0,
  explanation:"PII extraction requires explicit metadata flags (PII_detected, minimal_retention_period, access_control_required) and encryption flagging to ensure regulatory compliance and proper data handling throughout the pipeline."
},
{
  id:233, module:5, scenario:"Structured Data Extraction",
  text:"When your extraction output is used by multiple downstream systems with different data format requirements, how should you structure output?",
  options:[
    "Include original extracted values plus pre-computed normalization for common formats (standardized, csv, xml)",
    "Provide only the raw extraction and let systems normalize",
    "Generate separate output for each downstream system",
    "Extract once and ask each system to adapt"
  ],
  correct:0,
  explanation:"Including original extracted values plus pre-computed normalizations for common formats provides flexibility for downstream systems while avoiding redundant extraction or forcing each system to implement its own normalization logic."
},
{
  id:234, module:5, scenario:"Structured Data Extraction",
  text:"Your extraction system needs to handle documents with quality issues (blurry, poorly scanned, multiple languages). What preprocessing should you apply before extraction?",
  options:[
    "Extract from raw documents; preprocessing adds latency",
    "Ask users to provide clean documents",
    "For images/scans, apply OCR preprocessing; for poor quality, use image enhancement; track preprocessing applied in metadata",
    "Use only documents that pass quality checks; skip poor-quality ones"
  ],
  correct:2,
  explanation:"Applying OCR preprocessing for scans, image enhancement for poor quality, and tracking preprocessing steps in metadata ensures maximum extraction quality while maintaining an audit trail of what transformations were applied."
},
{
  id:235, module:5, scenario:"Structured Data Extraction",
  text:"When extraction confidence varies by document source (brand new documents vs. legacy formats), how should you account for this?",
  options:[
    "Require higher confidence for all documents regardless of source",
    "Extract with uniform confidence; downstream systems should account for source",
    "Track source_type and adjust confidence thresholds accordingly: legacy documents might need lower thresholds due to format inconsistency",
    "Ignore source; confidence should be uniform"
  ],
  correct:2,
  explanation:"Tracking source_type and adjusting confidence thresholds accordingly accounts for the reality that legacy documents have inherent format inconsistencies, allowing appropriate quality expectations per source rather than a rigid uniform threshold."
},
{
  id:236, module:5, scenario:"Structured Data Extraction",
  text:"When extraction fails partially (some fields succeeded, others failed), how should output be structured?",
  options:[
    "Return error and ask for retry",
    "Return only successfully extracted fields; skip failed ones",
    "Return only if all fields extract successfully",
    "Return all fields with extraction_status per field: success, not_found, ambiguous, extraction_failed"
  ],
  correct:3,
  explanation:"Returning all fields with per-field extraction_status (success, not_found, ambiguous, extraction_failed) provides maximum transparency about what succeeded and what failed, enabling downstream systems to make informed decisions about partial data."
},
{
  id:237, module:5, scenario:"Structured Data Extraction",
  text:"When you discover that extracted data contains systematic errors (e.g., dates always off by one month), what‘s the recovery strategy?",
  options:[
    "Manually correct the data in the database",
    "Accept the error; downstream systems should validate",
    "Identify the error pattern, fix the extraction logic, re-process affected documents, track what was corrected",
    "Ignore past errors and focus on preventing future ones"
  ],
  correct:2,
  explanation:"The correct recovery strategy involves identifying the error pattern, fixing extraction logic, re-processing affected documents, and tracking corrections. This addresses root cause, remediates existing data, and creates an audit trail."
},
{
  id:238, module:5, scenario:"Structured Data Extraction",
  text:"When scaling extraction to millions of documents, what efficiency optimization is most important?",
  options:[
    "Process documents in parallel with multiple model instances",
    "Process everything with highest accuracy (slow but accurate)",
    "Use a faster, less accurate model",
    "Use batch processing to reduce per-document overhead; prioritize high-confidence extractions for fast processing"
  ],
  correct:3,
  explanation:"Batch processing reduces per-document overhead at scale and prioritizing high-confidence extractions for fast processing optimizes throughput. This balances efficiency with accuracy rather than sacrificing one for the other."
},
{
  id:239, module:5, scenario:"Structured Data Extraction",
  text:"Your extraction system extracts text that should be interpreted as structured data (e.g., ‘Q1 2024‘ should be structured as quarter:1, year:2024). How should you handle this?",
  options:[
    "Include both original text and parsed structure in output: {“period_text“: “Q1 2024“, “period_structured“: {“quarter“: 1, “year“: 2024}, “parsing_confidence“: 0.95}",
    "Ask users how to interpret the text",
    "Always parse to structured form; don‘t return raw text",
    "Extract the text as-is; downstream systems interpret it"
  ],
  correct:0,
  explanation:"Including both the original text and parsed structured form with parsing confidence preserves provenance and allows downstream systems to verify the interpretation, which is essential for auditability and error recovery."
},
{
  id:240, module:5, scenario:"Structured Data Extraction",
  text:"When validating extracted data against downstream system requirements, what integration pattern is most maintainable?",
  options:[
    "Don‘t validate; systems handle their own errors",
    "Use a validation service that both extraction and downstream systems call",
    "Validate in the extraction system before passing to downstream",
    "Each downstream system validates independently"
  ],
  correct:1,
  explanation:"A shared validation service that both extraction and downstream systems call creates a single source of truth for validation rules, making it the most maintainable pattern since rules only need to be updated in one place."
},
{
  id:241, module:5, scenario:"Structured Data Extraction",
  text:"When your extraction system discovers that two fields contradict each other (e.g., ‘total amount‘ doesn‘t equal sum of ‘line_items‘), what should happen?",
  options:[
    "Recalculate one field to match the other",
    "Use one value and discard the other",
    "Return an error requiring human intervention",
    "Include both values with a detected_contradiction flag and explanation of the discrepancy"
  ],
  correct:3,
  explanation:"Including both values with a detected_contradiction flag and explanation preserves all data for human review rather than silently discarding information or making potentially incorrect corrections automatically."
},
{
  id:242, module:5, scenario:"Structured Data Extraction",
  text:"When you need to extract data that changes over time (e.g., status field with multiple values), how should the schema handle this?",
  options:[
    "Extract all statuses but don‘t track changes",
    "Ask the user which status matters",
    "Extract status_history as an array with timestamps: [{status: “pending“, timestamp: “2024-01-01“}, {status: “approved“, timestamp: “2024-02-01“}]",
    "Extract only the current status"
  ],
  correct:2,
  explanation:"Extracting status_history as a timestamped array captures the full temporal evolution of the data, which is essential for audit trails and understanding how a record changed over time rather than losing historical context."
},
{
  id:243, module:5, scenario:"Developer Productivity with Claude",
  text:"Your agent generates code in a language it hasn‘t seen before in this codebase. How should it ensure correctness?",
  options:[
    "Use an MCP language-specific tool",
    "First Glob+Read to find examples in that language, understand idioms and patterns, then generate",
    "Ask the user for language expertise",
    "Use its general knowledge and generate code as usual"
  ],
  correct:1,
  explanation:"Using Glob+Read to find existing examples in the unfamiliar language allows the agent to understand codebase-specific idioms, patterns, and conventions before generating code, ensuring consistency with the existing codebase."
},
{
  id:244, module:5, scenario:"Code Generation with Claude Code",
  text:"Your session context is getting very large after many interactions. The session is starting to become unreliable. What‘s the best recovery approach?",
  options:[
    "Increase the context window size parameter",
    "Start a fresh session with a summary of findings and key context, rather than resuming the stale session",
    "Clear the session cache and continue",
    "Use --resume to continue from where you left off"
  ],
  correct:1,
  explanation:"Starting a fresh session with a summary of key findings prevents context degradation that occurs in very large sessions. A clean session with curated context is more reliable than continuing with a bloated, stale context window."
},
{
  id:245, module:1, scenario:"Customer Support Resolution Agent",
  text:"You want to ensure the agent always checks the refund policy before calling process_refund. You‘re considering two approaches: (1) Include the policy in the system prompt, (2) Implement a PreToolUse hook that validates the policy. Why is approach 2 more reliable?",
  options:[
    "Only one approach can be used at a time",
    "PreToolUse hooks are faster than system prompts",
    "System prompts are sufficient; hooks are unnecessary overhead",
    "PreToolUse hooks provide programmatic enforcement that‘s independent of the model‘s reasoning. If the policy is in the system prompt and the model forgets or rationalizes, the hook still catches it"
  ],
  correct:3,
  explanation:"PreToolUse hooks provide programmatic enforcement independent of model reasoning. Unlike system prompt instructions which the model might forget or rationalize away, hooks execute deterministically before every tool call regardless of model behavior."
},
{
  id:246, module:4, scenario:"Customer Support Resolution Agent",
  text:"After a customer escalation is sent to the human team, you want to track what the human did and ensure the agent learns from it. What‘s a good pattern for closing the loop and improving the agent?",
  options:[
    "Implement a feedback loop: after human resolution, record the decision (approved refund, negotiated discount, etc.) and the reasoning. Use this to refine the agent‘s future behavior (improve system prompt, add examples, adjust policies)",
    "Have the human agent document their action separately; the support agent doesn‘t need to know",
    "Agent behavior can‘t be improved; accept that escalations are permanent",
    "Only collect feedback for failed escalations, not successful ones"
  ],
  correct:0,
  explanation:"A feedback loop that records human decisions and reasoning after escalation enables continuous improvement of the agent through refined prompts, additional examples, and adjusted policies, creating a virtuous cycle of learning."
},
{
  id:247, module:2, scenario:"Customer Support Resolution Agent",
  text:"You‘re designing tool descriptions for your MCP tools. A colleague argues that tool descriptions should be minimal to ‘keep things simple.‘ What‘s your response?",
  options:[
    "Tool descriptions are just metadata; they don‘t affect agent behavior",
    "Detailed tool descriptions are the primary mechanism for LLM tool selection. Vague descriptions lead to wrong tool choices and failed attempts. Include input formats, output examples, edge cases, and when NOT to use the tool",
    "Tool descriptions should be minimal; detailed descriptions confuse the model",
    "Tool descriptions should only describe inputs, not outputs"
  ],
  correct:1,
  explanation:"Detailed tool descriptions are the primary mechanism LLMs use for tool selection. They should include input formats, output examples, edge cases, and when NOT to use the tool to prevent wrong tool choices and failed attempts."
},
{
  id:248, module:2, scenario:"Customer Support Resolution Agent",
  text:"Your get_customer tool is slow (200ms average latency). You want to cache results so repeated lookups don‘t waste time. What‘s the best place to implement caching?",
  options:[
    "Cache in the conversation history by storing the customer object after the first lookup",
    "Don‘t cache; latency is acceptable",
    "Cache only unsuccessful lookups",
    "Cache at the MCP server level, so the same customer_id request returns cached results within 5 minutes"
  ],
  correct:3,
  explanation:"Caching at the MCP server level with a TTL (e.g., 5 minutes) is the correct architectural pattern because it transparently serves cached results for repeated lookups without polluting conversation history or requiring client-side changes."
},
{
  id:249, module:2, scenario:"Customer Support Resolution Agent",
  text:"You want to add a new tool called check_warranty that validates whether an item is under warranty. Should this tool have read-only access to customer data, or should it be able to modify warranty status?",
  options:[
    "check_warranty should be read-only. Only tools like process_refund (which has clear financial implications) should modify data. This principle of least privilege reduces unintended side effects",
    "Read-only access is too restrictive for modern agents",
    "Tools should be able to modify data for efficiency",
    "All tools should have the same permissions"
  ],
  correct:0,
  explanation:"check_warranty should be read-only following the principle of least privilege. Only tools with clear side-effect requirements (like process_refund) should modify data, reducing the risk of unintended changes from agent tool calls."
},
{
  id:250, module:1, scenario:"Customer Support Resolution Agent",
  text:"Your agent is handling a complex case with multiple subagents. You want to ensure that if one subagent fails, the coordinator can recover gracefully. What pattern supports this resilience?",
  options:[
    "Subagents cannot fail; accept this limitation",
    "Require human approval for all subagent work",
    "Use fork_session to create parallel subagent branches that can be independently retried",
    "If a subagent fails, cancel the entire operation"
  ],
  correct:2,
  explanation:"Using fork_session to create parallel subagent branches enables independent retry of failed subagents without affecting other branches, providing resilience in multi-agent orchestration through isolated execution contexts."
},
{
  id:251, module:3, scenario:"Customer Support Resolution Agent",
  text:"Your CLAUDE.md file has grown to 500 lines with rules for refunds, returns, billing, warranty, account issues, and escalation. It‘s becoming hard to maintain. How should you refactor it?",
  options:[
    "Use @import in the root CLAUDE.md to reference feature-specific rule files (.claude/rules/refunds.md, .claude/rules/returns.md, etc.)",
    "Split into multiple modular CLAUDE.md files in subdirectories",
    "Store rules in a database instead of files",
    "Keep everything in one file for simplicity"
  ],
  correct:0,
  explanation:"Using @import in the root CLAUDE.md to reference feature-specific rule files in .claude/rules/ provides modular organization. This is the recommended Claude Code pattern for managing large configuration files."
},
{
  id:252, module:3, scenario:"Customer Support Resolution Agent",
  text:"Your team uses a custom skill called /case-analysis that summarizes customer cases for review. However, the skill is stored locally and not part of version control. What‘s the risk?",
  options:[
    "Without version control, you can‘t track changes, revert to previous versions, or collaborate safely. Skills should be in .claude/skills/ and committed to git",
    "Version control is only for source code, not configuration",
    "Skills are temporary; they don‘t need to be version-controlled",
    "Skills cannot be version-controlled"
  ],
  correct:0,
  explanation:"Skills stored outside version control cannot be tracked, reverted, or safely collaborated on. They should be in .claude/skills/ and committed to git for proper change management and team collaboration."
},
{
  id:253, module:4, scenario:"Customer Support Resolution Agent",
  text:"You want the agent to output a summary of each resolved case in a specific JSON format for logging. The agent sometimes includes extra fields or uses different field names. How can you ensure consistent output structure without manual post-processing?",
  options:[
    "Define a summary_case tool with a JSON schema that includes required fields (issue_type, resolution, customer_satisfaction_score). Use tool_choice to force the agent to call this tool when summarizing",
    "Ask the agent politely in the system prompt to follow the format",
    "Accept variable output formats and store them as unstructured text",
    "Parse and transform the agent‘s output manually"
  ],
  correct:0,
  explanation:"Defining a summary_case tool with a JSON schema and using tool_choice to force its use ensures consistent output structure. The schema enforces required fields, and tool_choice guarantees the agent uses the correct format."
},
{
  id:254, module:4, scenario:"Customer Support Resolution Agent",
  text:"Your agent occasionally misunderstands edge cases like ‘Customer returned item but also wants credit for time spent on the return.‘ Your few-shot examples in the system prompt don‘t cover this case. What‘s the best way to teach the agent this pattern?",
  options:[
    "Escalate all edge cases to humans",
    "Create an in-context example in the conversation before asking the agent to handle similar cases",
    "Add more and more examples to the system prompt",
    "Give up on handling edge cases"
  ],
  correct:1,
  explanation:"Creating an in-context example in the conversation before handling similar cases is the few-shot learning approach. It teaches the agent the specific edge case pattern without overloading the system prompt with too many examples."
},
{
  id:255, module:2, scenario:"Customer Support Resolution Agent",
  text:"Your agent‘s tool calls are sometimes rejected due to validation errors (e.g., ‘Order ID must be numeric‘). Rather than blocking the agent, you want it to learn from the error and retry. How should you structure error feedback?",
  options:[
    "Modify the input silently to make it valid",
    "Reject the call without explanation",
    "Return {“isError“: true, “errorCategory“: “validation_error“, “details“: “Order ID must be numeric. You provided: ‘ + provided_id + ‘. Please ask the customer for clarification or retry with a numeric ID.“} to give the agent specific, actionable feedback",
    "Return a generic error message and let the agent guess how to fix it"
  ],
  correct:2,
  explanation:"Returning structured error feedback with isError, errorCategory, and specific actionable details gives the agent the information it needs to self-correct. This follows the structured error pattern that enables validation/retry loops."
},
{
  id:256, module:5, scenario:"Customer Support Resolution Agent",
  text:"You‘re monitoring tool latencies and notice lookup_order is slow (500ms). This is delaying customer resolutions. What‘s a reasonable approach to improve responsiveness?",
  options:[
    "Ask customers to wait longer",
    "Implement response streaming where the agent starts reasoning about partial results (e.g., order status) while more detailed data (items, tracking) is still being fetched",
    "Make lookup_order faster by rewriting the database query",
    "Eliminate the lookup_order tool entirely"
  ],
  correct:1,
  explanation:"Response streaming where the agent begins reasoning about partial results while more data is fetched improves perceived responsiveness without eliminating the tool entirely or requiring database-level changes."
},
{
  id:257, module:5, scenario:"Customer Support Resolution Agent",
  text:"You want to measure whether your 80%+ first-contact resolution target is being met. What metrics should you track to validate agent performance?",
  options:[
    "Metrics are not important; focus on volume only",
    "Track: (1) Escalation rate (% of issues escalated), (2) First-contact resolution rate (% resolved without escalation), (3) Customer satisfaction (post-resolution rating), (4) Refund accuracy (% of refunds that were policy-compliant). This gives a complete picture of agent effectiveness",
    "Only track response time",
    "Only track the number of conversations handled"
  ],
  correct:1,
  explanation:"Tracking escalation rate, first-contact resolution rate, customer satisfaction, and refund accuracy provides a comprehensive view of agent effectiveness across multiple dimensions rather than a single narrow metric."
},
{
  id:258, module:5, scenario:"Customer Support Resolution Agent",
  text:"Your agent will eventually make mistakes (wrong refund amounts, missed policy violations, incorrect escalations). Rather than trying to prevent 100% of mistakes, what‘s a realistic reliability strategy?",
  options:[
    "Attempt to build a perfect agent that makes zero mistakes",
    "Accept all mistakes without learning from them",
    "Don‘t validate agent output; trust it completely",
    "Accept mistakes as inevitable and implement defense layers: (1) Validation hooks that catch obvious errors, (2) Post-resolution audits to find mistakes, (3) Feedback loops where human resolutions improve agent training. Aim for acceptable error rates (e.g., <2% policy violations) rather than perfection"
  ],
  correct:3,
  explanation:"A layered defense strategy with validation hooks, post-resolution audits, and feedback loops accepts that mistakes are inevitable while implementing multiple safeguards to catch and learn from them, targeting acceptable error rates."
},
{
  id:259, module:1, scenario:"Customer Support Resolution Agent",
  text:"You‘re designing an AgentDefinition for your support agent. You want to restrict it to specific tools and configure its system prompt with detailed refund policies. What parameters of AgentDefinition are most important for configuring these constraints?",
  options:[
    "AgentDefinition is auto-generated; you can‘t configure it",
    "Only the name and description are required",
    "Only allowedTools matters; systemPrompt is ignored by the SDK",
    "The systemPrompt (detailed rules), allowedTools (list of tools), and tool descriptions are key for configuring behavior and constraints"
  ],
  correct:3,
  explanation:"The systemPrompt (for detailed rules), allowedTools (to restrict available tools), and tool descriptions are the key AgentDefinition parameters for configuring agent behavior and constraints in multi-agent orchestration."
},
{
  id:260, module:5, scenario:"Code Generation with Claude Code",
  text:"You‘re generating multiple test cases for the same function. On the second request, Claude Code suggests test cases identical to the first. How can you prevent this duplication?",
  options:[
    "Use a deduplication tool to remove identical tests",
    "Ask Claude Code to generate different test cases in the second prompt",
    "Include existing test cases in the prompt context so Claude Code can see what‘s already covered",
    "Create separate sessions for each test generation request"
  ],
  correct:2,
  explanation:"Including existing test cases in the prompt context allows Claude Code to see what's already covered and generate complementary tests, leveraging context preservation to prevent duplication."
},
{
  id:261, module:5, scenario:"Code Generation with Claude Code",
  text:"You need to review a pull request and provide feedback in Claude Code. On re-review of the same PR, Claude Code repeats the same comments. What context should you provide to avoid duplicate feedback?",
  options:[
    "Include prior review findings so Claude Code recognizes already-discussed issues",
    "Start a new review session to ensure fresh perspective",
    "Ask Claude Code to provide only new findings not mentioned before",
    "Use a different reviewer instance to get independent feedback"
  ],
  correct:0,
  explanation:"Including prior review findings in context lets Claude Code recognize already-discussed issues and focus on new findings, preventing duplicate feedback through proper context management."
},
{
  id:262, module:3, scenario:"Code Generation with Claude Code",
  text:"Your CLAUDE.md file specifies testing standards for your codebase. You‘re running Claude Code in CI mode to generate tests for a PR. Should the CI-invoked Claude Code have access to this CLAUDE.md?",
  options:[
    "Use a CI-specific CLAUDE.md rather than the project one",
    "Yes; provide CLAUDE.md to the CI process so Claude Code generates tests matching your team‘s standards",
    "No; CI-invoked Claude Code should be independent of project configuration",
    "Only provide CLAUDE.md for critical tests, not all test generation"
  ],
  correct:1,
  explanation:"Yes, CI-invoked Claude Code should have access to the project CLAUDE.md so generated tests follow the team's established standards, conventions, and patterns defined in the configuration."
},
{
  id:263, module:3, scenario:"Code Generation with Claude Code",
  text:"You‘re using Claude Code to generate a new feature that spans 5 files and requires significant architectural planning. The plan phase outputs a detailed strategy. Should you accept this plan before execution?",
  options:[
    "No, let Claude Code execute directly for speed",
    "No, plans are usually wrong; direct execution finds real issues faster",
    "Yes, review the plan for correctness before executing to catch issues early",
    "Yes, but only approve the plan if you understand every line of code"
  ],
  correct:2,
  explanation:"Reviewing the plan for correctness before execution catches architectural issues early when they're cheap to fix, which is the core value of plan mode for complex multi-file changes."
},
{
  id:264, module:5, scenario:"Code Generation with Claude Code",
  text:"Your team is split on whether to use Claude Code for exploratory analysis (finding potential issues) or only for targeted fixes. An engineer wants to use the Explore subagent for verbose discovery. Where should this run?",
  options:[
    "Only in interactive sessions, never in automation",
    "In a CI pipeline for automated discovery",
    "In a separate session with context: fork isolation to avoid polluting main analysis context",
    "In the main session alongside other analysis"
  ],
  correct:2,
  explanation:"Running exploratory analysis in a separate session with fork isolation prevents verbose discovery output from polluting the main analysis context, keeping concerns separated and context clean."
},
{
  id:265, module:5, scenario:"Code Generation with Claude Code",
  text:"A colleague prefers to use Claude Code in a session-per-task model (separate session for review, separate for generation, etc.). What‘s an advantage of this approach?",
  options:[
    "Session isolation prevents token limits from being exceeded",
    "Different sessions have isolated context, preventing review findings from biasing code generation",
    "Sessions run in parallel, enabling faster overall completion",
    "Separate sessions allow different CLAUDE.md configurations per task"
  ],
  correct:1,
  explanation:"Session isolation ensures that review findings don't bias code generation and vice versa. Different tasks benefit from clean, isolated contexts that prevent cross-contamination of reasoning."
},
{
  id:266, module:3, scenario:"Code Generation with Claude Code",
  text:"You have a .claude/rules/ directory with 8 rule files. You‘re editing a TypeScript file in the /api directory. How does Claude Code determine which rules apply?",
  options:[
    "It loads rules with glob patterns matching the file path /api/*.ts, plus any directory-level CLAUDE.md in /api",
    "It loads only the first matching rule by priority",
    "It loads all 8 rules regardless of file path",
    "It loads rules only if explicitly mentioned in project CLAUDE.md"
  ],
  correct:0,
  explanation:"Claude Code uses glob patterns in .claude/rules/ files to match against the current file path, plus any directory-level CLAUDE.md. Only rules whose glob patterns match the file being edited are loaded."
},
{
  id:267, module:3, scenario:"Code Generation with Claude Code",
  text:"You want to create a code generation command that‘s available only to your team, not shared beyond the project. Where should this go?",
  options:[
    "A bash script in the repository root",
    "A custom tool in your project‘s MCP server",
    "A skill in .claude/skills/ instead of a command",
    ".claude/commands/ in the project repository for version control and team sharing"
  ],
  correct:3,
  explanation:".claude/commands/ in the project repository provides version-controlled, team-shared custom commands. This is the standard Claude Code location for project-specific commands that should be available to all team members."
},
{
  id:268, module:3, scenario:"Code Generation with Claude Code",
  text:"You‘re using Claude Code to refactor a legacy module. Multiple strategies could work. Direct execution would start changing files immediately. What‘s the value of using plan mode here?",
  options:[
    "Plan mode automatically selects the best strategy without human input",
    "Plan mode runs faster than direct execution",
    "Plan mode lets you choose between strategies before committing to changes",
    "Plan mode prevents mistakes, making review unnecessary"
  ],
  correct:2,
  explanation:"Plan mode lets you evaluate and choose between multiple refactoring strategies before any files are modified, allowing informed decision-making about architectural approaches for complex legacy code changes."
},
{
  id:269, module:4, scenario:"Code Generation with Claude Code",
  text:"You want Claude Code to generate API endpoint handlers with consistent error handling and logging. What‘s the most effective approach?",
  options:[
    "Write detailed documentation of error handling standards",
    "Ask Claude Code to follow best practices for API handlers",
    "Provide 3-4 complete endpoint examples showing error handling, logging, and expected format",
    "Use a code linter to validate generated handlers after creation"
  ],
  correct:2,
  explanation:"Providing 3-4 complete endpoint examples demonstrating error handling, logging, and expected format is the few-shot approach that most effectively teaches Claude Code concrete patterns to replicate consistently."
},
{
  id:270, module:3, scenario:"Code Generation with Claude Code",
  text:"You‘re creating a CLAUDE.md file for your project. Should user-level CLAUDE.md (~/.claude/CLAUDE.md) be version-controlled?",
  options:[
    "It depends on the file size",
    "Only if the user is a team lead",
    "No, user-level CLAUDE.md is personal and shouldn‘t be in version control",
    "Yes, all CLAUDE.md files should be version-controlled for team consistency"
  ],
  correct:2,
  explanation:"User-level CLAUDE.md (~/.claude/CLAUDE.md) contains personal preferences and configurations that are specific to an individual developer and should not be committed to version control."
},
{
  id:271, module:3, scenario:"Code Generation with Claude Code",
  text:"You have MCP servers configured in your project .mcp.json. A developer wants to add a personal MCP server that‘s specific to their workflow. Where should they configure this?",
  options:[
    "In a custom .claude/personal.json file",
    "In a .mcp-local.json that‘s git-ignored",
    "In the project .mcp.json for team consistency",
    "In ~/.claude.json for personal, non-shared configuration"
  ],
  correct:3,
  explanation:"Personal MCP server configuration belongs in ~/.claude.json (user-level config), keeping it separate from the shared project .mcp.json while still being available to the developer's Claude Code sessions."
},
{
  id:272, module:4, scenario:"Code Generation with Claude Code",
  text:"You‘re building a prompt that extracts structured data from code comments. The extraction often fails on edge cases. What technique should you use?",
  options:[
    "Use a validator tool to check extraction results",
    "Ask Claude Code to be extra careful with edge cases",
    "Increase the context window to provide more examples",
    "Provide retry-with-error-feedback: show extraction errors and ask Claude Code to fix them"
  ],
  correct:3,
  explanation:"Retry-with-error-feedback shows extraction errors and asks Claude Code to fix them, implementing a validation/retry loop that progressively improves results on edge cases through iterative correction."
},
{
  id:273, module:5, scenario:"Code Generation with Claude Code",
  text:"Your team runs Claude Code to generate different types of artifacts: code, tests, documentation, and review comments. Should each use a separate session or share one?",
  options:[
    "One shared session for efficiency and consistency",
    "The session choice doesn‘t significantly impact results",
    "Separate sessions per artifact type to keep context isolated and prevent cross-contamination",
    "Separate for complex work, shared for simple work"
  ],
  correct:2,
  explanation:"Separate sessions per artifact type keep context isolated and prevent cross-contamination, ensuring that test generation context doesn't influence documentation and review findings don't bias code generation."
}
);